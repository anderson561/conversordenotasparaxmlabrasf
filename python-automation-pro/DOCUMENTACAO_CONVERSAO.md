# Documentação de Conversão — Conversor NFS-e / Contratos ABRASF 2.01

Este documento detalha os layouts e formatos suportados pelo conversor.

---

## Layouts Suportados (PDF → XML)

O extrator PDF (`SPPdfExtractor`) conta com um motor de heurística profunda, capaz de ler as disposições textuais do PDF e classificar dinamicamente qual regra de negócio aplicar (`_detect_layout` / `_detect_layout_page`). São **45 layouts** ao todo (44 específicos + o genérico de fallback), agrupados abaixo por tipo. Obs.: o São Paulo (nº 7) tem **duas variantes dedicadas** (digital e escaneada) e o Camaçari (nº 3) tem **quatro** (digital, escaneada, Nota Avulsa da Prefeitura — nº 16d, e SISLOC/NFS-e Easy — nº 16e); Mata de São João (nº 16b), Rosário da Limeira (nº 16c), Monte Santo (nº 16f), F&F Comércio (nº 26b), PJB Construção (nº 26c), NFCom Salvador/EBJ (nº 27b) e São José/SC (nº 8b) foram adicionados fora da sequência — por isso a numeração das entradas não bate 1-para-1 com as constantes de layout (uma delas, `LAYOUT_CAMACARI_3`, é um superset interno da variante escaneada do Camaçari — nº 3 — sem entrada numerada própria, ver detalhe na seção). PDFs escaneados/imagem (sem texto extraível) passam automaticamente por **OCR** (Tesseract via `pytesseract` + PyMuPDF, `lang='por'`) antes da extração.

> **Nota sobre OCR:** o texto pós-OCR (e às vezes até o de PDF com texto embutido) diverge do que parece "óbvio" na imagem — troca de caracteres, glifos ilegíveis, colunas intercaladas. As regras por layout são propositalmente tolerantes a esses ruídos.

> **Nota de escopo:** os 45 layouts acima são todos de **NFS-e (nota de SERVIÇO)**. Um documento de **PRODUTO/mercadoria** (NF-e Modelo 55, DANFE Estadual, tributado por ICMS) é estruturalmente diferente e tem seção própria: [DANFE Estadual — NF-e de Produto (Modelo 55)](#danfe-estadual--nf-e-de-produto-modelo-55--xml-nf-e-400).

### Prefeituras / NFS-e municipal

### 1. Cuiabá/MT — `cuiaba_issnet`
- **Cabeçalho**: "Prefeitura Municipal de Cuiabá / Nota Fiscal de Serviço Eletrônica - NFS-e"
- **Número da Nota**: Canto superior direito
- **Cód. de Autenticidade**: Abaixo da data de competência (Ex: `E6679FDB0`)
- **Data de Competência**: Rótulo `Data de Competência`
- **Entidades**: Blocos "Dados do Prestador de Serviço" e "Dados do Tomador de Serviços"
- **Cidade/UF**: Rótulo `Cidade/UF` (Ex: `Lauro de Freitas/ BA`)
- **⚠️ Dois formatos de OCR (rótulo limpo vs grade escaneada)**: notas de boa qualidade têm rótulos limpos (`Número da Nota Fiscal: 205`, `Vl. Total dos Serviços: R$ ...`) e caem nos extratores genéricos. Já em **scans degradados** (ex.: consolidado "NFS PRESTADORES MTI", nota nº 134) o cabeçalho e a grade de valores saem garbleados, exigindo ramos dedicados:
  - **Número**: a caixa do número sai ilegível; o número vem **imediatamente antes de "Dados do Prestador de Serviço"** (confirmável pelo rodapé "substitui a nota nº N-1"). Sem isto o genérico pescava o `Número: 554` do **endereço do tomador** ("Avenida Praia de Pajussara Número: 554").
  - **Município do prestador**: extraído de `- Cuiabá! MT` (ou `Cidade/UF`); sem isto o resolver escolhia um IBGE errado (pegava os dígitos da Inscrição Municipal, ex.: `295033` → `2950330`, em vez de **Cuiabá 5103403**).
  - **Código de serviço**: item da LC116 na grade de atividade (`... Serviços de engenharia - 5,00 | 701 <NBS 9díg>`) → `0701`.
  - **Valores (grade)**: linha de valores lida por **posição** (`[0]`=serviços, `[3]`=base, `[4]`=Total do ISSQN); alíquota da coluna de atividade; ISS confere com base×alíquota (560×5% = 28,00); "Não/Sim" na linha define ISSQN retido. O OCR troca "Vl."→"Vi.".
  - **Cód. autenticidade**: primeiro token alfanumérico **misto** de 7-10 caracteres (ex.: `3B3DC3576`) — CNPJ/CEP/telefone são só dígitos e não casam.
- **⚠️ Vazamento do TOMADOR para o bloco do PRESTADOR (cabeçalho "Dados do Tomador" ausente)**: em scans mais degradados (ex.: página 14 do mesmo consolidado MTI, nota ANDERSON FAUSTINO/FA TELAS → São Pedro) o cabeçalho "Dados do Tomador de Serviços" some por completo do OCR. Sem ele, o bloco do PRESTADOR (delimitado genericamente até o próximo rótulo reconhecido) engole também o CNPJ/Razão/Endereço do TOMADOR até "Dados do Intermediário" — o CNPJ ainda sai certo (1º a validar), mas razão social e município saem do TOMADOR. Correção: usar a **inversão de ordem CNPJ/CPF** como âncora do início do bloco do tomador — o prestador sempre usa `CPF/CNPJ` (CPF antes), o tomador usa `CNPJ/CPF` ou `CNPJICPF` (CNPJ antes, a barra vira "I" no OCR) — independente do rótulo de cabeçalho estar legível. Quando nem essa âncora sobrevive, um fallback secundário usa o **CNPJ nu seguido de "Razão Social:"** na linha seguinte (validado: essa combinação só ocorre no bloco do tomador). **Número honesto**: quando nenhuma âncora confiável de número existe neste scan, não cai no padrão genérico `Número[:\s]+(\d+)` (que pescaria o "Número: 554" do endereço do tomador) — vai direto ao fallback de nome de arquivo/placeholder + aviso, em vez de fabricar um número plausível-porém-errado.
- **⚠️ Grade de valores truncada no zoom 3 padrão + intermediário fantasma (mesma página 14)**: além do vazamento acima, essa nota expôs mais dois problemas:
  1. A grade "Detalhamento dos Tributos" quebrava a linha de valores no meio (`R$443,80 | R$000` numa linha, `R$ 0,00` isolado bem abaixo), perdendo o Total do ISSQN e parte da Base de Cálculo — saía serviços 443,80/alíquota 0/ISS 0 quando o real (confirmado por recorte em zoom alto) era 4.113,50/2%/R$ 82,27. `_ocr_page` agora detecta a grade truncada (menos de 4 tokens `R$` na linha logo após "Vl. Total dos Serviços") e reprocessa a página inteira em **zoom 5 + PSM 6** (`_ocr_valores_cuiaba`), prependando o texto limpo — mesma técnica das demais notas degradadas; zoom 6 também recompõe a grade, mas nesse zoom específico a alíquota "2,00" cai do texto, por isso zoom 5.
  2. A tabela "Dados do Intermediário de Serviços" desta nota está **vazia** (só o cabeçalho da grade), mas faltava o delimitador para a variante **plural** "Descrição **dos Serviços**" (só havia o singular "Descrição do Serviço"); o bloco genérico vazava para o texto de descrição do serviço seguinte e pescava o **CNPJ do próprio PRESTADOR** (linha "pix para pagamento cnpj ...") como se fosse do intermediário. Corrigido o delimitador e adicionado um rótulo completo (`Dados do Intermediário de Serviços`) para consumir a frase toda. Como o intermediário é uma entidade **opcional** (ao contrário de prestador/tomador, que sempre aparecem), quando nada de fato é identificado (CNPJ cai no sentinela **e** razão fica no default genérico) o extrator agora devolve `None` em vez de fabricar um intermediário fantasma — o `<Intermediario>` some do XML.
  3. **Número**: nenhuma das duas âncoras de número sobrevivia neste scan (usuário confirmou contra o documento real que o número é **16**). Um recorte dedicado da caixa "Número da Nota Fiscal" (canto superior direito, excluindo o logo/QR "NOTA CUIABANA" ao lado — que confunde o OCR e faz o dígito variar entre zooms, ex.: "16"→"18") em **zoom 6/8/10 + PSM 7** (linha única) + whitelist de dígitos recupera o valor de forma estável; só é aceito quando **pelo menos 2 dos 3 zooms concordam** — sem consenso, segue para o fallback honesto de sempre em vez de arriscar um dígito errado. Só dispara quando as duas âncoras normais já falharam (não roda para notas já resolvidas, ex.: nº 134).
- **⚠️ Nota inteira sumindo do resultado (heurística de "nova nota" COMPARTILHADA por todos os layouts, achado no PDF "NFS PRESTADORES ANALISE DE NFS-iss e inss retido", pág. 3, nota nº 10)**: `parse_multiple`/`is_new_invoice` decide se um bloco de texto é nota nova ou continuação da anterior comparando "números" extraídos com a regex genérica `(?:Número|Nº).*?(\d+)`. Quando a caixa real "Número da Nota Fiscal" sai ilegível no OCR, essa regex cai no PRÓXIMO "Número" do texto — o do **endereço do tomador** ("Avenida Praia de Pajussara Número: 554"), igual em toda nota do mesmo tomador. Páginas de notas DIFERENTES batendo no mesmo "554" eram tratadas como continuação uma da outra — a nota seguinte nunca virava um XML próprio, ficava silenciosamente engolida (achado nas duas PDFs consolidados de Cuiabá desta sessão: recuperou 2 notas na "ANALISE" e 3 na "MTI"). Corrigido com `_numero_heuristico_bloco`: tenta primeiro rótulos específicos de "número da nota"; no fallback genérico, pula ocorrências cuja linha contém "Endereço".
- **⚠️ Prestador REAL deslocado para depois de "Dados do Intermediário de Serviços" (mesmo PDF "ANALISE", pág. 3, nota nº 10, DR3 TERCEIRIZAÇÃO → SÃO PEDRO CONSTRUTORA)**: variante ainda mais embaralhada do vazamento acima — depois de "Dados do Prestador de Serviço" vem, sem rótulo próprio, o CNPJ do TOMADOR; só depois de "Dados do Intermediário de Serviços" aparecem os dados REAIS do prestador (nome, endereço, CNPJ). Sem tratamento, prestador saía com o CNPJ do tomador e razão social quebrada, e o "intermediário" (tabela vazia) roubava os dados reais do prestador. Corrigido: quando o bloco "normal" do prestador não tem a assinatura `CPF/CNPJ` (CPF antes), procura essa assinatura no trecho entre "Dados do Intermediário de Serviços" e "Descrição dos Serviços" — só assume o deslocamento quando o trecho REALMENTE a tiver (não regride notas sem esse problema). Guard simétrico do lado do intermediário: se o bloco carrega a assinatura do PRESTADOR, devolve `None` em vez de um intermediário fantasma. Também ajustada a grade de valores: uma linha **completa** desta grade sempre tem **6 tokens `R$`** (serviços, desc. incond., deduções, base, ISS, desc. cond.) — o limiar do recut de valores (`_ocr_valores_cuiaba`) subiu de "< 4" para "< 6" tokens, pois esta nota tinha só 5 (faltava a Base de Cálculo, sem quebra de linha) e não disparava o recut antes.
- **⚠️ Extração do número da nota — endurecimento geral (achado ao consertar a nota nº 10, pág.3 do PDF "ANALISE": mesmo com os fixes anteriores, o número ainda saía `00000000`)**: três problemas distintos, todos no mecanismo de número:
  1. **Dígito espúrio isolado entre o rótulo e o valor** (nota real pág.10 do MTI, RC CONSTRUÇÕES ELÉTRICAS, número real **205**): o OCR intercala um dígito isolado numa linha própria — `"Número da Nota Fiscal\n5\n205\n"` — o "5" é ruído, "205" é o número de verdade; o padrão antigo pegava o PRIMEIRO grupo de dígitos (o "5" errado) sem nenhum aviso. Corrigido: captura até 3 grupos de dígitos após o rótulo e fica com o **mais longo** — um ruído de 1 dígito nunca vence o número real ao lado.
  2. **PSM errado no recorte dedicado do número** (`_ocr_numero_box_cuiaba`): nem PSM 6 (bloco) nem PSM 7 (linha única) é confiável sozinho — o recorte tem duas linhas (rótulo + número) e qual PSM lê melhor varia por nota (pág.14 do MTI só lê com PSM 7; nota nº 10 do "ANALISE" só lê de forma estável com PSM 6). Corrigido: vota com AMBOS os PSM em 3 zooms (6/8/10 — 6 tentativas), aceita com ≥2 votos concordando. Faixa vertical do recorte também alargada (0.065-0.098 da altura da página) — a faixa antiga (mais estreita) cortava ao meio números de 3 dígitos como "205".
  3. **Recorte dedicado agora roda SEMPRE** (não só quando as âncoras normais falham) e tem prioridade sobre a leitura de página inteira — porque a leitura de página inteira pode ler um dígito ERRADO com confiança total, sem nenhum sinal de ambiguidade. O recut de valores (`_ocr_valores_cuiaba`) também teve sua própria leitura incidental de "Número da Nota Fiscal" removida antes de ser prependado, para não competir com o recorte dedicado do número.
  - **Limite conhecido, não resolvido:** na nota GMS FLATS (pág.17), o recorte dedicado do número agora lê `9699` com consenso TOTAL (6/6 votos, todos os zooms e PSMs) — mas o número real, confirmado pela imagem, é `5639`. Mesmo um recorte cross-validado não detecta quando o Tesseract erra de forma unânime nessa nota especificamente (já sabidamente degradada — CNPJ do prestador com vírgula, intermediário residual, alíquota sem vírgula). Sem uma técnica de reconhecimento de dígito mais sofisticada (ex.: modelo dedicado), não há como distinguir algoritmicamente esse caso de um acerto real — registrado como limitação conhecida, não fabricado silenciosamente sem aviso ao usuário.

### 2. Barreiras/BA — `barreiras`
- **Cabeçalho**: "MUNICIPIO DE BARREIRAS"
- **Data de Competência**: Baseia-se no rótulo `Data Fato Gerador` ou através do recorte do Mês/Ano diretamente na **Chave de Acesso Nacional** (quando há falha estrutural de OCR nas datas).
- **⚠️ Locação de bens móveis não sujeita a ISS, emitida pelo MESMO portal (achado real 2026-07-31, nota nº 1162, OLIVEIRA & CHAVES → SÃO PEDRO CONSTRUTORA):** o portal municipal de Barreiras também emite fatura de locação (não só NFS-e tributada), com o item "00.00 - LOCAÇÃO DE BENS MÓVEIS" (código próprio do portal, não é item real da LC116) → mapeado para `0000`. **Valores em grade de 3 colunas**: "VALOR SERVIÇO (R$) DEDUÇÕES (R$) DESCONTO INCONDICIONAL" traz os 3 rótulos primeiro, só depois os valores ("4.755,00 0,00") — diferente da variante já suportada onde o valor vem colado ao próprio rótulo ("VALOR SERVIÇO (R$)\n16.473,00"). O genérico caía no fallback **zero** nessa estrutura (ERP contábil rejeitava a importação: "Valor contábil zerado para nota com situação diferente de cancelada"). Fix gateado por ausência de dígito logo após o rótulo "VALOR SERVIÇO" (a variante já funcional não é afetada); Base de Cálculo/Alíquota/ISS pela mesma técnica de grupo "rótulos-depois-valores"; Valor Líquido pelo ÚLTIMO número antes de "Chave de acesso" (fica separado do próprio rótulo por uma sequência de campos de retenção federal zerados).

### 3. Camaçari/BA — `camacari` (digital + escaneado) / `camacari_ba_scan` ⚠️ **DOIS LAYOUTS**
- **Sistema / Cabeçalho** (ambos): CPqD - Gestão Pública / "PREFEITURA MUNICIPAL DE CAMAÇARI".
- **Data de Competência**: Rótulo `Data da prestação do serviço`.
- **Discriminador**: a **origem do texto** decide o layout via `self.from_ocr`. PDF com texto embutido (pdfminer) → `camacari`. PDF **escaneado** (foto/JPG → OCR) → `camacari_ba_scan_v3` (`LAYOUT_CAMACARI_3`, layout de topo desde a nota nº 20335/PADUA COMÉRCIO), com `camacari_ba_scan` (`LAYOUT_CAMACARI_2`) preservado e ainda acessível diretamente.
- **Diferença em relação ao SP**: o `camacari` original **já tratava notas escaneadas** (grade OCR "Retenções × Totais", valores sem pontuação, CETREL). Por isso `camacari_ba_scan` (v2) e `camacari_ba_scan_v3` são **SUPERSETS em cadeia**: v3 herda todos os branches de v2 como fallback (que por sua vez herda os do `camacari` original), e cada um só sobrepõe o tratamento próprio quando dá match. Assim as notas escaneadas que já funcionavam em v2 não regridem, e cada nova família de problema (fotos de baixa qualidade em v2, número/CNPJ do prestador incorretos em v3) é atendida num superset novo em vez de arriscar o código já validado dos anteriores.
- **`camacari_ba_scan` (escaneado)**: para fotos de baixa qualidade em que a leitura padrão (zoom 3) **descarta a metade inferior inteira** da nota (grade de totais) e a caixa de cabeçalho:
  - **Re-OCR de página inteira** em zoom 4 + PSM 6 (`_ocr_camacari_scan`) recupera o corpo (grade, `Serviço: 000713`, entidades), na orientação já corrigida.
  - **Recorte dedicado do cabeçalho** (`_ocr_header_box_camacari`, zoom 6) recupera o número (ex.: `1050`, `4494`) e a data/hora de emissão — a caixa some no zoom 3.
  - **⚠️ Achado real 2026-07-31 (nota nº 4494, LAVANDERIA ÁGUA DE CHEIRO): número desaparecendo por completo, sem gerar aviso plausível de causa.** O limite superior do recorte do cabeçalho (`h * 0.045`) começava exatamente no início da linha "Data de Emissão", cortando fora a linha "Número da Nota" (rótulo + valor) inteira — o número não saía nem garbled, simplesmente não existia em lugar nenhum do texto (nem no recorte, nem na leitura de página inteira, que também perde essa caixa). Corrigido subindo o limite para `h * 0.01` (testado de 0.005 a 0.025 sem diferença no resultado — margem generosa, sem risco de também cortar as 2 linhas de baixo).
  - **⚠️ Achado real 2026-08-03 (nota nº 9100, CONCRETO FORTE → PH Gestão, pág. 14 do lote PH Gestão): número E data de emissão errados por "sangria" da célula vizinha.** Nesta nota o recorte largo do cabeçalho (`x = 0.72`) captura, logo abaixo da célula "Número da Nota / Data de Emissão / Código", a linha **"Inscrição Municipal: 0042148001"** do prestador — o PSM 6 lê essa IM (como `9042148001`) no lugar dos valores. Resultado: `9100` e `11/05/2026 12:50` **não chegavam ao texto**, o número saía `9042148001` (a IM) e a data caía no fallback "Data da prestação do serviço" (`08/05`). Corrigido de forma **aditiva** (sem mexer no recorte já validado das notas 1050/4494 → zero regressão): um **segundo recorte estreito** (`x = 0.78`) isola só a coluna da célula e recupera `9100 / 11/05/2026 12:50 / 8075HO406` (só corta a 1ª letra dos rótulos), concatenado ao recorte largo. A extração foi robustecida junto: (a) número itera **todas** as ocorrências de "…mero da Nota" e descarta candidatos de **≥8 dígitos** (comprimento de IM/CNPJ, nunca de nº de NFS-e); (b) data tolera o "D" cortado ("…ata de Emissão"); (c) código de autenticidade ganhou ramo próprio que exige **letra + dígito** (rejeita a IM puramente numérica). Nota de limitação: o "O" do código sai como "0" (`8075H0406`, ambiguidade O/0 da fonte fraca) — plausível-porém-1-char-errado, mesma classe do item abaixo; ainda assim muito melhor que a IM anterior.
  - **⚠️ Achado real 2026-08-03 (nota nº 246, AVANÇO GESTÃO → PH Gestão, pág. 29 do lote PH Gestão): número virando `00000000` por PÁGINA TORTA.** O scan desta nota está levemente inclinado (~-1°). A linha "Número da Nota" é a **mais alta** da célula do cabeçalho; com a página torta ela sai do enquadramento de **ambos** os recortes fixos anteriores — o largo (`x = 0.72`) lê só `246` solto, sem o rótulo; o estreito (`x = 0.78`) corta o rótulo para `"o da Nota"` — e em nenhum a âncora `…mero da Nota` casa, então o número desabava para o fallback honesto `00000000` (data `27/05/2026 13:46` e código `2AI9RS657` sobreviviam). Corrigido com um **terceiro recorte aditivo com deskew fino**: estima a inclinação **por página** varrendo ±3° (passo 0,25°) e maximizando a variância do perfil horizontal da faixa do cabeçalho — texto horizontal → linhas nítidas → variância máxima —, desentorta a página inteira por esse ângulo e reprocessa a **mesma caixa larga já validada** (`0.72 / 0.01–0.16`), onde o PSM 6 volta a ler rótulo+valor intercalados (`Número da Nota → 246`). É **aditivo e condicional** (só concatena quando `|ângulo| ≥ 0,25°`): páginas retas retornam o texto anterior byte-a-byte → **zero regressão** (validado contra o PDF completo: 26 notas, exatamente **uma** mudou — a 246 —, nº 9100 da pág.14 intacto). Regressão coberta por `tests/test_camacari2_pag29_skew.py` com o OCR real. Estimar o ângulo na faixa do cabeçalho (não na página inteira) mantém o custo baixo e evita que a metade inferior da nota puxe o ângulo para o lado errado.
  - **Alíquota × ISS trocados**: a grade lê "Aliquota (%) 35,75" e "ISS 6,5%", mas o real é alíquota **6,5%** e ISS **35,75**. Regra imune à troca: a alíquota é o único token seguido de `%`; o ISS é **derivado de base × alíquota**.
  - **CNPJ do tomador com 1º dígito trocado** (OCR `49...` → real `19...`): corrigido por validação do dígito verificador (`_corrige_cnpj_primeiro_digito`), testando só as 10 variações do 1º dígito e aceitando apenas quando **exatamente uma** valida.
  - **Município do prestador some** no OCR → default Camaçari (toda NFS-e municipal de Camaçari é emitida por prestador local).
  - **Código de autenticidade** impresso em fonte fraca → ilegível mesmo com recorte; fica sinalizado em `avisos` **quando cai no placeholder `XXXX-XXXX`**. Limitação conhecida (não corrigida, registrada mas não escondida): em algumas notas (ex.: nº 4494) o OCR produz um valor **plausível-porém-errado** em vez do placeholder (`TOTAM7HFA` em vez do real `70T4M7HFA` — "7"/"0" e "T"/"O" são visualmente ambíguos nessa fonte fraca), e nesse caso **nenhum aviso é gerado** para o campo.
  - **⚠️ Achado real 2026-08-10 (nota nº 20335, PADUA COMÉRCIO E REFORMA DE PNEUS → DELTALINE SERVIÇOS): número da nota e CNPJ do prestador saindo incorretos.** Nesta nota, nenhuma das 3 tentativas concatenadas do recorte de cabeçalho preserva "Número" grafado corretamente e o valor que aparece junto ("20338") sai com um dígito trocado pelo OCR — o número real só é recuperável pelo **fallback de nome do arquivo** (`NF <número>`), já existente para outros municípios (ex.: Cuiabá), agora também usado por Camaçari quando o recorte falha totalmente. Mais grave: o CPF/CNPJ do prestador saía **`24.928 188/0001-47`** (dígito trocado, checksum inválido) e o extrator genérico, ao não achar um CNPJ válido perto do prestador, caía no fallback `all_cnpjs[0]`/`[1]` e atribuía a ele o **CNPJ do TOMADOR** (mesma classe de bug já latente desde Cuiabá/GMS Flats 2026-07-29). Corrigido criando **`LAYOUT_CAMACARI_3`** (`camacari_ba_scan_v3`) — um **novo superset** que herda 100% dos branches do `LAYOUT_CAMACARI_2` como fallback (código deste **intocado**, por decisão explícita do usuário de minimizar risco de regressão nas notas já validadas) e passa a ser o layout de topo para **toda nota escaneada** de Camaçari. Muda apenas: (a) número com fallback direto ao nome do arquivo/`00000000` (nunca cai no padrão genérico frágil `Nº:`); (b) CNPJ do prestador com **validação de checksum obrigatória** — se o candidato capturado não bater o dígito verificador (e não há, como há para o tomador, um mecanismo de correção de 1º dígito), é **descartado para o sentinela `00000000000000` + aviso "Dados do prestador não identificados"** em vez de arriscar herdar o CNPJ de outra entidade do documento; (c) âncora do TOMADOR tolerante a `\bTOMADOR\b` solto (sem exigir "DE SERVIÇOS" completo); (d) razão social ancorada só no sufixo estável `Raz[ãa]o Social` (sem exigir o prefixo "Nome/", que nesta nota sai garblado de duas formas diferentes — "Nome/Razho" no prestador, "None/Razão" no tomador); (e) separador de rótulo (CNPJ/IM/logradouro/CEP) tolerante a `.` além de `:`; (f) número do endereço (`Nº`) exige pontuação explícita logo após (evita colidir com o "Nº" solto dentro de "Razão Social" garblado). Regressão coberta por `tests/test_camacari3_padua_layout.py` com o OCR real; suíte completa 191→192, zero regressão nas notas já validadas do `LAYOUT_CAMACARI_2` (código-fonte inalterado).
  - **⚠️ Achado real 2026-08-12 (nota nº 285, AVANÇO GESTÃO → PH Gestão, pág. 20 do lote PH Gestão 07/2026): número virando `00000000` por outra degradação do rótulo, distinta da nota 20335 acima.** Nesta nota, uma das 3 tentativas de recorte concatenadas perde o **"úm" inteiro** do rótulo ("nero da Nota" em vez de "número"/"...mero da Nota") — mas, ao contrário da nota 20335, o número que vem colado é **confiável**: o mesmo valor ("285") também aparece como **1ª linha solta** (sem rótulo nenhum por perto) em outra das tentativas de recorte da mesma caixa — as 3 leituras independentes concordam. A ocorrência com o rótulo **limpo** ("Número da Nota"), mais abaixo, cai no corpo do documento sem número nenhum por perto (antes do nome da prefeitura). Corrigido tolerando a âncora degradada "nero da Nota" (além da "...mero da Nota" original), mas **só** quando o candidato colado a essa variante degradada também aparece como linha isolada em **outro bloco** do texto (separado por linha em branco), fora da própria janela de busca — corrobora que é o número real, não ruído local. Essa exigência de corroboração é deliberadamente estrita: testado que bastava "o número repete em qualquer lugar do texto" para produzir um falso-positivo na PRÓPRIA nota 20335 (o fragmento `"09"` de timestamps `HH:MM` repete por coincidência, e `"014"` só repete porque a janela de busca alcança o próprio bloco onde ele aparece) — por isso a corroboração exige um bloco **fora** da janela já usada para achar o candidato. Regressão coberta por `tests/test_camacari3_numero_nota_degradado_nero.py` (texto OCR real); zero regressão (baseline-vs-fix no lote de 24 notas do mês muda só a página-alvo; suíte da nota 20335/PADUA intacta); suíte 211→212 verdes.

### 4. Salvador/BA — `salvador_ba` (Nota Salvador)
- **Cabeçalho**: "PREFEITURA MUNICIPAL DO SALVADOR" ou "NOTA SALVADOR" ou "Xique-Xique"
- **Código de Verificação**: Extração de código de autenticidade/verificação.
- **Número da Nota**: Extração nativa com fallback defensivo para o Nome do Arquivo caso o layout sofra extrema degradação.
- **Extração de Município/UF**: Tratamento robusto para remover sufixos de UF como `UF: BA` do nome do município e atualizar o endereço corretamente.
- **⚠️ Duas sub-estruturas de entidade (digital rotulada vs escaneada sem rótulo de tomador)**: na variante **digital** o tomador vem sob "TOMADOR DE SERVIÇOS" com campos "Município:/UF:" (caminho genérico). Já na variante **escaneada/OCR** (ex.: nota Cajado → São Pedro) **não há cabeçalho "TOMADOR"**: prestador e tomador são dois blocos consecutivos separados apenas pelos rótulos "Nome/Razão Social", com **ordem de campos invertida** (prestador `CPF/CNPJ → Nome → Endereço`; tomador `Nome → CPF/CNPJ → Endereço`). Sem recorte dedicado, a busca genérica pelo rótulo "TOMADOR" falhava, o bloco virava o texto inteiro e o **tomador copiava o CNPJ/nome/endereço do prestador**. Correção: quando não há rótulo de tomador, fatiamos prestador (de "PRESTADOR DE SERVIÇOS" até o 2º "Nome/Razão Social") e tomador (do 2º "Nome/Razão Social" até "DISCRIMINAÇÃO"). Endereço em texto livre `logradouro - [bairro -] município - CEP:` (município no penúltimo/último segmento; ex.: tomador em **Lauro de Freitas** IBGE 2919207). Gate por ausência do rótulo → a variante digital rotulada segue intocada.
- **⚠️ Tomador corrompido no zoom 3 → recut em zoom alto (`_ocr_tomador_salvador`)**: em scans de baixa qualidade **com** cabeçalho "TOMADOR DE SERVIÇOS" (ex.: nota nº 46, BALUARTE → SÃO PEDRO), o zoom 3 padrão corrompe o CNPJ e a razão do tomador (`03.051.741/0001-90` → `05051.74110001.00`; `SÃO PEDRO` → `es EO`), fazendo o CNPJ cair no sentinela `00000000000100`. O bloco é localizado corretamente (município/CEP certos), mas o CNPJ/razão dentro dele saem ilegíveis. Correção: um re-OCR da página em **zoom 5** (`_ocr_tomador_salvador`) recupera ambos limpos; devolve só o recorte "TOMADOR … DISCRIMINAÇÃO", que é **prependado** ao texto base para a extração genérica achar o CNPJ/razão corretos primeiro. **Gate:** só dispara quando o bloco do tomador no zoom 3 **não** tem um CNPJ bem-formado (`\d{2}.\d{3}.\d{3}/\d{4}-\d{2}`) — notas que já saem limpas pulam o custo extra. Não troca o texto inteiro (zoom 5 fragmenta a discriminação — por isso o zoom global segue 3x).
- **⚠️ Rótulo "TOMADOR" só no CORPO da discriminação (sem cabeçalho "TOMADOR DE SERVIÇOS")** (2026-08-04, nota nº 624, GEOLINER → DELTALINE, lote 06/2026): variante escaneada **sem** cabeçalho "TOMADOR DE SERVIÇOS" — o tomador só se distingue pelo **2º "Nome/Razão Social"** (mesma forma do teste `test_salvador_scan_tomador.py`, que já é tratada por um recorte dedicado ativado quando *não há* rótulo de tomador **e** existem ≥2 "Nome/Razão Social"). O quirk: a **discriminação** desta nota contém a palavra "TOMADOR" no corpo — `"-I8S RETIDO PELO TOMADOR 5% = R$450,00 (DEVIDO NA CIDADE DE CAMAÇARI- BA)"`. Como a heurística `tem_label_tomador` varria o **texto inteiro** (`_LABELS_TOMADOR` inclui "Tomador"/"Cliente" avulsos), esse "TOMADOR" do corpo a tornava verdadeira → o recorte dedicado era **pulado** e a extração genérica ancorava naquele "TOMADOR" da discriminação, fazendo o tomador sair com razão `"5% = R$450,00 (DEVIDO...)"` e CNPJ sentinela `00000000000100`. **Correção (aditiva, gated em `salvador_ba`):** a busca do rótulo do tomador passou a considerar só a região **ANTES da DISCRIMINAÇÃO** (`t[:disc_pos]`) — cabeçalho da nota. As variantes com cabeçalho "TOMADOR DE SERVIÇOS" real (antes da discriminação) continuam pulando o recorte e caindo no genérico/recut (sem regressão). Resultado: tomador = **DELTALINE SERVICOS LTDA / 01.813.680/0001-25 / Salvador-BA**. Baseline-vs-fix na própria nota + 3 testes Salvador + suíte de 179 verdes. Teste `test_salvador_tomador_no_body_label.py`. *(Escopo confirmado com o usuário: corrigir só o tomador — o `<Numero>` do prestador em linha única e a incidência do ISS "devido para Camaçari" ficaram intencionalmente como estão.)*
- **⚠️ Espaço espúrio antes do dígito verificador do CNPJ do PRESTADOR → 3 campos contaminados de uma vez** (ex.: nota nº 00000072, ORGEN ENGENHARIA → SÃO PEDRO): o OCR gera `48.310.477/0001 -08` (espaço antes do `-08`) e lê o `:` do rótulo `Nome/Razão Social:` como a letra `e`. Uma única degradação produzia três erros simultâneos: (1) **CNPJ do prestador saía igual ao do tomador** — o regex de CNPJ exigia `-` colado, não casava, e o fallback "primeiro CNPJ válido da página inteira" (`is_prestador → all_cnpjs[0]`) roubava o do tomador; (2) **Inscrição Municipal saía `483104770001`**, um pedaço do próprio CNPJ — ao remover a pontuação, `48.310.477/0001` vira um blob de 12 dígitos que passa pelo filtro de IM; (3) **Razão Social saía `e ORGEN ENGENHARIA…`**, com o glifo do rótulo colado. Correções: separadores do regex de CNPJ aceitam `[ \t]*` em volta de `/` e `-`; o candidato a IM é descartado quando é substring do CNPJ já identificado (`d not in cnpj`); e um caractere **minúsculo** solto seguido de maiúscula é removido do início da razão (só minúsculo, para não comer um `E`/`A` legítimo em caixa alta). Distinto dos dois casos acima: aqui é o **prestador** que corrompe, e o rótulo "TOMADOR DE SERVIÇOS" está legível (não passa pelo recorte sem rótulo nem pelo recut de zoom alto). Coberto por `tests/test_salvador_prestador_cnpj_ruido.py`.
- **⚠️ 4 bugs na MESMA nota, achados num review pedido pelo usuário (2026-08-10, nota nº 00006508, INTERNET TECNOLOGIA DE SISTEMAS LTDA → ITACIMIRIM PARTICIPAÇÕES, R$301,00):**
  1. **CNPJ inválido (prestador E tomador) contamina a Inscrição Municipal**: os dois CNPJs desta nota reprovam o dígito verificador (1 dígito corrompido no scan de cada um — mesma classe já vista no Camaçari nº 20335) e corretamente caem no sentinela `00000000000100` (nunca propagar CNPJ com checksum inválido, mesmo precedente do Camaçari-3). Mas o filtro de Inscrição Municipal só excluía dígitos iguais ao `cnpj` **já resolvido** (o sentinela) — não ao candidato bruto **rejeitado** — então os próprios dígitos do CNPJ reprovado vazavam pra IM (`00.073.343/001-13`, a IM real, ficava descartada). Fix: guarda a lista de todos os candidatos formatados como CNPJ perto do rótulo (válidos ou não) e exclui os dígitos deles também na busca de IM.
  2. **Endereço do prestador — `Numero` nunca separado do complemento**: o bloco Salvador-específico (que corrige logradouro/bairro/município do formato livre `logradouro - [bairro -] município - CEP:`) nunca tocava o campo `Numero`, que ficava com o lixo do split genérico por vírgula (complemento+bairro+cidade inteiros: "COND BOULEVARD SIDE EMPR SALA - CAMINHO DAS ÁRVORES - Salvador -" em vez de "290"). Fix: mesma técnica já usada no `LAYOUT_SAO_PAULO_2` — separa o 1º segmento em `<logradouro> <número>[, <complemento>]`, extraindo o número final por regex (`^(.*\D)\s+(\d+[A-Za-z]?)$`) em vez de nunca tentar.
  3. **Grade Base de Cálculo/Alíquota/Valor do ISS quebrada por ruído DENTRO do próprio rótulo**: o rótulo saiu como `"Alíquota (9%)"` (o "9" é ruído de OCR, não é dado) em vez de `"Alíquota (%)"`. As 3 regex antigas ("rótulo + primeiro número plausível depois") liam esse "9" solto como Base de Cálculo (9.00 em vez de 301.00), nunca casavam a Alíquota (exige `(%)` literal vazio → 0.00 em vez de 5.00%) e pegavam o 1º valor da linha em vez do 4º para o ISS (0.00 em vez de 15.05). Fix: casar a linha de 5 valores de uma vez só, na posição — mesma técnica já usada na grade INSS/PIS/COFINS/IR/CSLL/Outras/Líquido logo acima e no `LAYOUT_SAO_PAULO_2` — imune a ruído dentro do rótulo porque não lê dígito nenhum ali. De quebra, `ValorDeducoes` deixou de ser hardcoded em `0.0` e passou a vir da própria grade.
  4. **Código de serviço cai no fallback genérico `03115`** porque o OCR leu **"ltem"** (l minúsculo) em vez de **"Item"** no rótulo "Item da Lista de Serviços" — a regex exigia a palavra literal e nunca casava. Fix: tolera `[Il]tem` (as duas letras visualmente confundíveis nesta posição), recuperando o código real (`0105`, confirmado também pelo "Código de Tributação do Município: 0105-0/01" no rodapé da nota).
  5. **"SN"/"S/N" (sem número) colado ao logradouro do TOMADOR sem vírgula** ("RUA ALA DAS DUNAS SN") — achado ao verificar o fix #2 ponta a ponta contra o PDF real: a regex de separação de número só casava dígitos, então nesta variante (sem vírgula, "SN" grudado no fim do logradouro) o `numero` nunca era sobrescrito e ficava com o lixo do split genérico. Fix: a mesma regex agora também aceita `SN`/`S/N` como "número" válido, normalizado para `S/N`.

  Nenhum dos 5 exige layout novo — mesmo template físico das notas Salvador já cobertas, só variantes de ruído de OCR não testadas antes. Zero regressão nos 4 testes Salvador pré-existentes (1 assert em `test_salvador_scan_tomador.py` foi ajustado: o número agora sai separado do logradouro, como deveria — antes ficava colado por um efeito colateral do mesmo bug #2). Teste novo `tests/test_salvador_cnpj_invalido_endereco_e_grade.py` (7 testes, texto OCR real capturado via `_ocr_page` — o mesmo caminho usado em produção). Suíte 198→205 verdes.
- **⚠️ Recuperação automática de CNPJ com dígito errado NO MEIO do número (2026-08-10, mesma nota nº 00006508), depois de confirmar na PRÁTICA que o sentinela quebrava a importação no sistema contábil do usuário (Domínio Sistemas rejeita `00000000000100` como "CNPJ do fornecedor inválido" e bloqueia a nota inteira):** renderizando a página em PNG e lendo a imagem (mesma disciplina de "não confiar no OCR, ler a imagem"), confirmei que os DOIS CNPJs da nota são **válidos de verdade** — o OCR trocou 1 dígito no MEIO do número em cada um (prestador `...699...`→real `...688...`; tomador `...895...`→real `...885...`), não no dígito verificador como a hipótese inicial supunha. Cheguei a testar "corrigir só o dígito verificador" contra a API pública de CNPJ antes de olhar a imagem — bateu num CNPJ real, só que de **outra empresa** (Viamão/RS), confirmando que adivinhar dígito é perigoso e a disciplina de sentinela+aviso continua certa como ÚLTIMO recurso. **Fix (aditivo, só dispara quando o candidato de CNPJ perto do rótulo já reprovou o checksum):** `_ocr_recut_cnpj_invalido_salvador` localiza a N-ésima ocorrência do rótulo "CPF/CNPJ" via `image_to_data` no zoom padrão (3x) — robusto mesmo quando o próprio rótulo sai garblado ("CPEICNP A:") — e reprocessa só a LINHA DE VALORES logo abaixo (pulando a linha do rótulo) em zoom 8x. Testado e descartado: re-OCR de **página inteira** em zoom 8 recupera o CNPJ do tomador mas ainda erra o do prestador de outra forma (`"3d.288.688"`, um "4" virando a letra "d") — só o recorte ESTREITO e dinamicamente localizado deu os dois certos de forma estável. O CNPJ recuperado só substitui o candidato original (`str.replace`) se **também** passar no checksum — nunca propaga um 2º valor não validado no lugar do sentinela. Resultado: `Cnpj` do prestador e do tomador saem com os valores reais (`34288688000179`/`61229885000190`) em vez do sentinela, desbloqueando a importação. Sem teste unitário dedicado (mesmo padrão dos outros recortes de página do Salvador — `_ocr_header_box_salvador`/`_ocr_tomador_salvador` — validados contra a nota real, não mockáveis sem o PDF de origem); validado ponta a ponta via `app.py --pages` contra o PDF real.
- **⚠️ Gatilho do recut de tomador MAIS ESTRITO que a extração real → CNPJ certo shadowed por um recut errado (2026-08-12, nota nº 00011629, SAFE - SEGURANÇA ELETRÔNICA LTDA → MANUELLA CARVALHO MARTINS BAHIA, R$699,00):** o OCR da página principal já lia o CNPJ do tomador corretamente (`13.583.542/0001 -86`, só com um espaço extra antes do hífen — o mesmo tipo de ruído tolerado pela extração real via `[ \t]*` em volta do `-`). Mas o **gatilho** de `_ocr_tomador_salvador` (que decide se vale a pena re-OCRar em zoom 5) usava um regex mais estrito (`\d{2}\.\d{3}\.\d{3}/\d{4}-\d{2}`, sem tolerar esse espaço) — julgava o bloco "malformado" e disparava o recut **sem necessidade**. O recut então lia o CNPJ ERRADO (`...0001 66`, reprova checksum — confirmado via `_validate_cnpj_cpf`, contra `...0001 86` que É válido) e, por ser **prependado** ao texto (`f"{tomador_text}\n{best_text}"`), criava um **2º** bloco "TOMADOR DE SERVIÇOS" — o fatiamento genérico por `re.search` (que pega a 1ª ocorrência) encontrava esse bloco recut primeiro, e o CNPJ correto do bloco original (mais abaixo no texto) nunca era alcançado; tomador caía no sentinela `00000000000100` com aviso "Dados do tomador não identificados". **Fix:** alinhar a tolerância do regex do GATILHO à do regex de EXTRAÇÃO (mesmo `[ \t]*` em volta de `/` e `-`) — o recut só passa a disparar quando o bloco de fato não tem CNPJ bem-formado, igual à extração real já tolera. Confirmado via `_ocr_page(0)` direto contra o PDF real: só um bloco "TOMADOR DE SERVIÇOS" no texto pós-fix (sem duplicação). **Achado colateral não corrigido (fora do escopo desta correção):** `_scavenge_all_cnpjs()` (fallback genérico de resgate de CNPJ, compartilhado por várias camadas) tem uma regex candidata (`r'\d\s*[\d\.\s/\\-]{10,80}\d'`) permissiva demais — inclui espaço na classe de caracteres, então funde grupos de dígitos ADJACENTES separados só por espaço (ex.: CNPJ + Inscrição Municipal na mesma linha) num blob só, que falha o filtro `len(p) in (11, 14)` e a função retorna `[]` mesmo havendo CNPJs válidos no texto — confirmado via teste direto nesta mesma nota. Não fixado aqui porque o fix do gatilho já resolve o sintoma reportado sem precisar tocar em código compartilhado de maior risco; considerar registrar como problema latente numa consolidação futura. **Ampliação na MESMA nota — Código de Verificação com o prefixo "ALVADOR" colado:** o guard antigo do `_extrair_codigo_verificacao` (ramo `LAYOUT_SALVADOR`) exigia que o candidato tivesse ao menos 1 dígito (`re.search(r'\d', candidato)`) para aceitar — pressuposto FALSO: o código real desta nota ("ETNEWBUQ") e de um caso anterior já catalogado ("YRYSURMV") são inteiramente letras. O guard rejeitava o valor certo e caía num fallback mais permissivo que reconcatenava o fim de "Salvador" (do título, impresso bem perto do rótulo "Verificação:") com o código real, sem separador — `"ALVADORETNEWBUQ"` em vez de `"ETNEWBUQ"`. Fix: a regex de extração passa a pular explicitamente o prefixo opcional `(?:S?ALVADOR\s*)?` logo após "Verificação:", e o guard troca a exigência de dígito por uma checagem defensiva `candidato != 'ALVADOR'`. Zero regressão (nenhum teste pré-existente assertava `codigo_verificacao` para `LAYOUT_SALVADOR`); suíte 207→**211 verdes**; teste novo `test_salvador_tomador_cnpj_espaco_antes_hifen.py` (4 testes, texto OCR real capturado via `_ocr_page` já com os 2 fixes aplicados).
- **⚠️ Marca d'água diagonal cobrindo a PÁGINA INTEIRA — degradação de um tipo novo para este layout (2026-08-14, nota nº 00039029, A LIMPCANO DESENTUPIMENTO E SUCÇÃO DE FOSSAS LTDA - EPP → SOHO RESTAURANTE LTDA):** um carimbo grande e semitransparente ("...ISS DEVERÁ SER RETIDO E RECOLHIDO À PREFEITURA DE SALVADOR...") impresso na diagonal, cruzando a página inteira — distinto de todos os bugs Salvador anteriores (rabisco/marca-texto pontual, dígito único trocado, rótulo colado): aqui o padrão de pontos (halftone) do carimbo degrada o OCR em QUALQUER lugar onde cruza texto impresso. Efeitos: (1) **Código de Verificação** saía `"ALVADORLYQ"` (puro lixo); (2) **CNPJ/razão do PRESTADOR vazava para o TOMADOR** — o rótulo "PRESTADOR DE SERVIÇOS" saía como `"PRESPAD RVIÇOS"`, que nenhuma etiqueta de `_LABELS_PRESTADOR` reconhece, então o bloco genérico da entidade virava o documento INTEIRO e o único par CNPJ/razão bem formado que sobrava (o do TOMADOR) vazava para as DUAS entidades; (3) **grade de valores inteira zerada** — o rótulo "Valor do ISS" saía como `"Ne alét.do ISS"`, quebrando o regex de 5 valores, e o fallback antigo herdava `base_calculo = valor_servicos` (também zerado, porque a própria linha "VALOR TOTAL DA NOTA" saía ilegível). **Fix (4 recortes dedicados em `_ocr_page`, cada um gateado por evidência do próprio defeito, sem geometria fixa):** `_ocr_recut_codigo_verificacao_marca_agua_salvador` localiza a palavra "Salvador" que segue "Nota" (a mesma âncora "Nota Salvador <código>" ao lado do título — há OUTRA ocorrência de "Salvador" mais acima, no cabeçalho da Prefeitura) e recorta só a faixa à direita, em zoom 10x + filtro de mediana (despeculagem); `_ocr_recut_prestador_marca_agua_salvador` recorta a faixa entre o título "...ELETRÔNICA" e a palavra "TOMADOR" (ambas sobrevivem legíveis mesmo com a marca d'água) e devolve um bloco `PRESTADOR DE SERVIÇOS / CPF/CNPJ / <cnpj> / Nome/Razão Social / <razão> / Endereço` já no formato que a extração genérica reconhece — só devolve algo quando o CNPJ recuperado passa o checksum E uma razão social plausível (padrão `...LTDA/EPP/ME/EIRELI/S/A`) é encontrada, senão mantém o comportamento atual (sem regressão); `_ocr_recut_valor_total_marca_agua_salvador` localiza a linha "VALOR ... TOTAL" via `image_to_data` e recorta só ela; `_ocr_recut_grade_valores_marca_agua_salvador` localiza a linha de valores logo abaixo de cada rótulo de grade e recorta CADA CÉLULA individualmente pela própria posição do token já lido (mesmo com conteúdo errado — só a posição importa), testado e validado como a ÚNICA forma estável de recuperar Dedução/Base/Alíquota sem confundir dígitos entre colunas vizinhas. A célula do "Valor do ISS" continua ilegível mesmo isolada (watermark mais denso ali) — em vez de arriscar um dígito errado, é **derivada matematicamente** (Base × Alíquota, relação sempre válida). "Crédito Nota Salvador" e "Outras Retenções" são fixados em `0,00`: todo documento deste layout traz o texto fixo "Esta Nota Salvador não gera crédito", e "Outras Retenções" foi a única célula que NENHUMA combinação de zoom/kernel testada recuperou de forma legível — mesmo valor que o fallback genérico já usa quando a grade inteira falha, sem risco novo. Todos os 4 gatilhos são independentes e específicos (nenhum rótulo de prestador reconhecível antes de "TOMADOR" / linha "VALOR TOTAL DA NOTA" ilegível) — não disparam para nenhuma nota Salvador já coberta pelos 24 testes pré-existentes. Suíte 223→**224 verdes**; teste novo `tests/test_salvador_marca_agua_pagina.py` (texto OCR real capturado via `_ocr_page` já com os 4 fixes aplicados).
- **⚠️ Prestador trocado pelo tomador via fallback + 2 bugs colaterais achados no mesmo par de notas (2026-08-17, notas nº 2150 e 2169, INSTITUIÇÃO ASSISTENCIAL BENEFICENTE CONCEIÇÃO MACEDO → BONI TRANSPORTES; reportado pelo usuário: "está extraindo o prestador incorreto"):**
  1. **Bug principal — CNPJ do prestador saía o do TOMADOR**: `_ocr_recut_cnpj_invalido_salvador` (recorte dinâmico em zoom 8x, ver achado 2026-08-10 acima) já lia o dígito CORRETO do prestador (`584`, não o `684` do zoom 3x — conferido na imagem real), mas o 2º separador entre os grupos de 3 dígitos saía como ESPAÇO em vez de ponto (`"00.584 568/0001 -05"`); a regex do recut exigia ponto literal ali e nunca casava, devolvendo `None` mesmo com o dígito certo já lido. Sem a correção recuperada, o fallback genérico de `_extrair_entidade` ("nenhum CNPJ válido no bloco do prestador → pega o 1º CNPJ válido do documento inteiro") atribuía ao prestador o CNPJ do TOMADOR — o único que sobrava validável. **Fix:** a regex do recut passa a tolerar espaço/tab OU ponto como 2º separador, e o valor recuperado é reformatado com pontuação canônica antes de devolver (a extração downstream exige pontuação estrita).
  2. **Código de Verificação saía como a palavra "PRESTADOR"** (só na nota 2169): o rótulo "Código de Verificação:" ficava colado, sem NENHUM valor legível no meio, diretamente ao início da seção seguinte ("PRESTADOR DE SERVIÇOS") — nem o recorte dedicado (`_ocr_header_box_salvador`, hfrac 11% da altura) alcançava essa linha nesta nota (caixa de cabeçalho um pouco mais alta que o padrão). O regex específico de Salvador (que tolera o rótulo saindo truncado, ex.: "erifica[çc][aã]o") capturava a palavra do rótulo seguinte como se fosse o próprio código — mesma classe do guard "ALVADOR" já existente (achado 2026-08-12), só que para um rótulo diferente. **Fix:** `_ocr_header_box_salvador` ganhou tentativas adicionais (alturas de recorte maiores, 16%; zoom/PSM diferentes: 2150 só lê limpo em zoom 4.5/PSM6, 2169 só em zoom 8/PSM4) e passou a EXIGIR que o texto recuperado contenha um candidato plausível (não só a presença do rótulo) antes de aceitar um recorte — devolver um recorte com rótulo mas sem valor era pior que não prependar nada, pois criava um "Verificação:" pendurado que o texto principal concatenado logo em seguida podia preencher errado (achado durante o próprio desenvolvimento do fix: "PREFEITURA", início do bloco seguinte, foi capturado assim numa 1ª versão da correção). O guard de extração também passou a rejeitar explicitamente `'PRESTADOR'`/`'TOMADOR'` (mesma lista de `'ALVADOR'`). Resultado: código de verificação recuperado corretamente em AMBAS as notas (`AYYEQUUZ` na 2150 — antes caía no fallback seguro `XXXX-XXXX` — e `RKARXEE` na 2169 — antes saía `PRESTADOR`).
  3. **CNPJ/razão social/endereço do TOMADOR corrompidos** (só na nota 2169): o zoom 3 padrão lia um CNPJ com formatação PERFEITA mas dígito errado (`04.655.283/0003-50`, reprova checksum; o certo é `04.555.283/0003-50` — mesmo tomador da nota 2150, onde esse CNPJ já sai correto até no zoom padrão), razão social com lixo à direita (`"...LTDA. Pe"`) e endereço embaralhado (`"Ea ARA: QUITERIA"` em vez de `"RUA MARIA QUITERIA"`). Como o CNPJ malformado tinha pontuação sintaticamente válida, o gatilho do recut dedicado do tomador (`_ocr_tomador_salvador`, que só olhava a FORMATAÇÃO) nunca disparava — sem o recut, esse CNPJ inválido caía direto no sentinela `00000000000100` em vez de ser corrigido. **Fix:** o gatilho passou a validar também o CHECKSUM do CNPJ candidato, não só o formato; e `_ocr_tomador_salvador` passou a tentar múltiplos zooms em sequência (5x/6x/7x — 5x é o único validado contra a nota nº 46 antes deste fix), ficando com o 1º cujo CNPJ recuperado passa no checksum (zoom 6x é o que lê esta nota corretamente: CNPJ, razão social e endereço todos limpos).

  Nenhum dos 3 exige layout novo — mesmo template físico já coberto, variantes de ruído/geometria não vistas antes. Zero regressão: suíte completa 236→**241 verdes**; teste novo `tests/test_salvador_codigo_verificacao_e_tomador_2169.py` (5 testes, texto OCR real capturado via `_ocr_page`/`parse_multiple`, já com os fixes aplicados). O fix do CNPJ do prestador (#1) segue sem teste unitário dedicado, mesmo padrão dos outros recortes de página do Salvador (`_ocr_recut_cnpj_invalido_salvador` não é mockável sem o PDF de origem) — validado ponta a ponta contra os 2 PDFs reais.

### 5. Feira de Santana/BA — `feira_de_santana`
- **Cabeçalho**: "FEIRA DE SANTANA"
- **Data de Competência**: Busca do campo `Fato Gerador`.

### 6. Rio de Janeiro/RJ — `rio_de_janeiro` (Nota Carioca)
- **Cabeçalho**: "RIO DE JANEIRO" ou "NOTA CARIOCA"
- **Data de Competência**: Busca do campo `Mês de Competência`.

### 7. São Paulo/SP — `sao_paulo_sp` (digital) / `sao_paulo_sp_scan` (escaneado) ⚠️ **DOIS LAYOUTS**
- **Cabeçalho** (ambos): "PREFEITURA DO MUNICÍPIO DE SÃO PAULO".
- **Discriminador**: a **origem do texto** decide o layout, sem ambiguidade. PDF **digital** (texto embutido, pdfminer) → `sao_paulo_sp`; PDF **escaneado** (JPG/foto → OCR) → `sao_paulo_sp_scan` (`LAYOUT_SAO_PAULO_2`). Só o escaneado passa por OCR, então a flag `self.from_ocr` roteia entre os dois. **Os dois são layouts/constantes separados** — o digital fica 100% isolado das regras do escaneado.
- **`sao_paulo_sp` (digital)**: competência via rótulo `Compe:` (mês/ano, ex.: `Jan/2026`); demais campos via regras genéricas (texto limpo).
  - **Achado 2026-07-31 (nota AMIL/TEMIS):** em algumas notas reais o pdfminer extrai o texto numa ordem física **diferente da visual** — os cabeçalhos "PRESTADOR DE SERVIÇOS"/"TOMADOR DE SERVIÇOS" ficam **deslocados no meio dos próprios dados** da entidade (o CNPJ do prestador chega a vazar sozinho, antes de qualquer cabeçalho; "TOMADOR DE SERVIÇOS" só aparece depois de Nome/Razão + CPF/CNPJ + Endereço do tomador já terem passado). O extrator genérico (que delimita bloco por cabeçalho de seção) erra o alvo: CNPJ do prestador saía = CNPJ do tomador; razão social do tomador saía = bairro dele; logradouro vazio nas duas entidades; alíquota/ISS zerados (grade "rótulos em bloco, depois valores em bloco", mesmo efeito do `LAYOUT_CAMACARI_2`). Corrigido com um ramo dedicado (`_extrair_entidade_sao_paulo`), gateado pela presença do rótulo "Bairro:" (marca específica deste template, ausente do mock sintético mais simples que já cobria o caminho genérico) — usa CNPJ por ordem de aparição no documento (1º=prestador, 2º=tomador) em vez de bloco por cabeçalho, e ancora razão/endereço pelo próprio rótulo mais próximo, pulando rótulos decorativos sem valor colado.
- **`sao_paulo_sp_scan` (escaneado)**: OCR ruidoso de 2 colunas, frequentemente **rotacionado** (foto). A caixa "Número da Nota" do canto superior direito sai ilegível na página inteira (o número vira "5") → **recorte dedicado** (`_ocr_header_box_sao_paulo`, zoom 6 + PSM 6 + whitelist de dígitos, reaproveitando o ângulo de rotação já corrigido) recupera o número (ex.: `00331020`). Branches próprias: código de verificação `XXXX-XXXX` no fim da linha do RPS (evita casar "RPS Nº"→"RPSN"); item de serviço do cadastro paulistano (`02498`); valores pela grade oficial "Base de Cálculo / Alíquota (%) / Valor do ISS", ignorando os **valores-isca** do corpo (ex.: "Valor ISS: 137,06" que é o COFINS de 7,60%); discriminação limpando rótulos/Lei 12.741 vazados.
- **⚠️ Achado 2026-07-31 (nota UNIMED CNU, `sao_paulo_sp_scan`, scan de boa qualidade sem rotação):** 3 bugs, todos gateados só pra São Paulo (digital + escaneado), sem tocar no extrator genérico compartilhado por ~30 layouts:
  - **Endereço em linha única sem rótulo "Bairro:" separado** (formato `Endereço: LOGRADOURO NÚMERO - BAIRRO - CEP: ...`, diferente do caso AMIL que tinha "Bairro:" próprio): sem vírgula, o número nunca era separado do nome da rua (`R FREI CANECA 1355` ficava inteiro no logradouro); com vírgula mas o texto após ela sendo um COMPLEMENTO em vez de um número real (`RODOVIA BA 099 ESTRADA DO COCO, GUARAJUBA SHOPPING`), o 2º bit vazava inteiro pro campo "numero". Fix: o último segmento (separado por `" - "`) é sempre o bairro; um número só é aceito quando é de fato numérico (dígitos ou `S/N`) — texto genuíno vira complemento, sem fabricar um número.
  - **Intermediário fantasma**: o bloco "INTERMEDIÁRIO DE SERVIÇOS" com `CPF/CNPJ: ---- Nome/Razão Social: ----` (tudo vazio) tinha o "DE SERVIÇOS" do próprio rótulo da seção capturado como razão social válida pelo fallback genérico linha-a-linha (o rótulo "Intermediário" era reconhecido e descartado, mas "DE SERVIÇOS" sobrava como se fosse conteúdo). Fix: guard que retorna `None` quando o `CPF/CNPJ:` do bloco vem só com traços (`----`), antes da extração de razão rodar.
- **⚠️ Recorte fixo por percentual do "Número da Nota" errava a caixa em nota com cabeçalho mais compacto (achado real 2026-08-12, nota FLASH TECNOLOGIA nº 05121900, `sao_paulo_sp_scan`):** `_ocr_header_box_sao_paulo` recortava por percentual FIXO da altura da página (calibrado contra a nota BOM NEGÓCIO), mas a altura do cabeçalho acima da caixa "Número da Nota" (logo + título + "RPS Nº..." + "NOTA FISCAL ELETRÔNICA...") varia de nota para nota — nesta, o recorte caiu 1 caixa abaixo do esperado e leu "Código de Verificação" (`MKT3-B9ZH`) em vez de "Número da Nota", e a whitelist de dígitos "inventou" um número (`392`) a partir das letras. Fix: localiza a palavra "Número" dinamicamente via `image_to_data` (zoom 3x, restrito à metade direita/terço superior da página) e recorta só a linha abaixo dela em zoom alto — imune à altura variável do cabeçalho; recorte fixo antigo mantido como fallback. Também corrigido: a whitelist de dígitos também "inventa" um dígito solto a partir da borda vertical da célula (`|`→`4`) colado ao número real — extrair o MAIOR grupo contíguo de dígitos em vez de concatenar tudo (`re.sub(r'\D', '', txt)`) evita que esse dígito espúrio se cole ao número.
- **⚠️ Ampliação na MESMA nota — hora de emissão zerada por prioridade de rótulo em vez de completude do match (achado real 2026-08-12, mesma nota FLASH TECNOLOGIA nº 05121900):** a extração genérica de Data de Emissão (`_extrair_data_emissao`, compartilhada por ~30 layouts, não específica do São Paulo) tenta uma lista fixa de rótulos em ORDEM DE PRIORIDADE e retorna no primeiro que casar. Nesta nota, o aviso de substituição do RPS ("...Esta NFS-e substitui o RPS Nº 3574129 Série NFSE2, emitido em 06/07/2026") casa com o rótulo "Emitido em" (1º da lista, SEM hora) antes do rótulo "Data e Hora de Emissão" (mais abaixo na lista, mas com o timestamp completo do cabeçalho de verdade, "06/07/2026 16:41:44") — a hora saía sempre `00:00:00`. Fix: coleta TODOS os rótulos que casaram antes de decidir, e prefere o primeiro CANDIDATO COM HORA em vez do primeiro da lista — só cai no candidato sem hora se nenhum tiver hora (comportamento de todo layout com um único rótulo casando fica idêntico). Teste `test_data_emissao_prefere_horario_completo.py`; suíte 206→207.
- **⚠️ 2 bugs distintos no mesmo cabeçalho (achado real 2026-08-13, notas FLASH TECNOLOGIA nº 05114339/05210826, pasta "0001-80" 07/2026):**
  - **Rótulo "Número" corrompido em fragmentos no zoom de localização (3x):** a técnica dinâmica descrita acima (localizar a palavra "Número" via `image_to_data`) falha quando o PRÓPRIO rótulo sai OCR'd em pedaços que não casam a palavra inteira (ex.: "N?" + "daN", tokens separados) — `candidatos` fica vazio mesmo com o valor real bem legível ao lado (`05114339`, único token puramente numérico da região, fonte maior/negrito), caindo direto no recorte fixo por percentual (que, como já documentado acima, pode acertar a caixa ERRADA). Fix: quando o rótulo não é localizado, busca o valor DIRETO pela própria assinatura — o token puramente numérico (≥6 dígitos) mais ao TOPO da região (a caixa "Número da Nota" é sempre a 1ª das 3 empilhadas) — antes de cair no recorte fixo.
  - **Código de Verificação com espaço espúrio inserido no meio** (`"1 LU3-QLER"` em vez de `"1LU3-QLER"`, nota 05210826): o regex rígido `[A-Z0-9]{4}-[A-Z0-9]{4}` (sem tolerância a espaço) falhava por completo, caindo no fallback genérico da função, que reconcatenava o watermark (`"20260724u32223020000118"`) com um fragmento do rótulo "RPS Nº" (`"RPSN"`), devolvendo `"20260724U32223020000118RPSN"` em vez do código real. Fix: captura uma janela de até 15 caracteres após "emitido em `<data>`", remove qualquer espaço interno e só então casa o formato `XXXX-XXXX`. Testes `test_ocr_header_box_sao_paulo_rotulo_corrompido_usa_valor_direto` e `test_codigo_verificacao_sao_paulo2_tolera_espaco_espurio`; suíte 212→214.
- **⚠️ Valor do "Número da Nota" com ruído colado é descartado pelo `fullmatch`, caindo no recorte fixo ERRADO (achado real 2026-08-14, nota PLUXEE BENEFÍCIOS BRASIL S.A. nº 08336055, pág.23 do lote Guarajuba 07/2026):** mesma classe do bug FLASH TECNOLOGIA acima (rótulo "Número" corrompido em fragmentos que não casam a palavra inteira — aqui `'N�'`+`'da'`+`'Not'`), mas o defeito no VALOR é distinto: o token do valor saiu com uma aspa espúria colada na frente (`'"08336055'`, ruído de borda de célula do OCR — mesma classe de ruído `|`/`4` já tolerada perto do CNPJ em outros layouts), e o fallback dinâmico (busca do "token puramente numérico ≥6 dígitos mais ao topo") usava `re.fullmatch(r'\d{6,}', ...)`, que exige o token INTEIRO ser dígito puro — o candidato com a aspa era descartado mesmo tendo 8 dígitos legíveis. Sem candidato algum, caía no recorte fixo por percentual (calibrado numa nota de cabeçalho mais baixo), que nesta nota acerta a caixa "Data e Hora de Emissão" (`16/07/2026 20:32:05`) e devolve os dígitos da data+hora colados como se fosse o número (`numero` saía `"167072026203205"` em vez de `"08336055"`). Fix: trocar `fullmatch` por `search` no gatilho — aceita dígitos com ruído colado antes/depois, mantendo a exigência de 6+ dígitos CONSECUTIVOS (datas/CEPs/Inscrição Municipal, com separador de pontuação a cada 2-5 dígitos, continuam não casando — verificado sem regressão contra os 2 testes já existentes deste recorte). Teste novo `test_ocr_header_box_sao_paulo_valor_com_aspa_espuria_colada`; suíte 224→**225 verdes**.
- **⚠️ Dobra física do papel cobrindo "PREFEITURA DO" no título — cascata de 7 efeitos colaterais na mesma nota (achado real 2026-08-17, nota nº 00028202, VALESTRA NEGOCIOS E INVESTIMENTOS LTDA → MASSA ALIMENTACAO E SERVICOS S/A):** um canto da foto fisicamente curvado/dobrado cobre literalmente as palavras "PREFEITURA DO" no cabeçalho, deixando só "MUNICÍPIO DE SÃO PAULO" legível. A detecção de layout (`_detect_layout`/`_detect_layout_page`, e o gatilho do recorte de cabeçalho) exigia a frase completa "PREFEITURA DO MUNIC[IÍ]PIO DE S[AÃ]O PAULO" — sem o prefixo, a nota inteira caía em `generico` (0 notas / campos genéricos). Fix: prefixo "PREFEITURA DO " tornado opcional no regex (`(?:PREFEITURA\s+DO\s+)?MUNIC[IÍ]PIO...`). A mesma dobra ainda derruba, na MESMA nota: (1) **Data e Hora de Emissão** — recuperada por recorte dedicado ancorado no token "Emiss[ãa]o" (ou no âncora do número, se o rótulo não for achado), zoom 6, crop alto (`8x` a altura do rótulo); (2) **Código de Verificação** — o rótulo some por completo do OCR em zoom 3 (não só corrompe); busca por FORMA do próprio valor (`^[A-Z0-9]{3,6}[-.][A-Z0-9]{3,6}`) restrita ao terço superior da página (pra não colidir com um CEP de forma parecida mais abaixo), recortado numa 2ª renderização em zoom 10 (zoom 6 partilhado com número/data perdia um dígito: "BWP2-LR3I" em vez de "BWP2-LR3Z"); (3) **Endereço do prestador com 3 segmentos** (`logradouro+nº - bairro - complemento`, em vez dos 2 historicamente assumidos) — bairro passa a ser sempre o 2º segmento (não o último) quando há 3+ segmentos e o 1º não tem vírgula; um fragmento de CEP truncado pela própria dobra ("CE...") é limpo do complemento; (4) **"Município" sem acento** ("Municipio") quebrava o `relax("Município")` (que exige o "í" acentuado literal — `re.IGNORECASE` não equipara acentuado/não-acentuado) — TANTO o prestador quanto o tomador caíam no fallback de capital (Salvador/BA, `2927408`), inclusive o tomador, que É de Salvador (acerto coincidente, não genuíno); trocado por `Munic[íi]pio` direto; (5) **E-mail do tomador** com "@" lido como "Q" (`oriane.costaQnwgroup.com.br`) — sem "@" a regex genérica de e-mail nunca casava; adicionado fallback genérico (qualquer layout) reaproveitando `_corrigir_arroba_ocr` (originalmente escrito pro BioControl); (6) **Rótulo "Nome/Razão Social" com ";" em vez de ":"** — a limpeza de pontuação inicial da razão social não incluía ";" na classe de caracteres, deixando `"; MASSA ALIMENTACAO..."` como razão (bug **genérico**, não específico de São Paulo — corrigido na função compartilhada); (7) **Grade de valores (INSS/IRRF/CSLL/COFINS/PIS-PASEP) e grade de Deduções/Alíquota/ISS totalmente ilegíveis em zoom 3** — nenhum zoom/PSM único recupera as duas grades (federais precisam zoom 4/PSM 6 com recorte alto ancorado em "IRRF"; Alíquota/ISS precisa zoom 9/PSM 6 ancorado em "Alíquota"); a "Base de Cálculo" provou-se não confiável em NENHUM zoom testado, então passou a ser sempre **derivada** (`valor_servicos - valor_deducoes`) em vez de re-OCRizada; o Valor Líquido, quando há retenções federais, também passa a ser derivado (`valor_servicos - IRRF - CSLL - COFINS - PIS`) em vez do fallback antigo (`= valor_servicos`), confirmado contra a linha "Valor Líquido: R$ 17155,45" da própria discriminação. Teste `test_sao_paulo2_valestra_fold_defect.py`; suíte 236→**237 verdes**.
- **⚠️ Ampliação na MESMA nota — "Código do Serviço" some por completo do OCR, item de tributação municipal cai no fallback genérico (achado real 2026-08-18, reconferência campo a campo do XML após o fix acima):** depois de reportar o fix da dobra física como pronto, o usuário colou o XML gerado ("gerou um arquivo totalmente diferente da nota fiscal informada") — reconferindo TODOS os campos contra a imagem (não só prestador/tomador/valores, que já batiam), o único campo genuinamente errado era `ItemListaServico`/`CodigoTributacaoMunicipio`, saindo `03115` (fallback genérico) em vez do real `01899` ("Planejamento, coordenação, programação ou organização técnica, financeira ou administrativa"), claramente legível na própria imagem da nota mas AUSENTE por completo do texto OCR (nem rótulo nem valor, mesma degradação estrutural das outras grades desta nota). Recuperado estendendo o mesmo `_ocr_recut_grade_valores_sao_paulo` com uma 3ª captura na MESMA região/zoom já usado pro IRRF (zoom 4), mas com **PSM 4 em vez de PSM 6** — achado técnico novo: a mesma imagem/zoom pode precisar de PSM diferente pra sub-regiões adjacentes da mesma célula recortada (PSM 6 dá ruído nessa faixa mais larga; PSM 4, colunas, recupera a linha limpa). Teste `test_sao_paulo2_valestra_fold_defect.py` ampliado com a asserção do item; suíte permanece **242 verdes** (sem teste novo, só extensão do existente).
- **⚠️ 2ª ampliação na MESMA nota — intermediário FANTASMA sobrevivia ao guard existente por causa de um travessão em vez de hífen (achado 2026-08-18, retomando uma pendência sinalizada e não respondida no fim da sessão anterior):** o bloco "INTERMEDIÁRIO DE SERVIÇOS" desta nota vem com `CPF/CNPJ: —` / `Nome/Razão Social: —` (tudo vazio, um único travessão — em dash, `U+2014`). O guard que descarta intermediário fantasma (achado 2026-07-31, nota UNIMED CNU, ver acima) só reconhecia o placeholder `"----"` (2+ hífens ASCII, `-{2,}`) — sem casar com o travessão único, o rótulo "INTERMEDIÁRIO DE SERVIÇOS" tinha o resíduo "DE SERVIÇOS" (após o rótulo "Intermediário" ser reconhecido e descartado) tratado como razão social válida pelo fallback genérico linha-a-linha, fabricando `<Intermediario>` com CNPJ sentinela `00000000000100` e `RazaoSocial=": —"` no XML. Corrigido tolerando também 1+ caractere da família en/em dash (`U+2010`-`U+2015`, intervalo que NUNCA aparece num CNPJ real formatado) além do caso `-{2,}` original — mantendo a exigência de 2+ hífens ASCII (um CNPJ real bem formado, ex. `12.345.678/0001-01`, tem exatamente 1 hífen; exigir 1+ genérico ali geraria falso positivo). Gated aos mesmos layouts do guard original (`sao_paulo_sp`/`sao_paulo_sp_scan`), sem tocar no extrator genérico. Teste `test_sao_paulo2_valestra_fold_defect.py` ampliado com `assert nfse.intermediario is None`; suíte permanece **242 verdes** (extensão do existente, sem teste novo).

### 8. Joinville/SC — `joinville_sc`
- **Cabeçalho**: "Prefeitura de Joinville" ou "NF-em"
- **Data de Competência**: Busca do rótulo `Competência`.

### 8b. São José/SC — `sao_jose_sc`
- **Cabeçalho**: "PREFEITURA MUNICIAL DE SÃO JOSÉ" — "MUNICIAL" é erro de digitação **real** do próprio gerador do PDF desta prefeitura (não "MUNICIPAL"), preservado/tolerado como está impresso. Detecção com negative lookahead em "DOS"/"DO" logo após "JOSÉ" para não colidir com municípios homônimos qualificados (São José dos Campos/SP, São José do Rio Preto/SP etc.). PDF DIGITAL, sem OCR.
- **Achado real 2026-08-14, nota nº 348301 (INTELBRAS S/A - IND DE TEL ELET BRA, CNPJ 82.901.000/0001-27, matriz em São José/SC → SINDICATO DOS DELEGADOS DE POLICIA, Salvador/BA, R$ 178,80) — 3 quirks estruturais próprios, nenhum layout dedicado existia até então:**
  1. **Canhoto/recibo do destinatário ANTES do conteúdo da nota, separado por uma linha de 200+ hifens**: o PDF trazia "Identificação e assinatura (eletrônica ou física) do recebedor:" seguido de uma linha divisória longa, ANTES de "PREFEITURA MUNICIAL DE SÃO JOSÉ". O `parse_multiple` fatia blocos por linhas divisórias (`_{20,}|={20,}|-{20,}`) — sem tratamento, esse preâmbulo curto virava uma "nota" fantasma isolada (nº `00000000`, razão social = o próprio texto do canhoto) quando o bloco seguinte (a nota real) forçava o flush do que já estava acumulado. **Corrigido de forma GENÉRICA (não gated a este layout)**: um bloco sem NENHUM sinal de nota (CNPJ/CPF, rótulo de entidade, "Nota"/"NFS") é descartado quando `current_invoice` ainda está vazio (nenhuma nota iniciada) — restrito a esse caso para não afetar páginas de CONTINUAÇÃO de uma nota já iniciada (ex.: 2ª página do Monte Santo, que também não parece "nota nova" mas é sempre anexada DEPOIS da 1ª página).
  2. **Blocos "PRESTADOR DE SERVIÇOS"/"TOMADOR DE SERVIÇOS" com reordenação própria** (distinta do "labels dumped, depois values dumped" de Monte Santo/Guarulhos): razão social + nome fantasia vêm ANTES do bloco de 7 rótulos (Nome Fantasia/Nome-Razão Social/CPF-CNPJ/Endereço/Complemento/Município/E-mail); dos 5 valores restantes, o **Município vem REALOCADO para o INÍCIO da sequência** (ordem real: Município, CPF/CNPJ, Endereço[+Complemento na mesma sub-linha], E-mail) — sem tratamento dedicado, o parser genérico atribuiria o Município ao CPF/CNPJ e vice-versa.
  3. **CEP/UF do PRESTADOR saem deslocados para depois do cabeçalho "TOMADOR DE SERVIÇOS"** (artefato de leitura em 2 colunas do pdfminer) — o parser do prestador busca CEP/UF numa janela logo após esse cabeçalho (ANTES do nome do tomador de fato começar), e o parser do tomador isola seu próprio bloco a partir do FIM dessa janela, para não capturar o CEP/UF do prestador como se fossem próprios.
  4. **Bug lateral corrigido de passagem**: as 2 funções de entidade novas chamavam `IBGEResolver.extract_and_validate(municipio, uf)` sem passar `city_hint=municipio` — o lookup direto por nome (mais confiável que buscar dígitos no texto) nunca era acionado, e o código caía no fallback de CAPITAL do estado (São José/SC → Florianópolis, `4205407`, em vez de São José, `4216602`). Mesmo bug, "funcionando por coincidência", também existia em `_extrair_tomador_nfcom_salvador` (Salvador é a capital da Bahia, o fallback por acaso acertava) — corrigido nos 3 call sites introduzidos nesta e na sessão anterior; **não auditado** nos demais layouts que compartilham essa omissão (ver [[gotcha-layout-detection-collision]] Forma B).
  - Item LC116 no formato "N.NN" sem 3º par de desdobro ("1.05" → "0105", diferente da maioria dos layouts que trazem "NN.NN.NN"); discriminação usa o nome do produto/plano faturado ("LIC SOFT CLOUD-STANDARD 36X"), não o texto legal do item; grade de valores com ordem invertida (valores ANTES dos rótulos "Quantidade/Valor Unitário/..."), ancorada no rótulo ESTÁVEL "Quantidade" (nunca em um valor, que varia por nota); `Código de Verificação` distinto do `Código de verificação de Autenticidade` do rodapé (string mais longa, concatenação com data/sequência) — usado o primeiro, mais próximo do padrão dos demais layouts.

  IBGE de São José/SC (`4216602`) confirmado via [ibge.gov.br](https://www.ibge.gov.br/cidades-e-estados/sc/sao-jose.html) e cross-checado via fonte independente antes de registrar em `KNOWN_CITIES`. Zero regressão (layout novo + fix genérico de preâmbulo, sem gate de layout específico); suíte 227→**229 verdes**; teste novo `test_sao_jose_sc_layout.py` (2 testes, texto digital real via `pdfminer.extract_text`).

### 9. Fortaleza/CE — `fortaleza_ce`
- **Cabeçalho**: "PREFEITURA MUNICIPAL DE FORTALEZA"
- **Data de Competência**: Busca do rótulo `Competência`.

### 10. Brasília/DF — `brasilia_df`
- **Cabeçalho**: "Governo do Distrito Federal", "Secretária de Estado de Economia do Distrito Federal" ou "Coordenação do ISS"
- **Número da Nota**: Extração do campo `Número da Nota Fiscal` ou equivalente
- **Código de Autenticidade**: Campo específico extraído com padrão de 20-44 dígitos numéricos contínuos
  - Localizado na seção "Código de Autenticidade" ou "Data Emissão da DPS"
  - Exemplo: `5300010812249298570001590000000001182260517794 14799` (após limpeza: `530001081224929857000159000000000118226051779414799`)
- **Data de Competência**: Busca do rótulo `Data de Competência` ou `Data de Geração de NFS-e`
- **Entidades**: Suporte completo a extração de Prestador, Tomador e dados de serviço
- **Status**: ✅ Suportado nativamente

### 11. Simões Filho/BA — `simoes_filho_ba`
- **Cabeçalho**: "Simões Filho"

### 12. Ribeirão Pires/SP — `ribeirao_pires_sp`
- **Cabeçalho**: "Ribeirão Pires"

### 13. Campinas/SP — `campinas_sp`
- **Sistema**: NFSe Campinas (Secretaria Municipal de Finanças de Campinas).
- **Detecção**: "NFSe Campinas", "Prefeitura Municipal Campinas" ou "Nota Fiscal de Serviços eletrônica de Campinas".
- **Número/Série**: rótulo `Número / Série`, valor no formato `NNNN/L` (ex.: `1712/E` → `1712`).
- **Item de serviço**: item da LC 116/03 no formato `13.02 - FONOGRAFIA...` → `1302` (distinto do CNAE `5920-1/00`).
- **Valores**: grade "CÁLCULO DO ISSQN" — a **Base de cálculo do ISSQN** é usada como âncora do valor dos serviços, porque o OCR frequentemente corrompe o "Valor total" (ex.: `700,00` → `00,00`); o líquido é reconstruído quando a grade "VALOR TOTAL" vem truncada.
- **Optante do Simples**: detecção tolerante a "OPTANTE PELO SIMPLES" e a "OPTANTE"/"SIMPLES NACIONAL" em linhas separadas.
- **⚠️ Duas estruturas de texto para o MESMO layout** (ponto de atenção arquitetural):
  - **PDF imagem → OCR**: grade com vários campos por linha (`CPF/CNPJ NIF  Inscrição Municipal  Telefone`). Tratado por `_extrair_entidade_campinas`.
  - **PDF digital → pdfminer**: tabela de 2 colunas extraída campo a campo, com CNPJ/Nome/Endereço contíguos por entidade e os demais campos (IM, e-mail, município, telefone, CEP) num bloco posterior **com as colunas intercaladas**. Regra estável: a *N-ésima ocorrência* de cada rótulo pertence à N-ésima entidade (1ª = prestador, 2ª = tomador). Tratado por `_extrair_entidade_campinas_digital`, escolhido por detecção automática da estrutura.

### 14. Portal Nacional DANFSe — `danfse_nacional`
- **Cabeçalho**: "DANFSe v1.0", "Documento Auxiliar da NFS-e" (a detecção também casa pela âncora "Chave de Acesso", pois em DANFSe **escaneada** o OCR corrompe o cabeçalho — ex.: "DANFSo vi", "NFS-g").
- **⚠️ Detecção ANTES das marcas municipais (2026-08-05, pág. 11 do lote Guarajuba 06/2026, nota nº 3 ANA PAULA→PH GESTÃO):** a DANFSe é emitida **pela prefeitura do município**, então o cabeçalho traz "Prefeitura Municipal de \<X\>". Como o check da DANFSe Nacional ficava lá embaixo na cadeia de `_detect_layout`, a marca municipal casava ANTES (ex.: `PREFEITURA MUNICIPAL DE CAMAÇARI` roteava para `camacari_ba_scan`; outra DANFSe caiu em `rio_de_janeiro`). Roteada ao layout errado, nem o parser DANFSe nem a regra intermediário→tomador (gated em `LAYOUT_NACIONAL`) rodavam → **tomador não extraído**. Fix aditivo: um check estreito `DANFSe v\d | Documento Auxiliar da NFS-e` (strings do documento nacional padrão, ausentes dos formatos municipais próprios) no **topo** de `_detect_layout` e `_detect_layout_page`, antes de qualquer marca municipal. O check largo por "Chave de Acesso"/"Competência da NFS-e" permanece adiante como fallback de OCR severo. **Baseline-vs-fix no lote de 31 páginas mudou exatamente 3 páginas — todas DANFSe antes mal-roteadas (págs. 5 e 11 vinham de Camaçari, pág. 13 de Rio) — zero colateral.** É a Forma A do gotcha de colisões (formato nacional padrão posicionado depois de marcas municipais). Teste `test_danfse_camacari_deteccao_vs_municipio.py`.
- **Competência**: Rótulos `Competência da NFS-e` (aceita `MM/YYYY` e `DD/MM/YYYY`) com regra de prioridade sobre a *Data/Hora da Emissão* em caso de conflitos.
- **Entidades**: Suporte avançado a extração segmentada para **Prestador**, **Tomador** e **Intermediário do Serviço**, isolando perfeitamente seus respectivos CNPJs e controlando contaminações cruzadas quando campos vêm indicados como "NÃO IDENTIFICADO".
- **⚠️ Chave de Acesso como fonte de verdade (DANFSe escaneada)**: a Chave de Acesso de **50 dígitos** codifica IBGE do município + CNPJ do emitente + **número da NFS-e** (posições 24-36, zero-preenchidas). Em notas escaneadas o OCR come dígitos do número impresso ao lado do rótulo (ex.: `21` vira `2`), então o **número é decodificado da chave** (`chave[23:36].lstrip('0')`), não do rótulo. A própria chave (50 díg.) preenche o `<CodigoVerificacao>` do XML — o DANFSe **não tem** um "Código de Verificação" separado.
- **Item de serviço**: "Código de Tributação Nacional 16.02.01" → 2 primeiros pares (`1602`); o 3º par é o desdobro municipal. Sem este ramo, caía no default genérico `03115`.
- **Valores (grade "rótulo em cima / valor embaixo")**: campos vazios marcados por `-`; o `R$ n,nn` é capturado por **proximidade de cada rótulo próprio** ("Valor do Serviço", "Valor Líquido da NFS-e"). Os padrões genéricos falhavam nessa estrutura (pescavam o número da nota como ISS e deixavam o valor zerado). **MEI** ("Optante - Microempreendedor Individual"): BC/alíquota/ISS em branco → tributação zero, não retido.
- **⚠️ Regra de negócio — INTERMEDIÁRIO promovido a TOMADOR (2026-08-04, nota nº 44 pág.18 do lote Guarajuba Suítes):** algumas notas trazem, em tarja de largura total, **"TOMADOR DO SERVIÇO NÃO IDENTIFICADO NA NFS-e"** (campo em branco na origem) e listam a contratante como **INTERMEDIÁRIO DO SERVIÇO** (aqui o MEI prestador lançou a PH Gestão como intermediário e deixou o tomador vazio). Para a contabilidade, essa entidade é o tomador efetivo. Decisão do usuário: quando o tomador vem "não identificado" (CNPJ sentinela / razão "NÃO IDENTIFICADO") **e** há intermediário identificado, **promover o intermediário a tomador e esvaziar o `<Intermediario>`** (a mesma entidade não fica nos dois papéis). Regra **geral** (qualquer intermediário, não fixa a um CNPJ) e **gated por `LAYOUT_NACIONAL`**, aplicada no ponto de montagem (`parse()`). Dois fixes de apoio no parser de entidade DANFSe: (1) **município** — "Município" é cabeçalho de coluna e o valor real ("Camaçari - BA") fica na linha de valores; sem âncora, o resolver pescava "SALVADOR" do topo do documento e resolvia a capital (`2927408`). Extraído pelo padrão "<Cidade> - <UF> <CEP>" exigindo início Title Case (pula os tokens de endereço em CAIXA ALTA), resolvendo Camaçari/BA `2905701`. (2) **razão** — remove o prefixo do CNPJ com vírgula ("49.244,210 …") e a inicial isolada da coluna "E-mail" vazada no fim ("… S.A. **E**"). Zero regressão: baseline-vs-fix no lote de 21 notas muda **só a nota-alvo** (as demais, com tomador de verdade, não disparam a regra). Teste `test_danfse_intermediario_vira_tomador.py`. **Limitação conhecida (fatiamento do lote misto):** no PDF completo (26 págs, fornecedores variados) o fatiamento de `parse_multiple` ainda corta o bloco do intermediário de forma que o município volta a cair em Salvador em algumas notas, e a razão do prestador sai como "Série da DPS" — a identidade do tomador (o que foi pedido) fica correta, mas essa degradação residual é do fatiamento genérico (item separado nos "Problemas latentes"), não do parser DANFSe.
- **⚠️ Razão social do prestador saindo como o PRÓPRIO ENDEREÇO dele (2026-08-13, pág.15 do lote Guarajuba Suítes 07/2026, nota nº 4, prestador MEI ANA PAULA RIBEIRO DA SILVA/Camaçari-BA):** a grade imprime "Nome / Nome Empresarial" e "E-mail" na mesma linha de cabeçalho e, na linha de valores, o nome do MEI e o e-mail saem colados sem separador ("ANA PAULA RIBEIRO DA SILVA 77853423500 ANAPAULAENEO1 (OGMAIL.COM"). A limpeza de e-mail já tolerava o "@" corrompido pelo OCR em `Q`/`O`/`.` colados **sem espaço**, mas aqui o OCR o corrompeu em `" (O"` (espaço + parênteses + O) — padrão não reconhecido, então a linha inteira era descartada como "razão inválida" pelo `is_valid_razao`, e o fallback linha-a-linha acabava aceitando a linha seguinte (Endereço/Município/CEP, sem nenhum rótulo de ruído reconhecido) como se fosse a razão social. Fix: as duas limpezas de e-mail (`is_valid_razao` e a limpeza final) passam a tolerar `\s*(?:\(O|QO)` como forma corrompida do "@", além das já existentes. Teste `test_danfse_nacional_razao_social_email_colado.py`.
- **Retenções federais — IRRF, INSS e "Contribuições Sociais Retidas" (2026-08-17):** toda nota neste padrão traz uma seção "TRIBUTAÇÃO FEDERAL" com **IRRF**, **INSS** (rótulo "Contribuição Previdenciária - Retida") e um valor **COMBINADO** "Contribuições Sociais - Retidas" (soma de PIS+COFINS+CSLL, sem abertura individual) — nenhum dos três era extraído antes (ficavam sempre em `0.0`, mesmo com valor real impresso na nota, ex.: nº 730080, R$ 17,34 de Contribuições Sociais). Extraídos por adjacência ESTRITA rótulo→valor (rótulo, quebra de linha, `-` ou `R$ n,nn`/`R$ n.nn`) — em notas onde o pdfminer despeja todos os rótulos desta seção juntos e os valores nunca aparecem no texto extraído (achado real, ver `tests/test_danfse_intermediario_vira_tomador.py`), a âncora propositalmente NÃO casa, mantendo os campos em `0.0` em vez de atribuir o valor de outro rótulo por engano. Como não existe tag ABRASF dedicada para o valor combinado, ele vai para um campo próprio do modelo (`Valores.valor_contribuicoes_sociais_retidas`) e é somado a `OutrasRetencoes` na saída XML (`Abrasf201Transformer._outras_retencoes_total`). **Não confundir** com os rótulos vizinhos "PIS - Débito Apuração Própria"/"COFINS - Débito Apuração Própria" (débito tributário PRÓPRIO do prestador, não retenção pelo tomador) — esses continuam intencionalmente não extraídos. Teste `test_danfse_nacional_retencoes_federais.py`.

### 15. Lauro de Freitas/BA — `lauro_de_freitas_ba`
- **Detecção**: "MUNICÍPIO DE LAURO DE FREITAS" ou domínio `laurodefreitas.ba.gov.br`.
- **Item de serviço**: código municipal de 6 dígitos (item.subitem LC116 + subitem municipal, ex.: `110201`) → usamos os 4 primeiros (`1102`).
- **⚠️ Campos deslocados pelo pdfminer**: Município/UF/E-mail do **prestador** saem *depois* do cabeçalho "TOMADOR DE SERVIÇOS" (mas antes do nome do tomador). Extração dedicada particiona o texto em 3 blocos pelos cabeçalhos de seção para o tomador não herdar o município/e-mail do prestador.
- **⚠️ Variante NFTS (Nota Fiscal Eletrônica do TOMADOR de Serviços)**: neste tipo de documento (ex.: nota 2026302, BDP LOGISTICA → BONI TRANSPORTES) o cabeçalho "TOMADOR DE SERVIÇOS" vem **antes** de "PRESTADOR DE SERVIÇOS" (ordem invertida frente à NFS-e regular), e cada bloco sai completo/autocontido, sem vazamento. Assumir a ordem fixa da NFS-e regular fazia o bloco do prestador virar vazio (slice com início depois do fim) — o prestador saía inteiro como "Não Identificado". Corrigido detectando qual cabeçalho aparece fisicamente primeiro. Também nesta variante, a grade de valores (Deduções/Base/Alíquota/ISS/ISSQN Retido) pode vir **sem o prefixo "R$"** antes dos 2 primeiros números — tolerado como opcional no regex.
- **⚠️ Variante MEI/Simples com rótulo+valor na MESMA linha → valor zero + razão/município corrompidos (2026-08-05, nota nº 20264631, ALFA MEDICAL → SÃO PEDRO, pág.6 do lote "NFS HJHJ"):** duas causas independentes no mesmo scan. (1) **Valor zero**: nota MEI/Simples com Alíquota (%) e Valor do ISS "inutilizados" (art. 57 §2º I da Resolução 94 do CGSN) — a face imprime "\*" nessas 2 células; o OCR colapsa os DOIS asteriscos em UM só ao linearizar a grade ("0,00 283,39 \* Não"), e a regex estrita (4 grupos numéricos) nunca casava, zerando até a Base de Cálculo (283,39, um número real) — e a âncora "VALOR TOTAL DA NOTA FISCAL" também não sobreviveu a este OCR. Fix: fallback tolerante que exige só Dedução/Base (sempre numéricos) e trata Alíquota/ISS ausentes da região intermediária como 0,00 (fiel à face — inutilizado, não erro); com a Base correta, o fallback já existente (`val_serv = base` quando o total não é encontrado) resolve o valor sozinho. (2) **Razão/endereço/município corrompidos** (silencioso para o prestador — não dispara aviso): esta variante imprime rótulo+valor na MESMA linha ("Nome/Razão SAO PEDRO CONSTRUTORA LTDA", sem quebra), mas as regexes de Nome/Razão, Endereço e Inscrição exigiam `\n+` (quebra obrigatória) e caíam nos sentinelas "Prestador/Tomador Não Identificado"/"Não informado" com o valor real presente no texto; Bairro/Município também compartilham linha com o rótulo seguinte ("Bairro: Centro Município: LAURO DE FREITAS UF: BA"), e a captura genérica até fim-de-linha vazava o rótulo seguinte inteiro — o município corrompido não casava `KNOWN_CITIES` e caía no fallback de capital (Salvador `2927408`) em vez de Lauro de Freitas (`2919207`, Forma B do gotcha de colisões). Fix: `\n+` → `\n*` (tolera mesma linha) + captura de Bairro/Município não-greedy com lookahead pro próximo rótulo. Baseline-vs-fix no lote de 8 notas: só a nota-alvo muda, as outras 7 idênticas; suíte de 182; teste `test_lauro_freitas_mei_rotulo_valor_mesma_linha.py`.
- **⚠️ ISSQN devido em OUTRO município (obra fora da sede do prestador, LC 116/2003 art. 3º III) — 2026-08-11, nota nº 202645, ADRIAN NASCIMENTO DE JESUS → SÃO PEDRO CONSTRUTORA, obra em Salvador/BA:** a nota indica explicitamente "LOCAL DA PRESTAÇÃO DO(S) SERVIÇO(S): SALVADOR - BA" e "Competência: ... - Tributado fora do Município de Lauro de Freitas - ...", mas `Servico/CodigoMunicipio`/`MunicipioIncidencia` saíam com o município do prestador (Lauro de Freitas, `2919207`) em vez de Salvador (`2927408`) — o mecanismo de override (`_extrair_municipio_incidencia_override`) já existia mas só cobria `LAYOUT_GUARULHOS`, cuja frase-gatilho ("Tributação fora do município") e rótulo ("Local da Prestação:", sem "DO(S) SERVIÇO(S)" no meio) são levemente diferentes dos de Lauro de Freitas. Fix: estendida a mesma função para `LAYOUT_LAURO_FREITAS` com sua própria âncora textual; endereços de prestador/tomador continuam a sede de cada um, só a incidência do ISSQN muda. Teste `test_lauro_freitas_incidencia_fora_municipio.py`.

### 15b. BIO CONTROL DESINSETIZADORA (Lauro de Freitas/BA) — `biocontrol_dedetizadora`
- **Sistema**: BIO CONTROL DESINSETIZADORA LTDA (CNPJ 04.811.846/0001-62) emite pelo template próprio "DEMONSTRATIVO DA NOTA FISCAL DE SERVIÇO" — **3º sistema diferente** para o MESMO município de Lauro de Freitas/BA, ao lado da Prefeitura oficial (nº 15, `lauro_de_freitas_ba`) e da plataforma eNotas Gateway (nº 29, `password_enotas`). PDF escaneado (OCR). Detecção pelo CNPJ/razão social do emissor, não pela marca do município nem pelo título genérico do documento (2026-08-17, nota nº 202600000036345, BONI TRANSPORTES → BONI TRANSPORTES, LOGISTICA E COMERCIO LTDA, R$5.200,00, dedetização/controle de pragas).
- **Entidades**: ao contrário do padrão "prestador fixo" de outras faturas de locação (PJB/F&F/LMR/NFCom Salvador), aqui os blocos "Dados do Prestador"/"Dados do Tomador" saem limpos o bastante em zoom 3x para extração DINÂMICA das duas entidades (sem hardcode de endereço). Bairro/Município vêm colados sem separador na mesma linha ("Recreio Ipitanga Lauro de Freitas-BA...") — ancorado no nome literal "Lauro de Freitas" (município de todo emissor/cliente observado até agora); e-mail sai com o "@" lido como uma letra maiúscula solta pelo Tesseract ("administrativoObiocontrolbahia.com.br") — corrigido por um heurístico dedicado (`_corrigir_arroba_ocr`).
- **⚠️ Duas grades densas saem corrompidas na leitura de página inteira**: a linha "Tributação de Serviços" (Código LC 116 "7.13" vira "743", sem o ponto) e a dupla "Tributos Federais"/"Impostos sobre serviços ISSQN" (PIS/COFINS/IR saem com os valores trocados entre si; Alíquota ISS/Valor ISS somem por completo). Um recorte dedicado em zoom 8x de cada linha (`_ocr_recut_biocontrol`), isolado por faixa de Y, recupera os valores exatos — validado contra o render real da página (imagem, não OCR): LC116=7.13, PIS=33,80, COFINS=156,00, IR=52,00, INSS=572,00, CSLL=52,00, Alíquota ISS=5,00%, Valor ISS=260,00. O resumo em texto livre da nota ("Retencao para a Previdencia Social (11%): R$ 572,00" etc.) confirma os totais (572+52+241,80 = 865,80; 5.200,00−865,80 = 4.334,20 = Valor Líquido impresso).
- **Item de serviço**: LC116 "7.13" (Dedetização/desinfecção/desinsetização/imunização/higienização/desratização/pulverização e congêneres) → `0713`, confirmado tanto pelo recorte dedicado quanto pela discriminação real ("TERMONEBULIZAÇÃO, ATOMIZAÇÃO, CONTROLE DE BARATAS, MOSCAS, FORMIGAS, TRAÇAS DE CEREAIS E ROEDORES"). Fallback fixo "0713" caso o recorte não recupere o campo numa nota futura.
- **ISS não retido** ("Retem ISS: Não" — pago pelo prestador via guia própria, não deduzido do valor líquido), mas o valor ainda sai informado na nota (5% = R$260,00) e é extraído normalmente, não zerado.

### 16. Iaçu/BA — `iacu_nfse`
- **Sistema**: Prefeitura Municipal de Iaçu/BA via plataforma **nfservico.com.br** (NFS-e tributada, escaneada → OCR).
- **Detecção**: "PREFEITURA MUNICIPAL DE IAÇU" (tolerante ao "ç" corrompido no OCR) ou `nfservico.com.br/iacu`. Específico do município — **não** casa pela marca genérica da plataforma, para não colidir com outros municípios do mesmo SaaS.
- **⚠️ Caixa de cabeçalho + QR**: Número da nota / Data e hora / Código de Verificação ficam ilegíveis na leitura de página inteira (caixa pequena ao lado de um QR Code). Recorte dedicado do canto superior direito em zoom alto + PSM 6 (`_ocr_header_box_iacu`) recupera os três campos, prependido ao texto principal (mesmo padrão do Salvador).
- **Item de serviço**: item LC116 no formato `7.02` → `0702`.
- **Valores**: NFS-e **tributada** (ISS real, ex.: 3% sobre a base) — grade "Valor total das deduções / Base de cálculo / Alíquota / Valor do ISS / Crédito"; base/alíquota/ISS espelhados da face (diferente da família de locação).
- **Entidades**: endereço em linha única (`RUA X N, - BAIRRO - CEP: NNNNNNNN - CIDADE - UF`); parser ignora o ruído do carimbo de recebimento intercalado no bloco do tomador (o CNPJ correto é o primeiro de 14 dígitos).

### 16b. Mata de São João/BA — `mata_sao_joao_ba`
- **Sistema**: Prefeitura Municipal de Mata de São João/BA via plataforma **SAATRI** (`matadesaojoao.saatri.com.br`) — NFS-e tributada, escaneada → OCR, mas **scan de boa qualidade** (OCR limpo em zoom 3, sem rotação — bem mais simples que Camaçari/SP2).
- **Detecção**: "Mata de São João" (tolerante ao "ã" corrompido) ou `matadesaojoao.saatri`. Específico do município — **não** casa só por `saatri.com.br`, para não rotear outras prefeituras SAATRI ainda não testadas (decidido com o usuário). **Não** é gated por `from_ocr`: não há layout digital concorrente a proteger.
- **Número**: "Número da Nota ... 00000018" — zero-preenchido; removemos os zeros à esquerda (`00000018` → `18`).
- **Item de serviço**: "Classificação do Serviço (LEI 116/2003) + Desdobro: 01.01.01" — o 3º par é o desdobro municipal; usamos os 2 primeiros (`01.01` → `0101`). Ancorado no rótulo próprio para não casar com o NBS (`115021000`) logo abaixo.
- **Valores**: duas grades "rótulo-em-cima / valores-embaixo" (Serviços/Dedução/Desc.Incond./Base de Cálculo e Alíquota/ISS/ISS Retido/Desc.Cond.) + total em "Total do(s) Serviço(s) / Total Líquido". Já traz colunas de **IBS/CBS** (reforma tributária) — presentes mas zeradas; **não mapeadas** por ora.
- **Entidades**: blocos "Prestador/Tomador do(s) Serviço(s)" com linhas contíguas (razão / [fantasia] / logradouro / `Bairro - MUNICÍPIO/UF CEP` / `CNPJ Insc. Municipal`). A coluna de rótulos (`Nome/Razão Social:`, `CPF/CNPJ:`...) é dumpada em bloco separado no fim e ignorada.
- **⚠️ IBGE**: Mata de São João → **2921005**. Foi preciso registrá-lo em `IBGEResolver.KNOWN_CITIES` — sem isso o resolver caía no default **Salvador (2927408)**.
- **Simples**: a nota diz "optante **do** simples nacional" (não "pelo"); a regex genérica foi ampliada para `(?:PELO|DO)`.

### 16c. Rosário da Limeira/MG — `rosario_da_limeira_mg`
- **Sistema**: Prefeitura Municipal de Rosário da Limeira/MG via plataforma **FUTURIZE** — NFS-e tributada, **PDF digital** (pdfminer limpo, **sem OCR**).
- **Detecção**: "ROSÁRIO DA LIMEIRA" (específico do município). **Não** casa por "FUTURIZE" — plataforma usada por vários municípios; evita rotear prefeituras ainda não testadas (mesmo padrão de Iaçu/Mata, decidido com o usuário).
- **Número**: "Nº da Nota\n72/2026" → a parte antes da "/" (`72`); o resto é o ano. Ancorado no rótulo para não pegar o "Nº Integral" (`202600000000072`).
- **Item de serviço**: "Código de Trib. Nacional: 09.01.04" → 2 primeiros pares (`0901`); o 3º par é o desdobro. Ancorado no rótulo para não casar com o NBS (`1.0303.11.00`).
- **Discriminação**: o texto real ("HOSPEDAGEM") é entregue pelo pdfminer **entre** o rótulo "ART:" e o cabeçalho "DISCRIMINAÇÃO DOS SERVIÇOS" (a grade de valores vem logo após o cabeçalho, sem a descrição).
- **Valores**: grade FUTURIZE rótulo-em-cima/valor-na-linha-de-baixo; total de "VALOR TOTAL DE SERVIÇOS = R$ ..." (na mesma linha). ISS real (ex.: 2% sobre 158,40 = 3,17).
- **Entidades**: rótulos por linha ("Razão Social:" no prestador / **"Nome:"** no tomador — cuidado para não confundir com "Nome Fantasia:"); endereço em **linha única** "logradouro, nº - [extras] - bairro - CEP - MUNICÍPIO - UF", parseado de trás pra frente (UF=último, município=penúltimo, CEP pelo padrão, bairro antes do CEP) — robusto ao nº variável de segmentos (o tomador tem um "SC" extra). Quirk: bairro com **letra-espaçada** ("F R A N C I S C O B E R T O N I", todas em espaço simples) → colapsado sem inventar o espaço de palavra (`FRANCISCOBERTONI`), e só quando o segmento é de caracteres isolados.
- **⚠️ IBGE**: Rosário da Limeira → **3156452**. Registrado em `KNOWN_CITIES` — sem isso o resolver caía no default **MG Belo Horizonte (3106200)**.
- **⚠️ Tributação fora do município**: a nota é "TRIBUTAÇÃO FORA DO MUNICÍPIO" (prestação em Luís Eduardo Magalhães/BA). Por decisão do usuário, a incidência **mantém o município do prestador** (Rosário da Limeira) — igual aos demais layouts, sem alterar o transformer compartilhado.
- **Simples**: campo "Simples Nac/MEI/Outros: Simples Nacional" (não usa "optante") → tratado por regex própria; regime especial fica ausente (o campo "Reg. Especial Tributação:" vem vazio).

### 16d. Camaçari/BA — Nota Avulsa — `camacari_ba_avulsa`
- **Sistema**: **NOTA FISCAL DE PRESTAÇÃO DE SERVIÇOS (AVULSA)** Série "A", emitida diretamente pela **Prefeitura Municipal de Camaçari/BA** (Secretaria da Fazenda) — distinta das notas Camaçari via CPqD (`camacari`/`camacari_ba_scan`). Escaneada → OCR.
- **Detecção**: casa **`AVULSA` + `CAMAÇARI`** e **precede** o bloco Camaçari CPqD. A marca "AVULSA" não aparece nas notas CPqD (digital/escaneada), então não há falso positivo; e o OCR quebra "PREFEITURA MUNICIPAL DE" e "CAMAÇARI" em linhas separadas, por isso **não** se casa a frase inteira. Guarda de regressão no teste: nota CPqD sem "AVULSA" continua indo para `camacari`/`camacari_ba_scan`.
- **Número**: "...DE SERVIÇOS (AVULSA) 00000088462" → zeros à esquerda removidos (`88462`). Ancorado em "AVULSA" para não casar com o "Código Pessoa: 0000630812" do prestador.
- **Data**: "DATA DE PRESTAÇÃO: 12.06.2026" (datas com **ponto**). Não há rótulo de emissão; a competência é o mês da prestação.
- **Item de serviço**: "PE 000709" → item **7.09** da LC 116 (`0709`, 4 dígitos significativos). Ancorado no traço que separa código e descrição (o número da nota não é seguido de traço).
- **Discriminação**: linha do item logo após o cabeçalho da tabela ("...Preço Total"), no formato "1 TRANSPORTE E DESTINAÇÃO FINAL DE RESIDUO CLASSE II B 16.500,00! 16.500,00" → removidos a quantidade inicial e os dois valores finais (o "!" é ruído de borda).
- **⚠️ Valores (camada digital)**: nota **ISENTA** (alíquota 0 / ISS 0 / sem retenção — a própria nota diz "NÃO CABE RETENÇÃO NA FONTE"). O OCR troca o **1º dígito do VALOR TRIBUTÁVEL** (14.685 → **74.685**) e deixa o **VALOR LÍQUIDO em branco**, então `base`/`líquido` vêm da **camada digital** (`pdfminer`, relida no ramo de valores) — que traz os números exatos. O rótulo "TOTAL SERVIÇOS 16.500,00" sai limpo no OCR e identifica o bruto. **Decisão do usuário**: `ValorServicos` = total bruto (16.500), `BaseCalculo` = valor tributável (14.685); líquido = base.
- **Entidades**: blocos "IDENTIFICAÇÃO DO PRESTADOR/TOMADOR"; rótulos "Nome / Razão", "CPF / CNPJ:", "CEP: ... Município: ... UF:", "Logradouro: ... Nº ...", "Bairro: ...". Quirk: bairro do tomador sai **"API"** (o OCR comeu o "I" de "IAPI") — mantido fiel, sem inventar a letra.
- **IBGE**: Camaçari (**2905701**) e Salvador (**2927408**) já estavam em `KNOWN_CITIES`; nada a registrar, mas os códigos são **asseridos** no teste.
- **Código de verificação**: nota avulsa física **não** tem hash de autenticidade → cai no placeholder `XXXX-XXXX` e gera o aviso honesto "Código de verificação/autenticidade não encontrado".

### 16e. Camaçari/BA — SISLOC / NFS-e Easy (Benefix) — `camacari_sisloc`
- **Sistema**: NFS-e emitida via plataforma **SISLOC** com tecnologia **"NFS-e Easy®" da Benefix** — distinta das notas Camaçari via CPqD e da nota avulsa da Prefeitura. Detecção pelas marcas `SISLOC` / `NFS-e Easy` / `webenefix`, verificadas **antes** do check genérico de "PREFEITURA MUNICIPAL DE CAMAÇARI" (mesma nota real testada: FERIMPORTE SERVICE LTDA, NFS-e nº 24052).
- **PDF totalmente digital com ordem de texto quebrada**: o gerador do PDF desenha rótulo e valor como objetos de texto separados no fluxo interno — o `pdfminer.high_level.extract_text()` padrão despeja **todos os valores no fim do documento**, fora de ordem e sem vínculo com o rótulo. Sem correção, os campos saem vazios/zerados apesar do texto estar "todo lá".
- **Fix**: reconstrução dedicada do texto a partir das coordenadas de caractere (`_reconstruir_texto_por_coordenadas`, via `pdfminer.high_level.extract_pages` + `LTChar`) — agrupa caracteres por proximidade vertical (Y) em linhas e ordena cada linha por posição horizontal (X), reproduzindo a ordem visual real da nota. Acionada logo após a detecção do layout, substituindo `self.raw_text` antes de qualquer extração de campo.
- **Colunas de retenção/valores lado a lado**: a grade final de tributos tem 2 colunas na mesma banda de Y (retenções federais à esquerda; valores do serviço à direita) mais uma coluna decorativa de letras soltas rotacionada 90° — a reconstrução por coordenada as intercala numa mesma "linha" de texto. "Valor do Serviço" e "Valor Líquido" ficam colados ao próprio rótulo (extração direta); os demais 12 campos (PIS, COFINS, INSS, IR, CSLL, OUTRAS, Deduções, Descontos, Base de Cálculo, ISS Retido, Valor do ISS) usam extração **posicional** pela ordem fixa de aparição dos tokens "R$ ..." no bloco (mesmo princípio já usado no layout Cuiabá).
- **Código de serviço**: nota imprime "9901" (não é item real da LC116) → mapeado para `0000` (mesma convenção não-padrão de Barreiras/PJB).
- **Município de prestação**: código IBGE vem explícito no próprio texto ("Cód. de Município IBGE: 2905701") — sem depender de `KNOWN_CITIES`.

### 16f. Monte Santo/BA — `monte_santo_ba`
- **Sistema**: NFS-e digital (texto embutido, sem OCR), construída sobre o padrão nacional da NFS-e (traz "Chave de Acesso", "Série da DPS", "Número do Lote") mas com template/grade de campos própria do município ("PRESTADOR DO SERVIÇO"/"TOMADOR DO SERVIÇO"). Detecção pela frase "PREFEITURA MUNICIPAL DE MONTE SANTO", **posicionada antes** do fallback amplo `Chave de Acesso` → `LAYOUT_NACIONAL` (esta nota também traz esse rótulo, e cairia erradamente no DANFSe genérico sem essa marca municipal na frente).
- **`pdfminer` despeja rótulos e valores em blocos separados** ("labels dumped, depois values dumped" — mesmo racional já visto em Guarulhos/Campinas, mas aplicado aqui às entidades): não há par rótulo=valor na mesma linha; a extração usa âncoras posicionais fixas na ordem observada na nota real (nº 65, PEAD NORDESTE → DELTALINE).
- **Nota impressa em 2 páginas, valores só na 2ª**: a grade "VALOR TOTAL DA NOTA"/"TRIBUTAÇÃO FEDERAL" (Base de Cálculo, Alíquota, ISS, INSS retido, Optante pelo Simples) fica **inteiramente na 2ª página**, que não repete o cabeçalho da prefeitura nem número/CNPJ. Sem tratamento dedicado, essa página cai em `LAYOUT_GENERICO` e é descartada como "lixo" pelo filtro de páginas do `parse_multiple` (perdendo todos os valores), ou é tratada como o início de uma nota nova fantasma pelo `is_new_invoice` (o rodapé genérico "Nota Fiscal de Serviços" bate no padrão de início de nota). **Fix**: fingerprint próprio da 2ª página (`"Deduz Materiais?"` + `"Base de Cáculo R$"`, com o erro de digitação do próprio gerador — falta o "l" de "Cálculo") tanto na detecção de layout quanto num carve-out explícito no `is_new_invoice`, tratando-a como continuação da 1ª página em vez de nota nova.
- **Construção civil com dedução de materiais da base do ISS**: `ValorServicos` = Valor Total da Nota (bruto, com materiais); `ValorDeducoes` = Valor Total dos Materiais; `BaseCalculo` = ValorServicos − ValorDeducoes (bate exatamente na nota real: 12.000 − 7.200 = 4.800). Item da lista de serviço "07.02" (execução de obras de construção civil) → `0702`.
- **ISS retido pelo TOMADOR**: campo "Responsável pelo Pagamento do imposto: Contratante, tomador do serviço" → `iss_retido = True`. INSS retido na fonte vem da grade "Tributação Federal" (não do texto livre "INSS 11% = R$528,00" da 1ª página, que é só informativo).
- **`municipio_incidencia_override` (revisão 2026-08-10)**: a nota traz "Local do Serviço: Fora do Município" (obra fora de Monte Santo) e, em texto livre, uma linha "OBRA: <descrição>, <CIDADE>/<UF>" (ex.: "OBRA: DESVIO REDE DE ESGOTO DA CETREL, CAMAÇARI/BA"). Na implementação original (mesma data) essa cidade foi tratada como "só texto livre, frágil demais" e o override foi deixado de fora — decisão revista após um review completo do layout: a linha "OBRA:" sempre termina no formato ", <CIDADE>/<UF>" (âncora no ÚLTIMO "," da linha, tolerando vírgulas dentro da própria descrição), risco equivalente ao já aceito noutros layouts. Implementado: quando presente, a incidência do ISSQN (`Servico.MunicipioIncidencia`/`Servico.CodigoMunicipio`) vai para o município da OBRA (Camaçari, IBGE `2905701`), mantendo o cadastro/endereço do prestador em Monte Santo (IBGE `2921550`, em `IBGEResolver.KNOWN_CITIES`) intocado — mesmo padrão já usado no Guarulhos.

### Faturas de locação / serviços específicos

Layouts de emissores fixos (a razão social e o endereço do prestador são conhecidos e, em vários casos, fixados no código). Geralmente `CodigoVerificacao = "FATURA"` e item de serviço `0601` (locação de bens móveis).

### 17. Localiza — `localiza_fatura`
- Faturas de locação/revenda de serviços veiculares da Localiza Rent A Car.
- Identificado por "LOCALIZA RENT A CAR S/A" ou "FATURA / DUPLICATA".
- **Duas estruturas de texto bem diferentes, dependendo da nota (mesmo formato de PDF)**: (a) via OCR, quando a fonte embutida do PDF é ilegível para o pdfminer (texto quebrado em linhas, Tesseract) — a maioria das notas testadas; (b) via texto digital direto do pdfminer, quando a fonte é legível — tudo numa ÚNICA linha corrida, sem nenhuma quebra. As DUAS variantes, além disso, têm a ORDEM dos campos diferente entre si (e até entre notas do mesmo tipo — ver abaixo). Por isso nenhum regex do layout pode depender de `\n` ou de uma ordem fixa entre rótulos; todos usam âncoras "pegue a ÚLTIMA ocorrência antes de X" (via `finditer`) ou prefixos estáveis (ex.: logradouro sempre começa por AV/ROD/RUA/TV/...), validados contra 4 notas reais de filiais e formatos distintos.
- **CNPJ/endereço do prestador NÃO são fixos no código**: a Localiza usa um CNPJ por filial (raiz `16.670.085`, sufixo do estabelecimento). Um hardcode anterior (CNPJ/endereço de uma única filial amostrada) quebrava silenciosamente qualquer nota de outra filial com um `ValidationError` não capturado corretamente — a nota inteira era descartada (0 XMLs), sem aviso. A ordem relativa de "CNPJ - ..." e do logotipo "Localiza" TAMBÉM varia entre notas (ora um vem antes do outro) — a extração pega a última ocorrência de CNPJ e de CEP/Município/UF antes de "FATURA / DUPLICATA", não assume uma ordem fixa.
- A razão social do tomador aparece em 2 formatos: (a) quebrada em 2 fragmentos por colunas intercaladas do OCR (nome antes do rótulo "CÓDIGO:", sufixo "LTDA" depois do rótulo "CLIENTE:"); (b) já completa logo após "CLIENTE:" no texto digital sem quebras (só falta separar o sufixo societário colado, ex. "SUSTENTABILIDADELTDA"). Distinguido pela ordem entre os rótulos "CLIENTE:" e "CÓDIGO:" no texto.
- O CNPJ do tomador é buscado numa janela **depois** do endereço do tomador (buscar no texto inteiro pegaria o 1º "CNPJ:", que é o do prestador).
- "VALOR TOTAL" e o valor em R$ podem não ficar colados no texto (vencimento/condição de pagamento entre os dois, às vezes até colado sem espaço à data seguinte: "TOTAL04/05/2026") — regex tolerante a até 80 caracteres entre rótulo e valor, sem `\b` após "TOTAL" (letra→dígito não é fronteira de palavra pro regex).
- O "número" da fatura vem com um código de filial prefixado ("Nº: ACPIT - 311630") — o ERP contábil de destino rejeita "Número da NFS-e" não numérico, então extrai-se só os dígitos, descartando o prefixo (e qualquer rótulo colado sem espaço logo depois, ex. "212176CLIENTE:").
- A fatura (pág. 1) costuma vir seguida de um boleto/Pix (pág. 2) que repete "LOCALIZA RENT A CAR S/A" só como nome do beneficiário — tratado como continuação da mesma nota, não uma 2ª fatura.
- Não é uma NFS-e municipal (não tem código de verificação eletrônico) → `CodigoVerificacao = "FATURA"`, mesmo padrão dos demais layouts de locação.
- **Fix (2026-08-10, nota real ACFSA-237512, filial Agência Centro Feira de Santana)**: 5 bugs encadeados descobertos numa 3ª variante de texto (digital, 4 páginas):
  1. **Colisão de detecção com `LAYOUT_FEIRA`**: o endereço da própria filial emissora ("...FEIRA DE SANTANA - BA") disparava o check solto e genérico `FEIRA DE SANTANA` (usado por outro layout), que rodava ANTES do check do Localiza em `_detect_layout`/`_detect_layout_page` — a nota inteira caía no layout errado. Fix: reordenar, Localiza antes do check genérico de Feira de Santana (mesma classe de bug do `gotcha_layout_detection_collision`: string solta de município/rótulo genérico cedo na cadeia engole documento de outro layout que só cita a string no próprio cabeçalho/endereço).
  2. **Nota-fantasma na pág. 4**: o resumo de carros utilizados reintroduz "Localiza Rent a Car S.A." (com PONTO, não a barra "S/A" das págs. 1-2) — o carve-out de `is_new_invoice` só reconhecia a grafia com barra e deixava essa página virar uma "nota nova" fantasma. Fix: ampliar o regex do carve-out para `LOCALIZA\s+RENT\s+A\s+CAR` (sem exigir "S/A" nem "S.A." literalmente), mantendo a exclusão por "FATURA/DUPLICATA Nº" para não engolir a fatura real.
  3. **`city_hint` ausente na resolução do município por nome**: mesmo com "FEIRA DE SANTANA" e "SALVADOR" cadastrados em `IBGEResolver.KNOWN_CITIES`, as duas chamadas a `extract_and_validate` (prestador e tomador) não passavam `city_hint=mun` — a busca por nome nunca rodava e o código caía silenciosamente no fallback da capital padrão da UF (Salvador), mesmo quando a cidade certa era outra.
  4. **`servico_codigo` caindo no fallback genérico `03115`**: `LAYOUT_LOCALIZA` estava ausente da tupla que mapeia p/ `"0601"` (locação de bens móveis), apesar de já estar presente na tupla-irmã (mesmos layouts) que mapeia `codigo_verificacao == "FATURA"` — bastava adicionar o layout na tupla que faltava.
  5. Confirma o padrão de detecção "múltiplas âncoras, sem depender de ordem/formatação fixa" já descrito acima: a mesma filial usa `\n` de verdade nesta nota (diferente da variante YUI, toda em uma linha), mas o comportamento correto (âncoras via `finditer`, sem depender de posição) já cobria isso sem exigir mudança.
  - Nenhuma mudança de comportamento para as 2 variantes de texto pré-existentes (OCR Trade Center Pituba, digital YUI Agência Centro Cabula) — suíte de regressão ampliada de 2 para 4 testes no mesmo arquivo `tests/test_localiza_layout.py`.

### 18. CPE Tecnologia — `cpe_locacao`
- Fatura de locação; detecção por "CPE BAHIA" ou "cpe tecnologia".

### 19. Guincho Cidade — `guincho_cidade`
- Fatura de locação; detecção por "GUINCHO CIDADE".

### 20. B.F. Serviços Ambientais — `bf_ambientais`
- Fatura de locação; detecção por "B.F. SERVIÇOS AMBIENTAIS" (com/sem cedilha).

### 21. LMR Engenharia — `lmr_engenharia`
- Fatura/duplicata; detecção por "LMR ENGENHARIA" (tolerante a OCR: "LTR"/"L.M.R.").

### 22. Geração & Energia — `geracao_energia`
- Fatura de locação; detecção pelo CNPJ `03.292.008/0001-67`.

### 23. Locontainers — `locontainers`
- Locação de containers (Vidal Locação); detecção por "LOCONTAINERS", "VIDAL LOCAÇÃO" ou CNPJ `00.111.704`.

### 24. SUL&SEG — Nota de Cobrança — `sulseg_cobranca`
- **Nota de Cobrança privada** de locação de bens móveis (equipamento de alarme), distinta da NFS-e prefeitural da mesma empresa. Traz "OPERAÇÃO NÃO SUJEITA AO I.S.S.".
- Detecção por "NOTA DE COBRANÇA" + CNPJ da emitente (`18.294.792`). Prestador fixo; tomador extraído. Número ancorado em "NOTA DE COBRANÇA Nº" (evita colidir com o rótulo genérico "NÚMERO").

### 25. Fatura de Locação Genérica — `fatura_locacao_generica`
- Cobre **qualquer** "FATURA DE LOCAÇÃO" ainda não catalogada por emissor específico, parseando **locadora e locatário direto do texto** (sem hardcode).
- Detecção ancorada em "FATURA DE LOCAÇÃO", posicionada **por último** nas duas cadeias (depois de todos os emissores específicos e layouts municipais) para não "roubar" a detecção deles.

### 26. ARMAC — `armac_locacao`
- Fatura de locação de equipamentos pesados da ARMAC (CNPJ `00.242.184`), **PDF 100% imagem** (escaneado), com **tabela multi-item**.
- Detecção por CNPJ/"ARMAC" **antes** do genérico de locação (estrutura própria: blocos "Dados do Locador/Tomador", grade de equipamentos). A leitura padrão embaralha a grade; um **re-OCR dedicado da página inteira em zoom 4x + PSM 6** (`_ocr_armac`) recupera "Valor total", datas, CNPJs e endereços.

### 26b. F&F Comércio — `ff_locacao`
- Fatura de locação de CFTV da F&F Comércio e Serviços de Telecomunicações de Segurança Eletrônica LTDA, **PDF 100% imagem** (escaneado).
- **Detecção pelo CNPJ do emissor** (`13.398.812/0001-89`), não pela frase "FATURA DE LOCAÇÃO": o layout de 2 colunas do OCR quebra essa frase em duas linhas, intercalada com o nome da empresa ("...SEGURANÇA FATURA DE" / "ELETRONICA LTDA LOCAÇÃO") — a marca genérica de locação nunca casa nesta nota.
- **⚠️ Leitura padrão (zoom 3x, objeto PIL em memória) perde mais da metade do conteúdo desta nota específica** (558 de 1016 caracteres reais — some o rótulo "RAZÃO SOCIAL", o bloco "ENDEREÇO"/"CNPJ/CPF" do tomador e a tabela de itens inteira). O MESMO pixmap, passado ao Tesseract por **caminho de arquivo** em vez de objeto PIL em memória, recupera o texto completo — provável diferença de metadado de DPI mudando o pré-processamento interno do Tesseract. Isolado como recut de layout específico (`_ocr_recut_ff_locacao`, disparado quando o CNPJ da F&F já foi reconhecido no texto degradado, só troca se o resultado for estritamente mais completo) — os outros ~35 layouts que já usam OCR continuam no caminho por objeto, sem alteração.
- **Prestador (locadora) fixo no código**: CNPJ, razão social, endereço e telefone, mesmo padrão dos demais locadores de filial única (LMR/Geração/Locontainers).
- **Tomador extraído do bloco "DESTINATARIO"**: razão social mantém o código de cliente colado ("7396 - Boutique Guarajuba PH Gestão"), sem inventar separação que a nota não delimita; endereço é o nome do próprio estabelecimento (não rua+número tradicional) — extraído como está.
- **⚠️ Campo "VALOR TOTAL DA FATURA" com placeholder de template não substituído na própria nota-fonte** (não é erro de OCR — confirmado na imagem renderizada: `R$ #venda_valor_total#`, bug do sistema de faturamento do emissor). O valor real vem da tabela de itens (coluna "Valor Liquido"; se uma nota futura vier com o campo do cabeçalho devidamente preenchido, ele é usado primeiro).
- Não é NFS-e municipal (operação não sujeita a ISS) → `CodigoVerificacao = "FATURA"`, item de serviço `0601`, mesmo padrão dos demais layouts de locação.

### 26c. PJB Construção — `pjb_locacao`
- Fatura de locação de máquinas (locação de bens móveis, **sem incidência de ISS**) da **PJB Construção Aluguel de Máq. e Ser. LTDA** (CNPJ `08.885.357/0001-06`, Simões Filho/BA), **PDF 100% imagem** (escaneado). Nota real de referência: nº **22980**, locação de equipamento para bombeamento de concreto para PH Gestão e Consultoria S.A., R$ 1.050,00.
- **Detecção no TOPO da cadeia de layout**, e por **dois critérios combinados**: (1) marca do emitente — razão `PJB CONSTRU...` (o CNPJ `08.885.357` **não sobrevive ao OCR padrão** desta página) — **E** (2) um marcador **estrutural** da fatura (`DESTINATÁRIO` ou `NATUREZA DA OPERAÇÃO`). Os dois critérios são necessários porque:
  - a frase "FATURA DE LOCAÇÃO" chega **garblada** no OCR ("FATURA Dl Nº" + quebra de linha), então não se pode depender dela nem do fallback genérico de locação;
  - o texto cita **"SIMÕES FILHO", "CAMAÇARI" e "MONTE GORDO"** (cidades do emitente/tomador) — se a detecção ficasse mais abaixo, esses nomes disparariam antes os **layouts municipais homônimos** (Simões Filho/BA em especial);
  - a **planilha-resumo mensal** do mesmo PDF *lista* "PJB CONSTRUCAO ALUGUEL..." como uma linha de fornecedor — sem o marcador estrutural, essa linha casaria e geraria uma **nota-fantasma vazia**.
- **Prestador (locadora) fixo no código**: CNPJ, razão social e endereço (Via Acesso II BR 324, CIA Sul, Simões Filho/BA, CEP 43700-000), mesmo padrão dos demais locadores de filial única. **Simões Filho/BA registrada no `IBGEResolver.KNOWN_CITIES`** (IBGE `2929206`).
- **Tomador extraído do bloco "DESTINATÁRIO/REMETENTE"**: razão social + CNPJ da mesma linha; endereço da linha logo abaixo do rótulo "Endereço Bairro/Distrito" (o `0`/`O` após a vírgula é ruído → `numero = "S/N"`). **Município assumido Camaçari/BA** (Guarajuba/Monte Gordo é distrito de Camaçari) — decisão confirmada com o usuário. O **bairro vem impresso truncado pelo próprio scan** ("GUARAJUBA (MONTE GOR") e é extraído como está, sem completar/fabricar.
- Locação de bens móveis (não sujeita a ISS) → base/alíquota/ISS = `0`, item de serviço `0000` (mesmo critério do Barreiras para esse tipo de operação). Fatura não tem código de verificação de NFS-e → fica vazio + aviso honesto (o de "valor zero" **não** aparece, pois o valor é extraído).

### Outros documentos fiscais

### 27. NF-e de Serviço de Comunicação (Telecom) — `telecom_comunicacao`
- **Cabeçalho**: "NOTA FISCAL DE FATURA DE SERVIÇO DE COMUNICAÇÃO" — este título tem prioridade sobre qualquer CNPJ de emissor específico já cadastrado para outro layout (ver ⚠️ abaixo), porque o MESMO CNPJ pode emitir tanto NFS-e municipal quanto fatura de comunicação.
- CNPJ do emitente decodificado da chave de acesso de 44 dígitos (quando o OCR preserva a chave) ou, em último caso, do 1º CNPJ bem-formado do texto que NÃO esteja anexado ao rótulo "CNPJ/CPF" do tomador; total via "TOTAL A PAGAR"; BC/alíquota de ICMS mapeados.
- **⚠️ Layout sem teste nenhum até 2026-08-12, achado real (nota nº 31696, F&F Comunicações/Grupo F&F CNPJ 13.398.812/0001-89 → Boutique Guarajuba/PH Gestão, R$558,40) — 6 bugs na MESMA nota, todos a partir de UM pedido do usuário pra revisar se essa nota (que É da F&F, mas um documento diferente do `ff_locacao`) já era tratada:**
  1. **Colisão de detecção com `ff_locacao`**: o CNPJ da F&F emite tanto a fatura de locação de CFTV (`ff_locacao`) quanto esta fatura de comunicação — o check do CNPJ vinha ANTES do check do título "NOTA FISCAL DE FATURA DE SERVIÇO DE COMUNICAÇÃO" na cadeia de `_detect_layout`/`_detect_layout_page`, então uma leitura de OCR limpa do CNPJ (sem o ruído do bug #2 abaixo) roteava esta nota para `ff_locacao` por engano — estrutura totalmente incompatível (o parser de `ff_locacao` espera um bloco "DESTINATARIO", inexistente aqui). Corrigido excluindo o match do CNPJ quando o título da fatura de comunicação está presente — o título (mais específico) vence.
  2. **CNPJ do prestador saía IGUAL ao do tomador**: o CNPJ da F&F sai do OCR como "13.398,812/0001-89" (vírgula em vez de ponto) — não batia a regex antiga do fallback "1º CNPJ bem-formado do texto", que pulava direto pro 2º CNPJ (o do TOMADOR, labelado "CNPJ/CPF:"). Corrigido tolerando essa vírgula E excluindo explicitamente qualquer candidato precedido do rótulo "CNPJ/CPF" (esse é sempre do tomador).
  3. **Leitura padrão (zoom 3x) perde a coluna direita inteira do cabeçalho**: "NOTA FISCAL Nº", "DATA DE EMISSÃO", "REFERÊNCIA (ANO/MÊS)", "VENCIMENTO" e "TOTAL A PAGAR" não apareciam no texto — número caía no fallback genérico solto (pescava "nº 765" da "Resolução ANATEL nº 765/2023" citada no rodapé, em vez do número real 31696), data de emissão caía em `datetime.now()`, competência ficava sem resolver. Corrigido com um novo recorte dedicado (`_ocr_recut_telecom_comunicacao`, re-OCR da página inteira em zoom 6x, PREPENDADO — não substitui, mesma técnica segura do Cuiabá/Salvador) que recupera todos esses campos limpos (mesma classe de bug do Guarulhos/ARMAC: zoom baixo perde conteúdo específico desta nota).
  4. **Total a pagar com rótulo colado e sem vírgula decimal**: numa das variantes de OCR da coluna recuperada pelo recorte, o rótulo sai colado sem espaço ("TOTALAPAGAR:R$55840", sem vírgula) — sem tolerância, o valor sairia R$55.840,00 (100x o real, R$558,40). Corrigido tolerando o rótulo colado e, quando o valor capturado não tem separador decimal nenhum, tratando os 2 últimos dígitos como centavos.
  5. **Endereço do tomador vazando o do PRESTADOR**: o tomador não tem "Rua/Av" no próprio endereço (só o nome do bairro/praia, "Guarajuba") — a janela de busca alcançava (e "roubava") a "Rua Senhor do Bonfim..." do prestador, impressa mais acima. Corrigido restringindo a janela para não voltar antes do início do próprio nome do tomador. Achado colateral na mesma correção: o regex de município (`\w[\w\s]+` — o `\s` também casa quebra de linha) vazava o bloco inteiro acima colado ao município real quando não havia "Rua/Av" pra ancorar a janela; corrigido confinando o regex a uma única linha (mesmo fix aplicado ao prestador, por consistência).
  6. **Nomes corrompidos pelo recorte de zoom alto (bug #3) quando prependado**: o recorte tem suas PRÓPRIAS colunas fundidas/ruidosas — a 1ª ocorrência de "CNPJ/CPF" no texto combinado é a cópia garblada do recorte, não a da leitura padrão (mais limpa, que vem depois). Sem preferir a 2ª ocorrência, o nome do tomador saía como um fragmento de ruído do recorte ("Ds nn RR )") em vez de "Boutique Guarajuba PH Gestao", e o nome do prestador saía com ruído de pontuação colado nas pontas ("; Grupo FeF ." em vez de "Grupo FeF", pois o guard que pulava o título "DOCUMENTO AUXILIAR..." usava `re.match` — ancorado no início da linha — e não pulava a variante com ruído solto ANTES do título, ". | DOCUMENTO AUXILIAR..."). Corrigido usando a ÚLTIMA ocorrência do rótulo "CNPJ/CPF" para o nome do tomador, `re.search` em vez de `re.match` para o guard do prestador, e um trim leve de pontuação nas pontas do nome do prestador.

  Decisão do usuário: manter a razão social do prestador como vem do OCR ("Grupo FeF", nome comercial/fantasia — não hardcoded, ao contrário do `ff_locacao`, que já tem a razão social oficial fixa no código para o mesmo CNPJ). Zero regressão (layout não tinha teste nenhum antes); suíte 211→**218 verdes**; teste novo `test_telecom_comunicacao_ff_layout.py` (7 testes, texto OCR real capturado via `_ocr_page`). Discriminação (campo textual, não fiscal) permanece ruidosa nesta nota — fora do escopo aprovado, registrado como ponto de atenção para uma futura revisão.

### 27b. NFCom Salvador (EBJ) — `nfcom_salvador`
- **Cabeçalho**: "DOCUMENTO AUXILIAR DA NOTA FISCAL FATURA DE SERVIÇOS DE COMUNICAÇÃO ELETRÔNICA" — NFCom (Nota Fiscal de Serviço de Comunicação Eletrônica), padrão NACIONAL hospedado no portal SVRS (`dfe-portal.svrs.rs.gov.br/NfCom`), PDF DIGITAL (sem OCR). Estruturalmente distinto de uma NFS-e ABRASF: tributado por **ICMS**, não ISS; chave de acesso de 44 dígitos própria do modelo 62 (NF-e/NFCom), não a chave de 50 dígitos da DANFSe/NFS-e Nacional.
- **Emitente**: Empresa Baiana de Jornalismo S.A. (EBJ, CNPJ 14.583.041/0001-62, Salvador/BA) — detecção **específica** deste CNPJ (decisão do usuário), não pela marca genérica do documento, para não capturar futuras NFCom de outros emitentes/UFs sem revisão dedicada. Prestador é **fixo** (endereço do próprio letterhead hardcoded — mesmo racional de `pjb_locacao`/`ff_locacao`).
- **⚠️ Layout novo, criado 2026-08-14 a partir de um achado real (nota nº 624, SIND DELEGADOS DE POLICIA DO EST DA BAHIA, R$ 400,00, pág. do lote sindicato 07/2026) — sem esta detecção, a nota caía no fallback amplo "Chave de Acesso" → `danfse_nacional` (a NFCom também traz esse rótulo), e o parser da DANFSe (NFS-e ABRASF) não serve para a estrutura de uma NFCom:**
  1. **Valor zerado**: `_extrair_valores` da DANFSe procura rótulos como "Valor Total dos Serviços", ausentes nesta nota — saía `valor_servicos = 0.0` com o aviso "Valor dos serviços extraído como zero". Corrigido lendo o rótulo real "TOTAL A PAGAR (R$)".
  2. **Tomador não identificado**: o CNPJ saía como sentinela `00000000000100` e a razão social saía `"Nº TELEFONE"` (um RÓTULO vazado do bloco do destinatário, não o valor real). Causa: no bloco do destinatário, os RÓTULOS ("NOME DO DESTINATÁRIO:"/"END.:") vêm em uma ordem, mas os VALORES vêm em ordem PARCIALMENTE INVERTIDA (o valor do endereço aparece ANTES do valor da razão social) — o parser genérico/DANFSe, que assume par rótulo=valor na mesma ordem, não dá conta. Corrigido com um extrator dedicado (`_extrair_tomador_nfcom_salvador`) ancorado nos dois extremos do bloco ("END.:" até "NOTA FISCAL FATURA"), lendo as 2 linhas de conteúdo na ordem em que realmente aparecem.
  3. **Discriminação/código de serviço genéricos**: saíam com o texto padrão "Serviços prestados conforme nota fiscal." e o código LC116 "03115" (herdado do fallback DANFSe), sem relação com o item real da fatura ("VEICULACAO PUBLICIDADE JORNAL IMPRESSO"). Corrigido lendo a tabela "ITENS DA FATURA" e usando `"0000"` para o código de serviço (não é item real da LC116 — mesma convenção de não-incidência de Barreiras/`camacari_sisloc`, mais correta semanticamente que o `"0601"` de locação de bens móveis usado por outros layouts "não sujeitos a ISS").
  4. **Decisão do usuário sobre ISS×ICMS**: `BaseCalculo`/`Aliquota`/`ValorIss` ficam **propositalmente em 0,00** (a nota é tributada por ICMS, não por ISS — mapear os campos "BC ICMS"/"ALÍQ" da nota para os campos de ISS do XML seria semanticamente incorreto e poderia confundir a contabilidade na importação), sinalizado via `Nfse.avisos` em vez de fabricado ou escondido silenciosamente.

  Zero regressão (layout novo, nenhum caminho existente alterado); suíte 225→**227 verdes**; teste novo `test_nfcom_salvador_layout.py` (2 testes, texto digital real capturado via `pdfminer.extract_text`).

### 28. Osasco/SP — NF-R de Repasse — `osasco_nfr_repasse`
- **Cabeçalho**: "Nota Fiscal Eletrônica de Repasse" ou domínio `nfe.osasco.gov.br` (ex.: iFood Benefícios).
- Campos no formato "Rótulo: valor"; regime especial (sem BC/alíquota/ISS discriminados); competência via "Ref. Fiscal MM/AAAA".
- **⚠️ Achado real 2026-08-03 (nota nº 2279456 IFOOD → PH GESTÃO, pág. 8 do lote Guarajuba Suítes): versão ESCANEADA quebrava a extração mesmo com a detecção OK.** A **detecção** do layout funciona no OCR (a frase "Nota Fiscal Eletrônica de Repasse" sobrevive), mas as **âncoras de extração** — escritas/validadas contra o NF-R **digital** (texto limpo) — não toleravam o ruído do scan: (1) **número** `Nota\s+N[º°o]\.?\s*:?\s*(\d+)` tolerava ponto mas não a **vírgula** que o OCR pôs ("Nota No,: 2279456") → caía para `00000000`; corrigido com `[.,]?`. (2) **CNPJ do tomador**: o rótulo "CPF/CNPJ" degradou para "CEF/CNPI", a âncora por rótulo falhava e o CNPJ zerava; corrigido com **fallback ao 1º CNPJ/CPF bem-formado** (`\d{2}\.\d{3}\.\d{3}/\d{4}-\d{2}`) dentro do bloco EMITENTE/RECEPTOR já isolado — a formatação sobrevive ao OCR mesmo quando o rótulo não. (3) **UF**: o ":" após "UF" saiu como ";" ("UF; BA"), a regex não tolerava e o UF caía no default `SP`; **combinado com a ausência de `city_hint`** no resolver, Camaçari/BA era resolvido como **São Paulo/SP** (`3550308`); corrigido tolerando `[:;]` **e** passando `city_hint=municipio` (sem o hint, o resolver erra Camaçari mesmo com UF=BA: retorna `2927408`, não `2905701`). (4) **Hora de emissão**: o cabeçalho só traz a data ("Emissão: 07/05/2026"); a hora só aparece no rodapé ("emitida em 07/05/2026 **às 16:02:47**") — passamos a capturá-la de lá. Todos os fixes **gated por `LAYOUT_OSASCO_REPASSE`** e aditivos: baseline-vs-fix no PDF completo (21 notas) muda **só as 2 notas iFood**, as outras 19 idênticas. Coberto por `tests/test_osasco_repasse_scan.py` (OCR real). Limitação registrada: o código de autenticidade real `CKJGTPTE` sai `CKIGTPTE` (OCR lê "J" como "I", fonte fraca) — char-level, não corrigível de forma confiável, mesma classe do O/0 do Camaçari.

### 29. PASSWORD / eNotas Gateway — `password_enotas`
- NFS-e **tributada** (ISS 3%, Simples Nacional) emitida via **eNotas Gateway**. Não faz parte da família "locação não sujeita a ISS".
- Detecção ancorada especificamente no **CNPJ de cada emitente** (nunca pela marca genérica "eNotas"), para não colidir com futuros emitentes que usem o mesmo gateway. **3 emitentes cobertos** (todos de Lauro de Freitas/BA, mesma plataforma, mesmo nome de layout mantido por retrocompatibilidade):
  - **PASSWORD - SISTEMAS ELETRONICOS LTDA** (CNPJ `04.021.023`) — amostra original, **digital** (pdfminer).
  - **INFOMIX Soluções em Tecnologia LTDA** (CNPJ `29.869.622`, achado 2026-08-10, nota real 202600000001638 → TEMIS PROJETOS) — **digital**. Extração de entidades/valores é genérica o bastante pra servir os dois emitentes digitais sem ramo dedicado, exceto 2 diferenças pontuais de estrutura de texto encontradas nesta nota: (a) o "código interno" do serviço após a barra (`"01.07 / 107 -"`) tem só 3 dígitos, sem zero à esquerda (a nota PASSWORD tem 4: `"15.03 / 1503 -"`) — o regex antigo exigia `\d{4}` rígido e caía no fallback genérico truncado ("0001" em vez de "0107"); corrigido usando o próprio item LC116 (`"01.07"` → `"0107"`, grupos 1+2 sem ponto) em vez do código interno, o que dá o mesmo resultado também na nota PASSWORD (`"15.03"` → `"1503"` == código interno "1503" dela); (b) os rótulos do tomador "NOME/RAZÃO SOCIAL" e "E-MAIL" vêm às vezes **despejados juntos** antes dos 2 valores (em vez de rótulo-imediatamente-seguido-do-próprio-valor, como na nota PASSWORD) — sem tratar isso, a razão social do tomador saía literalmente `"E-MAIL"`.
  - **TÉSSERA HOSPITALITY LTDA** (CNPJ `03.814.827`, achado 2026-08-13, nota real 202600000001829/RPS 988, pág.4 do lote "Guarajuba Suítes" 07/2026) — 1ª nota **ESCANEADA** desta plataforma (as 2 anteriores são digitais). O scan funde a grade "DADOS DO TOMADOR" (NOME/RAZÃO SOCIAL | E-MAIL | TELEFONE, depois ENDEREÇO | BAIRRO | CEP, depois MUNICÍPIO | UF | PAÍS | CPF/CNPJ) numa única linha por rótulo — ilegível pela extração dedicada, que espera rótulo-em-cima/valor-embaixo sem fusão de coluna — e degrada a coluna direita do cabeçalho (Número/Competência/Código de Verificação/Data de Emissão se sobrepõem ao endereço do prestador). Correções:
    - Recorte dedicado do **topo da página** (28% da altura, zoom 8x, PSM automático) recupera Número/Competência/Código de Verificação/Data de Emissão/CNPJ/IM do prestador; a etiqueta "DADOS DO TOMADOR" (lida fora de ordem física pelo PSM automático, ANTES do próprio conteúdo do prestador nesse recorte) é removida antes de prependar ao texto principal — sem isso, o bloco do prestador saía truncado logo no título do documento, esvaziando CNPJ/razão/endereço.
    - Recorte dedicado do **bloco "DADOS DO TOMADOR"** (localizado dinamicamente entre os rótulos "TOMADOR" e "DISCRIMINAÇÃO"/"PRESTACAO" via `image_to_data`, zoom 8x, PSM 6) — guardado num atributo próprio, nunca prependado ao texto principal (o próprio recorte também começa com a etiqueta "DADOS DO TOMADOR").
    - **Bug de propagação em lote** (achado 2026-08-13): os 2 recortes acima ficavam guardados em atributos ESCALARES de instância, resetados no início de toda chamada a `_ocr_page` — em lotes de várias páginas, o valor da página da nota TÉSSERA era apagado pelo processamento das páginas seguintes antes de `parse_multiple()` propagá-lo para o `sub_ext` (extrator dedicado, novo, que nunca chama `_ocr_page`) que de fato monta a Entidade — resultado: CNPJ/razão social do prestador cruzados com o tomador. Corrigido trocando os escalares por **dicionários indexados por página** (0-based, mesma convenção de `_ocr_page`), propagados para o `sub_ext` certo em `parse_multiple()`.
    - Razão social do prestador prioriza a linha com **sufixo social** (LTDA/S.A./EIRELI/EPP/ME) sobre a heurística posicional antiga ("linha após 'emitido em: <data>'") — no recorte de cabeçalho (PSM automático, fora de ordem física) essa heurística caía num fragmento solto da legenda do logo ("HOSPITALITY").
    - CNPJ do tomador tolera separador final "." no lugar do "-" (achado no próprio recorte dedicado: "0001.09" em vez de "0001-09").
    - Município do tomador tolera um fragmento solto de até 3 letras minúsculas entre município e UF (achado: "Camaçari o BA Brasil" — ruído de OCR insere um "o" entre os dois).
    - "(=) BASE DE CÁLCULO" pode perder o rótulo por completo quando a fusão de coluna do OCR funde 2 valores pareados numa só linha ("RETENÇÕES FEDERAIS: R$ 0,00 R$ 2964,77", sem rótulo algum para o 2º valor) — reconstituída como Serviços - Deduções (conta do ISS) quando o rótulo não é encontrado.
- Código do serviço LC116 no formato `NN.NN / <código interno>` → usa o item `NN.NN` (sem ponto); "VALOR DO ISS" impresso como "-" (recolhido via DAS do Simples) → base/alíquota preenchidas, ISS = 0,00.

### 30. ISBET — `isbet_recibo`
- **Cabeçalho**: "NOTA DE CONTRIBUIÇÃO SOLIDÁRIA" ou "ISBET".

### Fallback

### 31. Genérico — `generico`
- Fallback para layouts de prefeituras ainda não mapeadas. Usa heurísticas universais de busca de tags de XML padrão ABRASF.

---

## 🚀 Escalabilidade e Adição de Novos Layouts

O **Conversor NFS-e** foi projetado seguindo o padrão de **Design Patterns Orientado a Expressões Regulares (Regex) e Etiquetas (Labels)**, o que significa que o código-fonte **nunca precisa ser reescrito ou quebrado** para adicionar novas prefeituras.

**Para adicionar um novo layout de qualquer cidade do Brasil, o software é escalável pelas seguintes características:**
1. **Registrar a detecção nos DOIS métodos**: adicione a marca do novo layout em `_detect_layout` **e** em `_detect_layout_page` (`if re.search(...): return LAYOUT_X`). ⚠️ Um layout com todas as regras de campo corretas ainda falha silenciosamente se a detecção não o reconhecer, ou se um layout anterior na cadeia "roubar" o documento — teste a detecção contra o texto real, não só a extração.
2. **Dicionário de Etiquetas**: adicione as palavras-chave do novo layout (ex: `"Tomador do Serviço:"`, `"Dados do Cliente:"`) nas listas `_LABELS_TOMADOR`/`_LABELS_PRESTADOR` no topo de `pdf_extractor.py`. O sistema automaticamente recorta e isola aquele bloco.
3. **Métodos Modulares**: cada metadado (ex: `_extrair_valores`, `_extrair_data_emissao`, `_extrair_numero`, `_extrair_entidade`) usa condicionais simples (`if self.layout == LAYOUT_NOVA_CIDADE:`). Plugue a regra do novo município sem afetar nenhuma cidade já suportada.
4. **IBGE resolver**: registre a cidade em `IBGEResolver.KNOWN_CITIES` (`src/utils/ibge_resolver.py`) e sempre passe `city_hint` específico da entidade — senão o resolver pode devolver a capital do estado (ex.: Salvador para um prestador de Camaçari).
5. **Isolamento entre Leitura e Escrita**: o robô de leitura (`pdf_extractor.py`) não conhece o de escrita (`abrasf_transformer.py`). Ele apenas preenche um Modelo Pydantic unificado (`Nfse`). Por mais fora de padrão que seja o PDF, basta ensinar o extractor a preencher o Modelo que o XML ABRASF sairá idêntico para o ERP.
6. **Avisos de baixa confiança**: `Nfse.avisos` acumula alertas quando um campo cai em fallback (número/CNPJ zerado, data atual, valor zero). Prefira sinalizar a mascarar — foi assim que bugs silenciosos de OCR foram flagrados.

> **Atenção — mesmo layout, estruturas de texto diferentes:** um layout pode chegar como PDF imagem (OCR, campos em grade) ou PDF digital (pdfminer, colunas intercaladas), exigindo parsers de entidade distintos com detecção automática da estrutura. Ver o layout **Campinas/SP** (`_extrair_entidade_campinas` vs `_extrair_entidade_campinas_digital`) como referência.

## Mapeamento para XML ABRASF 2.01 (NFS-e)

| Campo PDF | Tag XML ABRASF | Descrição |
| :--- | :--- | :--- |
| Número da Nota Fiscal | `<Numero>` | Número sequencial da nota |
| Cód. de Autenticidade | `<CodigoVerificacao>` | Código para validação no portal |
| Data de Geração | `<DataEmissao>` | Data e hora em formato ISO |
| Data de Competência | `<Competencia>` | Data no formato YYYY-MM-DD |
| CPF/CNPJ | `<CpfCnpj>` | Documento limpo (apenas dígitos) |
| Razão Social | `<RazaoSocial>` | Nome completo da entidade |
| Cidade/UF | `<CodigoMunicipio>` | Convertido para o código IBGE |

---

## Contratos de Locação (Formulário → XML)

Geração de XML ABRASF 2.01 diretamente a partir dos dados digitados na GUI, **sem necessidade de PDF**.

### Mapeamento de papéis

| Parte no Contrato | Tag no XML ABRASF | Motivo |
| :--- | :--- | :--- |
| **Locador** (proprietário do bem) | `<Tomador>` | Recebe o pagamento |
| **Locatário** (usuário do bem) | `<Prestador>` | Declara o serviço |

### Regras automáticas

| Campo | Valor gerado |
| :--- | :--- |
| `<Numero>` | Ano da data de emissão (ex: `2026`) |
| `<Acumulador>` | `916` (fixo) |
| `<CodigoVerificacao>` | `CONTRATO` |
| `<NaturezaOperacao>` | `1` (tributação no município) |
| `<ItemListaServico>` | `0601` padrão (locação de bens móveis — LC 116/2003) |
| `<ValorIss>` | `valor_mensal × aliquota_iss` |
| `<ValorLiquidoNfse>` | `valor_mensal − valor_iss` |

### Arquivo gerado

```
CONTRATO_LOCACAO_<ANO>.xml
```

---

## DANFE Estadual — NF-e de Produto (Modelo 55) → XML NF-e 4.00

Documento fiscal de **PRODUTO/mercadoria** (tributado por ICMS/IPI), estruturalmente diferente de qualquer NFS-e de serviço da lista acima (não tem "discriminação"/"código de serviço" únicos — tem tabela de N itens com NCM/CFOP, grade de ICMS e bloco "TRANSPORTADOR/VOLUMES TRANSPORTADOS"). Extraído para um modelo próprio (`NfeProduto`, em `src/models/nfe_produto_models.py`) e transformado por `NfeProdutoTransformer` (`src/transformers/nfe_produto_transformer.py`) num XML **NF-e 4.00** real — com a chave de acesso, ICMS e demais valores **extraídos do próprio documento**, não fabricados.

**Detecção** (`LAYOUT_DANFE_PRODUTO`, checada no TOPO de `_detect_layout`/`_detect_layout_page`, antes de qualquer marca de NFS-e): estrutural, não gated a nenhum emitente específico — exige a combinação "DANFE" + "Documento Auxiliar da Nota Fiscal Eletrônica" + "0-ENTRADA"/"1-SAÍDA", assinatura padronizada nacionalmente (SEFAZ/CONFAZ) para todo Modelo 55. Achado real que motivou este layout: uma nota de compra de café (GRAN COFFEE COM. LOC. E SERVICOS S.A. → SINDICATO DOS DELEGADOS DE POLICIA DO ESTADO DA BAHIA, nº 52.136, R$ 595,00) caía inteira em `LAYOUT_LOCALIZA`, porque o rótulo genérico "FATURA/DUPLICATA" (presente em QUALQUER DANFE) colidia com a marca da locadora Localiza — saía com tomador não identificado, valor zerado e o prestador hardcoded errado ("LOCALIZA RENT A CAR S/A").

**Campos extraídos:**
- Cabeçalho: número, série, tipo de operação (0-entrada/1-saída), chave de acesso (44 dígitos, REAL — não recalculada), protocolo de autorização (+ data/hora), natureza da operação, data de emissão, data/hora de entrada-saída.
- Emitente: razão social + endereço do letterhead livre (bloco impresso entre o canhoto/recibo e o box "DANFE" — texto livre, não uma grade de rótulos, pois o padrão SEFAZ não estrutura essa parte); CNPJ/Inscrição Estadual de rótulos isolados na grade.
- Destinatário: grade padronizada "DESTINATÁRIO/REMETENTE" (rótulos nacionais: NOME/RAZÃO SOCIAL, CNPJ/CPF, ENDEREÇO, BAIRRO/DISTRITO, MUNICÍPIO, CEP, UF).
- Itens (lista, N linhas): código, descrição, NCM, CST, CFOP, unidade, quantidade, valor unitário, valor total, base de cálculo/valor/alíquota do ICMS.
- Transportador/volumes (opcional): razão social, CNPJ/IE, frete por conta, peso bruto/líquido, quantidade de volumes.
- Valores totais: base/valor ICMS (+ ICMS ST), frete, seguro, desconto, outras despesas, IPI, valor total dos produtos, valor total da nota.

**Quirk de ordem na grade "CÁLCULO DE IMPOSTO"** (achado real): o rótulo "VALOR TOTAL DOS PRODUTOS" (1º bloco de 5 rótulos) tem o próprio VALOR deslocado para o FIM do 2º bloco de 6 valores (depois de FRETE/SEGURO/DESCONTO/OUTRAS DESPESAS/IPI/TOTAL DA NOTA) — mesma família geral de "labels dumped, depois values dumped" já vista em várias NFS-e, faceta nova aqui.

**Diferença importante em relação ao `NfeTransformer` (workaround legado):** o `NfeTransformer` existente (usado quando a nota-fonte É uma NFS-e de serviço, mas o ERP só aceita import de NF-e) **calcula** uma chave de acesso fake e zera ICMS/PIS/COFINS — é uma disfarce, não uma extração real. O `NfeProdutoTransformer` só é usado quando o documento-fonte É genuinamente um DANFE Modelo 55 (detecção automática via `isinstance(nfse_obj, NfeProduto)` em `src/main.py`); nesse caso a chave de acesso e os valores de ICMS são os REAIS do documento. Ambos os caminhos convivem sob a mesma opção "NF-e (DANFE Estadual - Modelo 55)" da GUI — o tipo do objeto extraído decide automaticamente qual transformer roda, não a escolha manual do usuário.

---

## Processamento de Múltiplas Páginas (PDFs)

O sistema conta com um motor de fatiamento inteligente que suporta:

- **Faturas com múltiplas páginas**: Identifica quando uma fatura continua em páginas subsequentes e agrupa o texto para garantir a extração completa da discriminação dos serviços.
- **Múltiplas faturas por página**: Detecta divisores visuais (como linhas horizontais longas) ou novos cabeçalhos no meio de uma página, separando-os em arquivos XML distintos.
- **Rastreamento de Número**: Usa o número da nota fiscal para decidir se um bloco de texto é uma nova nota ou a continuação da anterior.

---

*Documentação atualizada em: 2026-08-10 (41 layouts; **novo layout — Camaçari/BA via SISLOC/NFS-e Easy (Benefix)** — `camacari_sisloc`, nota real nº 24052 (FERIMPORTE SERVICE LTDA → DELTALINE SERVICOS LTDA., R$91,20, locação de máquinas e equipamentos): PDF totalmente digital cujo gerador desenha rótulo e valor como objetos de texto separados no fluxo interno do arquivo — o `pdfminer.high_level.extract_text()` padrão despejava TODOS os valores da grade no fim do documento, fora de ordem e sem vínculo com o rótulo (número saía `0`, o que o usuário reportou como incorreto), apesar do texto "estar todo lá". Corrigido com uma técnica nova neste projeto: reconstrução do texto a partir das coordenadas de caractere (`_reconstruir_texto_por_coordenadas`, via `pdfminer.high_level.extract_pages` + `LTChar`) — agrupa caracteres por proximidade vertical (Y, tolerância 2,5pt) em linhas e ordena cada linha por posição horizontal (X, usando o `x1` real de cada caractere, não uma aproximação — um protótipo inicial com `x0+3` fixo inseria espaços espúrios dentro de palavras), reproduzindo a ordem visual correta da nota; acionada em `parse()` logo após a detecção do layout (a marca "SISLOC"/"NFS-e Easy"/"webenefix" sobrevive no texto quebrado o suficiente pra detecção funcionar antes da reconstrução), substituindo `self.raw_text` antes de qualquer extração de campo. Detecção posicionada ANTES do check genérico de "PREFEITURA MUNICIPAL DE CAMAÇARI" (mesmo princípio já aplicado para DANFSe Nacional vs. layout municipal, e PJB Construção vs. municipais homônimos), evitando que a nota fosse roteada para o parser CPqD errado. Grade de tributos com 2 colunas lado a lado na mesma banda de Y (retenções federais à esquerda; valores do serviço à direita) mais uma coluna decorativa de letras soltas rotacionada 90°, que a reconstrução por coordenada intercala numa mesma linha — "Valor do Serviço"/"Valor Líquido" extraídos direto (colados ao rótulo); os demais 12 campos de valores usam extração POSICIONAL pela ordem fixa dos tokens "R$..." no bloco (mesmo princípio já usado no layout Cuiabá). Código de serviço "9901" (não é item real da LC116) mapeado para `0000` (mesma convenção do Barreiras/PJB); município de prestação resolvido pelo código IBGE explícito impresso na própria nota ("2905701"). Zero regressão (suíte de 189→**191 verdes**, +2 testes novos); teste `test_camacari_sisloc_layout.py` (texto real via `repr()` da reconstrução por coordenada). **fix anterior — nota-fantasma por página em DANFSe Nacional escaneada com UMA única nota** — `danfse_nacional`, PDF "análise de notas SP-iss retido - inss retido.pdf" (062026), págs. 3 e 4 (nota real nº 175, ARMANDO CACAMBA LTDA → SÃO PEDRO CONSTRUTORA LTDA, Prefeitura Municipal de Várzea Grande/MT, locação de caçambas, R$1.650,00): toda DANFSe Nacional imprime seu próprio título "DANFSe v1.0" nos primeiros ~70 caracteres da página (logo após o rótulo "Chave de Acesso da NFS-e") — mesmo com uma única nota impressa ali. O split de múltiplas-notas-por-página de `parse_multiple` (`headers_regex = r'(?=\n\s*\bDANFSe\b)'`) cortava exatamente nesse 1º "DANFSe", fatiando esse preâmbulo de boilerplate (sem CNPJ/valor algum) como uma nota-FANTASMA própria (número `00000000`, prestador/tomador = fragmento garbage "Serviço eletrônica", toda a grade zerada) logo ANTES da nota real — dando a impressão de "número zero" pra quem olha a 1ª entrada retornada pra aquela página; a mesma página 4 do mesmo lote exibia o padrão idêntico (fantasma "VÁRZEA GRANDE"/`00000000` antes da nota real nº 924, MT Concreto). Corrigido de forma aditiva, sem gate por layout (o código é compartilhado por todos): só trata uma ocorrência de "DANFSe" como início de nota NOVA quando ela aparece a partir de `DANFSE_HEADER_MIN_OFFSET = 200` caracteres do início do bloco — o título de uma nota sozinha na página nunca passa de ~70 chars; uma 2ª nota genuinamente colada na mesma página só teria seu próprio título bem depois de todo o conteúdo (milhares de caracteres) da 1ª, preservando exatamente o caso que este split foi feito pra resolver. Validado com baseline-vs-fix num recorte real de 6 páginas do mesmo lote (sem OCR-ar as 189 páginas inteiras): baseline gerava 9 blocos (2 fantasmas incluídos), fix gera 7 (só os reais), mudando SÓ as 2 páginas-alvo — as outras 4 páginas do recorte (notas 563, 567, 933→49, 491) idênticas. Suíte 189 verdes (+2); teste `test_danfse_nacional_pagina_unica_sem_fantasma.py` (texto real via script, `repr()`). **2ª correção na MESMA nota, mesmo dia — CNPJ do tomador saindo igual ao do prestador:** o tomador saía com `02.488.708/0001-69` (CNPJ do PRESTADOR, Armando Caçamba) em vez do seu próprio `03.051.741/0001-90` (São Pedro Construtora). Causa: a coluna "CNPJ/CPF/NIF" da grade DANFSe é comum a todas as entidades (rótulo compartilhado), e o OCR desta nota lê as linhas fora de ordem física, colando o CNPJ do prestador dentro do texto que compõe o BLOCO do tomador (delimitado pelos cabeçalhos de seção "TOMADOR DO SERVIÇO"/"INTERMEDIÁRIO..."); a extração genérica de CNPJ (`_extrair_entidade`, compartilhada por ~30 layouts) pega o PRIMEIRO CNPJ válido encontrado dentro do bloco, e nesse bloco o CNPJ do prestador (bleeding-in) aparece ANTES do CNPJ real do tomador (que fica sem rótulo próximo, mais adiante no texto). Corrigido de forma aditiva: `parse()` guarda o CNPJ do prestador (já extraído antes do tomador, na mesma chamada) em `self._cnpj_prestador_extraido`; dentro de `_extrair_entidade`, ao extrair tomador/intermediário, um candidato de CNPJ idêntico a esse valor é descartado a favor de outro CNPJ válido do MESMO bloco — só volta a ser aceito como último recurso se não existir nenhum outro candidato (preserva o comportamento antigo nesse caso-limite). Como a função é compartilhada, validei explicitamente que as outras 6 notas do mesmo recorte de 6 páginas (nºs 563, 567, 924, 933, 49, 491 — nenhuma delas com esse bug) saem BYTE-A-BYTE idênticas ao resultado sem o fix — a guarda só entra em ação quando o candidato é de fato igual ao CNPJ do prestador. Suíte **189 verdes**; mesmo teste `test_danfse_nacional_pagina_unica_sem_fantasma.py`, agora cobrindo os 2 bugs. **Uma anomalia adicional ainda não corrigida, fora do escopo:** município do prestador caindo no fallback Salvador/BA em vez de Várzea Grande/MT (não cadastrado em `KNOWN_CITIES`) — registrada para investigação futura. **fix anterior — DANFSe Nacional (Criciúma/SC) com valores estruturados em PONTO decimal saindo zerados** — `danfse_nacional`, nota real nº 730080 (THOMSON REUTERS BRASIL CONTEUDO E TECNOLOGIA → CAFES FINOS VITORIA DA CONQUISTA LTDA, plataforma Domínio Sistemas, Prefeitura Municipal de Criciúma/SC): esta nota imprime TODOS os campos monetários estruturados da grade ("Valor do Serviço", "BC ISSQN", "ISSQN Apurado", "Valor Líquido da NFS-e" etc.) com PONTO decimal ("R$ 372.96"), ao contrário de toda outra DANFSe Nacional já vista (vírgula, "R$ 400,00") — curiosamente só o texto livre da "Descrição do Serviço" da mesma nota usa vírgula ("Valor: R$ 372,96"). O regex de captura de `_extrair_valores` para `LAYOUT_NACIONAL` exigia vírgula (`[\d.]+,\d{2}`), então nunca casava nos campos estruturados desta nota e todo valor caía no fallback `0.0` — Valor dos Serviços, Base de Cálculo, Valor do ISS e Valor Líquido saíam zerados no XML. Corrigido de forma aditiva e gated ao mesmo branch (`_rs_apos`/`_num_rotulo`): novo regex de captura tolerante a qualquer combinação de ponto/vírgula (`R\$\s*(\d[\d.,]*\d)`) + novo helper `_parse_valor_tolerante` (ao lado do `_parse_valor` existente, sem alterá-lo) que trata o ÚLTIMO separador da string como o decimal — funciona tanto para "372.96" (ponto decimal) quanto para "400,00" (vírgula decimal, comportamento antigo preservado). Zero regressão: a nota MEI/Salvador já coberta por `test_danfse_nacional_layout.py` (valores em vírgula) continua batendo idêntico; suíte de 187 verdes (+2); teste novo `test_danfse_nacional_ponto_decimal.py`. Duas anomalias adicionais notadas nesta mesma nota (Bairro do prestador corrompido por ausência de rótulo "Município" no bloco EMITENTE; município do prestador cai no fallback Salvador/BA e município do tomador resolve para Florianópolis em vez de Criciúma) foram identificadas mas ficaram FORA do escopo deste fix a pedido do usuário — registradas para investigação futura. **fix anterior — novo layout Guarulhos/SP — plataforma Ginfes, origem em foto/CamScanner** — `guarulhos_sp`, nota real nº 3 (KICHLER INDUSTRIA COMERCIO E INSTALACAO DE ESQUADRIA DE ALUMINIO LTDA → SÃO PEDRO CONSTRUTORA, R$4.511,41, obra Sede de Tecnologia MTI/SECITECI em Cuiabá/MT): a leitura padrão de página inteira (zoom 3x, PSM automático) só recuperava ~850 caracteres desta nota — a grade densa de células cinza (baixo contraste) fazia o Código de Verificação, o Local da Prestação e toda a grade "Cálculo do ISSQN devido no Município" desaparecerem por completo, e a nota era descartada como "Layout não reconhecido" (0 XML, mesmo padrão de falha do Brotas de Macaúbas antes de existir suporte ao município). Corrigido com zoom 4x + PSM 6 (bloco único) para o corpo da página — recupera prestador/tomador/discriminação/código de serviço de forma legível — e um recorte dedicado (`_ocr_recut_guarulhos`, 3 sub-recortes em zoom alto com binarização por limiar de luminância, mesmo racional do recorte de recuperação do Camaçari) que resolve Número/Data/Código de Verificação/Local da Prestação e a coluna numérica da grade de valores (Valor dos Serviços/Base de Cálculo/Alíquota/Valor do ISS/Valor Líquido), devolvendo tudo como um bloco de texto canônico e limpo — mais fácil de casar por regex simples nos métodos de extração do que a leitura ruidosa original. **Decisão de negócio do usuário:** o serviço prestado é construção civil (item LC116 "7.02 / 439910100 - Administração de obras", impresso na própria nota sem ambiguidade de mapeamento — diferente do CNAE do Brotas de Macaúbas) executado em OUTRO município — a nota traz explicitamente "Local da Prestação: CUIABA-MT" + "Natureza Operação: Tributação fora do município" + "ISS a reter: Não" + "Valor do ISS: R$0,00" —, então a incidência do ISSQN deve ir para o município da obra (Cuiabá/MT, `5103403`), não para o município do prestador (Guarulhos/SP, `3518800`), conforme LC 116/2003 art. 3º III. Como o transformer ABRASF sempre gravava `<Servico><CodigoMunicipio>`/`<MunicipioIncidencia>` com o município do PRESTADOR (para todos os ~39 layouts, sem exceção), implementamos um campo novo e aditivo em `Nfse` — `municipio_incidencia_override: Optional[str] = None` — que o transformer usa em vez do município do prestador SÓ quando setado (todos os outros layouts continuam com o campo `None`, comportamento 100% inalterado); o endereço próprio do prestador dentro de `<PrestadorServico><Endereco>` continua apontando Guarulhos corretamente — só a tag de incidência do serviço muda. Município Guarulhos/SP cadastrado em `KNOWN_CITIES` (`3518800`, confirmado via IBGE/RuaCEP — CEP 07043-120 da nota real cai dentro da faixa 07000-001/07399-999 do município). Zero regressão nos ~39 layouts existentes (nenhum outro seta o campo novo); suíte de 185 verdes; teste `test_guarulhos_layout.py`. **fix anterior — novo layout Brotas de Macaúbas/BA — município nunca visto antes, origem em foto/JPG** — `brotas_macaubas_ba`, nota real nº 70 (M P C ARAUJO → SÃO PEDRO CONSTRUTORA, revitalização de cobertura de estacionamento, R$10.091,13, plataforma nfservico.com.br, mesma do Iaçu/BA): antes desta implementação a nota era 100% rejeitada pelo conversor (0 XML, "Layout não reconhecido" — não era um bug de campo, era ausência total de suporte ao município). Detecção por marca própria (prefeitura/CNPJ 13.797.600/0001-74), sem colidir com outros municípios da mesma plataforma (mesmo racional já aplicado ao Iaçu). Reaproveita a ESTRUTURA do parser de entidade do Iaçu (blocos "PRESTADOR/TOMADOR DE SERVIÇOS", rótulos "Nome/Razão Social:"/"CPF/CNPJ:", endereço em linha única "logradouro número - bairro - CEP - cidade - UF"), mas com extrator próprio (`_extrair_entidade_brotas_macaubas`) por 2 diferenças reais: o endereço do PRESTADOR tem um complemento ("CASA") entre o número e o bairro, mais um "|" de OCR colado no número ("26-B"); a razão do TOMADOR vinha colada ao nome/CREA do engenheiro responsável impresso à direita na mesma linha ("SAO PEDRO CONSTRUTORA LTDA Eng. Victor Hage Carmo") — cortada pelo token "Eng.". A caixa de cabeçalho (Número da nota/Data de Emissão/Código de Verificação) reaproveita o MESMO recorte dedicado do Iaçu (`_ocr_header_box_iacu`), agora com um parâmetro `angle` opcional (aditivo — o Iaçu continua com `angle=0`, comportamento 100% inalterado): as fotos de Brotas de Macaúbas chegam de cabeça para baixo (180°) e o recorte sem rotação caía no canto errado da imagem (caixa vazia, número/data/código todos perdidos). Grade de valores própria (`Base de cálculo/Alíquota/Valor do ISS/Crédito` numa linha + `Valor IR/CSLL/Outras retenções/Valor líquido` noutra + `Valor COFINS` isolado — SEM o campo "Valor total das deduções" que a grade do Iaçu tem na mesma linha da base/alíquota/ISS, por isso um regex próprio em vez de reaproveitar o grid do Iaçu; tolerante ao artefato de OCR "rentenções", com "n" extra). Município resolvido via novo código IBGE cadastrado em `KNOWN_CITIES` (Brotas de Macaúbas `2904506`, confirmado via IBGE/RuaCEP — antes caía no fallback da capital Salvador `2927408` por ausência no dicionário). Código de serviço fixo `0702`: a nota imprime "Item da lista de serviços: 0 - Prestação de serviços em geral", que não é um código LC116 válido (item "0" não existe na lista) — mapeado do CNAE impresso na nota ("4391600 - Obras de fundações") para o item 07.02 (execução de obras de construção civil), decisão do usuário confirmada com print da própria nota. Zero regressão nos layouts existentes (Iaçu e todas as variantes Camaçari, cujo código-fonte foi tocado apenas para adicionar o parâmetro opcional de ângulo); suíte de 184; teste `test_brotas_macaubas_layout.py`. **fix anterior — Camaçari/BA escaneado — CPF/CNPJ do TOMADOR ausente do OCR por marca-texto + rabisco sobre o campo** — `camacari_ba_scan`, nota nº 962 (L DE J NASCIMENTO TRANSPORTE → PH GESTÃO, pág.20 do lote Guarajuba 06/2026): no scan original, o campo CPF/CNPJ do bloco TOMADOR estava cobertO por um marca-texto amarelo com um rabisco a caneta por cima, que zerava o OCR só nessa célula (o resto da página, inclusive o CNPJ do PRESTADOR, lia normalmente) — o rótulo "CPF/CNPJ:" do tomador saía sem nenhum valor, caindo no sentinela `00000000000000`; confirmado isolado (as outras 2 notas de Camaçari do mesmo lote, págs.16 e 19, já extraíam o CNPJ do tomador corretamente pelo pipeline normal); corrigido de forma aditiva/gated — só dispara quando o CNPJ do tomador já extraído é o sentinela E a nota é escaneada (`from_ocr`) — com um recorte de recuperação (`_recuperar_cnpj_tomador_camacari`) que localiza dinamicamente o rótulo "CPF/CNPJ" do bloco TOMADOR por posição de palavra (`pytesseract.image_to_data`, ancorado após o token "TOMADOR"), recorta a região à direita do rótulo em zoom alto (10x) direto da página PDF, converte para escala de cinza + limiar de luminância (remove a cor do destaque/rabisco, preserva o traço preto) e faz o OCR final com whitelist numérico; a página de origem é propagada via `_pagina_hint` (setado por `parse_multiple` no fatiamento do lote) até a chamada de recuperação. Zero regressão (baseline-vs-fix no lote de 31 páginas muda só a nota-alvo); suíte de 183; teste `test_camacari2_tomador_cnpj_recorte_pag20.py`. **fix anterior — Lauro de Freitas/BA escaneado — rótulo e valor na mesma linha (MEI/Simples) causava valor zero e razão/município corrompidos** — `lauro_de_freitas_ba`, nota nº 20264631 (ALFA MEDICAL→SÃO PEDRO, pág.6 do lote "NFS HJHJ"): Alíquota/Valor do ISS inutilizados (MEI) imprimem "\*", o OCR colapsa os 2 asteriscos em 1 e a regex estrita nunca casava, zerando até a Base de Cálculo real (283,39); + a âncora do valor total ausente do OCR fazia o total final sair 0; corrigido com fallback tolerante (só Dedução/Base obrigatórios, Alíquota/ISS ausentes = 0,00) que reaproveita o fallback já existente de valor total←base. Junto, Nome/Razão/Endereço/Inscrição exigiam quebra de linha entre rótulo e valor (esta variante os imprime na mesma linha) e caíam nos sentinelas "Não Identificado"; Bairro/Município vazavam o rótulo seguinte por captura até fim-de-linha, corrompendo o município e caindo no fallback de capital (Salvador `2927408`) em vez de Lauro de Freitas (`2919207`); corrigido tolerando mesma linha e captura não-greedy. Zero regressão (baseline-vs-fix no lote de 8 notas muda só a nota-alvo); suíte de 182; teste `test_lauro_freitas_mei_rotulo_valor_mesma_linha.py`. **fix anterior — DETECÇÃO — DANFSe Nacional emitida por município ia para o layout municipal** — `danfse_nacional`, pág. 11 do lote Guarajuba 06/2026 (nota nº 3, ANA PAULA→PH GESTÃO): o cabeçalho "Prefeitura Municipal de \<X\>" casava a marca municipal (Camaçari; outra caiu em Rio) ANTES do check da DANFSe na cadeia, e como o parser DANFSe + a regra intermediário→tomador são gated em `LAYOUT_NACIONAL`, o tomador não era extraído; corrigido com um check estreito ("DANFSe v1.0"/"Documento Auxiliar da NFS-e") no TOPO de `_detect_layout`/`_detect_layout_page`; baseline-vs-fix no lote de 31 páginas mudou só 3 (todas DANFSe mal-roteadas: págs. 5, 11 de Camaçari, 13 de Rio), zero colateral; suíte de 181; teste `test_danfse_camacari_deteccao_vs_municipio.py`. **fix anterior — Salvador/BA escaneado — rótulo "TOMADOR" só no corpo da discriminação** — `salvador_ba`, nota nº 624 (GEOLINER → DELTALINE, lote 06/2026): variante sem cabeçalho "TOMADOR DE SERVIÇOS" (tomador só pelo 2º "Nome/Razão Social") cuja discriminação traz "-I8S RETIDO PELO TOMADOR 5% = R$450,00 (DEVIDO NA CIDADE DE CAMAÇARI- BA)"; esse "TOMADOR" do corpo fazia a heurística `tem_label_tomador` (que varria o texto inteiro) pular o recorte dedicado do 2º "Nome/Razão Social", e o tomador saía com razão "5% = R$450,00 (DEVIDO...)" e CNPJ sentinela `00000000000100`; corrigido restringindo a busca do rótulo à região ANTES da DISCRIMINAÇÃO (aditivo, gated), recuperando DELTALINE SERVICOS LTDA / `01.813.680/0001-25` / Salvador-BA; zero regressão (3 testes Salvador + suíte de 179; baseline-vs-fix na nota); teste `test_salvador_tomador_no_body_label.py`; escopo confirmado com o usuário — só o tomador (Numero do prestador e incidência do ISS mantidos). **fix anterior — regra de negócio no DANFSe Nacional — intermediário promovido a tomador** — `danfse_nacional`, nota nº 44 (pág. 18 do lote Guarajuba Suítes): a nota imprime "TOMADOR DO SERVIÇO NÃO IDENTIFICADO NA NFS-e" e lista a PH Gestão como INTERMEDIÁRIO; decisão do usuário — quando o tomador vem não identificado e há intermediário, promover o intermediário a tomador e esvaziar o `<Intermediario>` (regra geral, gated por layout, no `parse()`); + fixes de apoio no parser DANFSe (município pelo padrão "<Cidade> - <UF> <CEP>" resolvendo Camaçari/BA `2905701` em vez da capital Salvador `2927408`; limpeza da razão — prefixo do CNPJ com vírgula e inicial da coluna "E-mail" vazada); zero regressão (só a nota-alvo muda no lote de 21); teste `test_danfse_intermediario_vira_tomador.py`; limitação residual do fatiamento do lote misto registrada. **fix anterior no Osasco/NF-R de Repasse (iFood) ESCANEADO** — `osasco_nfr_repasse`, nota nº 2279456 (IFOOD → PH GESTÃO, pág. 8 do lote Guarajuba Suítes): a detecção do layout já funcionava no OCR, mas as âncoras de extração (feitas para o NF-R digital) quebravam no ruído do scan — número "Nota No,:" (vírgula) caía para `00000000`; rótulo do CNPJ do tomador degradado ("CPF/CNPJ"→"CEF/CNPI") zerava o CNPJ; "UF; BA" (ponto-e-vírgula) jogava o UF no default `SP` e, sem `city_hint`, Camaçari/BA virava São Paulo/SP (`3550308`); corrigido de forma aditiva/gated (número tolera vírgula; CNPJ do tomador com fallback ao 1º CNPJ bem-formado do bloco; UF tolera `;`; `city_hint` passado; hora de emissão capturada do rodapé "às HH:MM:SS"); baseline-vs-fix no PDF completo (21 notas) muda só as 2 notas iFood, as outras 19 idênticas; teste `test_osasco_repasse_scan.py`. **fix anterior no Camaçari escaneado com página torta** — `camacari_ba_scan`, nota nº 246 (AVANÇO GESTÃO, pág. 29 do lote PH Gestão): scan inclinado ~-1° tirava a linha "Número da Nota" do enquadramento dos dois recortes fixos do cabeçalho (o largo lia só `246` sem rótulo; o estreito cortava o rótulo para "o da Nota"), a âncora `…mero da Nota` não casava e o número desabava para o fallback `00000000`; corrigido com um **terceiro recorte aditivo com deskew fino** — estima a inclinação por página (±3°, por variância do perfil horizontal da faixa do cabeçalho), desentorta e reprocessa a mesma caixa larga validada, recuperando `Número da Nota → 246`; aditivo e condicional (só concatena com `|ângulo| ≥ 0,25°` → páginas retas byte-a-byte idênticas, **zero regressão**: PDF completo com 26 notas, só a 246 mudou, nº 9100 da pág.14 intacto); coberto por `tests/test_camacari2_pag29_skew.py`. No mesmo dia — **fix anterior no Camaçari escaneado** — `camacari_ba_scan`, nota nº 9100 (pág. 14 do lote PH Gestão): o recorte largo do cabeçalho (`x=0.72`) lia a **Inscrição Municipal do prestador** no lugar da célula "Número/Data/Código", fazendo o número sair a IM (`9042148001`) e a data cair na "Data da prestação" (`08/05`) em vez da emissão (`11/05`); corrigido com um **recorte estreito aditivo** (`x=0.78`, concatenado ao largo → zero regressão nas notas 1050/4494 já validadas) + extração robusta (número descarta candidato de ≥8 dígitos e itera todas as ocorrências do rótulo; data tolera o "D" cortado; código de autenticidade exige letra+dígito, rejeitando a IM numérica). No mesmo dia — novo layout **PJB Construção** — `pjb_locacao`, fatura de locação de máquinas/bens móveis escaneada — Simões Filho/BA, CNPJ `08.885.357/0001-06` —, sem incidência de ISS (item `0000`); detecção no TOPO da cadeia por dois critérios combinados — razão do emitente "PJB CONSTRU" **E** marcador estrutural da fatura (`DESTINATÁRIO`/`NATUREZA DA OPERAÇÃO`) — para não colidir com os layouts municipais homônimos citados na nota (Simões Filho/Camaçari/Monte Gordo) nem gerar nota-fantasma a partir da linha da planilha-resumo que lista o fornecedor; prestador fixo (Simões Filho registrada no `KNOWN_CITIES`, IBGE `2929206`) e tomador parseado do bloco DESTINATÁRIO (município Camaçari/BA assumido, bairro truncado pelo scan extraído como está). Anteriormente — 2026-07-31 (37 layouts): novo layout **F&F Comércio** — `ff_locacao`, fatura de locação de CFTV, detectado pelo CNPJ do emissor por causa da frase "FATURA DE LOCAÇÃO" quebrada pelo OCR de 2 colunas, com recut dedicado de OCR por caminho de arquivo — `_ocr_recut_ff_locacao` — e valor extraído da tabela de itens por causa de um placeholder de template quebrado na própria nota-fonte; mais cinco fixes de robustez em layouts existentes: (1) Localiza — `localiza_fatura` tornado robusto a 2 estruturas de texto distintas (OCR multi-linha vs. digital em linha única sem quebras) e a ordem variável de rótulos entre notas, mais correção do número — só dígitos — e do regex de valor total; (2) São Paulo digital — `sao_paulo_sp` ganhou ramo dedicado de extração de entidades/valores, corrigindo CNPJ/razão/endereço/ISS que saíam trocados numa nota real onde os cabeçalhos de seção vêm deslocados no meio dos próprios dados da entidade — nota AMIL/TEMIS; (3) Camaçari escaneado — `camacari_ba_scan` corrigido: o recorte dedicado do cabeçalho (`_ocr_header_box_camacari`) cortava a linha inteira do "Número da Nota", que sumia por completo — número nº 4494; (4) Barreiras — `barreiras` ganhou ramo dedicado de valores para a variante de locação de bens móveis não sujeita a ISS (item "00.00" → `0000`), cuja grade de 3 colunas ("rótulos primeiro, valores depois") caía no fallback zero no genérico — nota nº 1162; (5) São Paulo (digital + escaneado) — endereço em linha única sem rótulo "Bairro:" separado corrigido (número não separava do logradouro sem vírgula; complemento vazava pro campo "numero" com vírgula) e intermediário fantasma eliminado (bloco totalmente vazio "----" fabricava razão social a partir do próprio rótulo da seção) — nota UNIMED CNU.*
