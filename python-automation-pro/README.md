# Conversor de Notas Abrasf

## Visão Geral

Este projeto fornece um conversor de PDFs de Notas Fiscais de Serviço (NFS‑e), Contratos de Locação e NF-e de Produto (DANFE Estadual, Modelo 55) para o padrão **ABRASF 2.01 XML** (serviço) ou **NF-e 4.00 XML** (produto). Ele inclui:

- **Extratores de PDF** com detecção automática de layout, cobrindo **52 layouts** (51 específicos + genérico de fallback) de múltiplos municípios e emissores:
  - **Prefeituras/NFS-e:** Cuiabá/MT, Barreiras/BA, Camaçari/BA (digital, escaneado/OCR, Nota Avulsa da Prefeitura e SISLOC/NFS-e Easy — quatro layouts dedicados), Salvador/BA, Feira de Santana/BA, Simões Filho/BA, Lauro de Freitas/BA, Iaçu/BA, Brotas de Macaúbas/BA (plataforma nfservico.com.br), Mata de São João/BA (plataforma SAATRI), **Monte Santo/BA**, Rosário da Limeira/MG (plataforma FUTURIZE), Rio de Janeiro/RJ (Nota Carioca), São Paulo/SP (digital **e escaneado/OCR** como layouts dedicados), Guarulhos/SP (plataforma Ginfes, escaneada), Santos/SP (plataforma Ginfes, digital), **Vinhedo/SP (plataforma Balker, digital)**, **Barueri/SP (digital)**, Ribeirão Pires/SP, Campinas/SP, Joinville/SC, **São José/SC**, Fortaleza/CE, Brasília/DF (GDF), **Goiânia/GO (plataforma ISSNet Online, digital)** e o Portal Nacional (DANFSe v1.0).
  - **Faturas de locação / serviços específicos:** Localiza Rent A Car, CPE Tecnologia, Guincho Cidade, B.F. Serviços Ambientais, LMR Engenharia, Geração & Energia, Locontainers, SUL&SEG (Nota de Cobrança), F&F Comércio (locação de CFTV, escaneada), **PJB Construção** (locação de máquinas, Simões Filho/BA, escaneada), **Fatura de Locação Genérica** (qualquer locadora não catalogada) e **ARMAC** (escaneada, tabela multi-item).
  - **Outros:** NF-e de Serviço de Comunicação (Telecom), **NFCom Salvador** (Empresa Baiana de Jornalismo, padrão nacional NFCom/ICMS), Osasco/SP NF-R de Repasse (ex.: iFood Benefícios), PASSWORD/eNotas Gateway (NFS-e tributada, cobre múltiplos emitentes na mesma plataforma — PASSWORD e INFOMIX, digitais, e TÉSSERA HOSPITALITY, escaneada, todos Lauro de Freitas/BA), ISBET (Nota de Contribuição), **SKYTEF** (São Paulo/SP, licenciamento de software via plataforma Qive), **Caixa Cartões** (São Paulo/SP, taxa de serviço de cartões pré-pagos, mesma plataforma Qive do SKYTEF, outro emitente) e um layout **Genérico** de fallback.
- **OCR integrado** (Tesseract via `pytesseract` + PyMuPDF) para PDFs escaneados/imagem sem camada de texto — o extrator tolera os erros de reconhecimento típicos de cada layout.
- **Transformadores** que mapeiam os dados extraídos para o XML ABRASF 2.01.
- **Interface de linha de comando** (`app.py`) para conversões individuais ou em lote.
- **Interface gráfica** (`gui_app.py`) baseada em *flet* para usuários não‑técnicos.
- **Gerador de contratos de locação** a partir de JSON, produzindo XML pronto para importação.
- **DANFE Estadual (NF-e Modelo 55) real**: detecção estrutural (independe do emitente) de um DANFE genuíno de PRODUTO/mercadoria (tributado por ICMS/IPI, tabela de itens com NCM/CFOP, bloco de transportador) — distinto de qualquer NFS-e de serviço. Gera XML NF-e 4.00 com a chave de acesso e os valores REAIS extraídos do documento (não uma chave calculada/valores zerados como no antigo *workaround* de NFS-e→NF-e).

## Principais Funcionalidades

- Suporte a múltiplos layouts de NFS‑e com detecção automática (`_detect_layout`).
- OCR automático para PDFs escaneados (sem texto extraível).
- Processamento de PDFs com múltiplas páginas e múltiplas notas por página.
- Geração de lote XML (`ListaNfse`).
- Conversão de contratos de locação (dados preenchidos via GUI ou JSON).
- **Avisos de baixa confiança** (`Nfse.avisos`): sinaliza quando um campo caiu em valor de fallback (número/CNPJ zerado, data atual, valor zero) em vez de falhar silenciosamente.
- Logs detalhados e barra de progresso.

## Como Instalar

```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt  # dependências principais
pip install "flet[desktop]"   # para a GUI
```

## Uso via CLI

```bash
# Conversão de um PDF único
python app.py --input path/to/file.pdf --output path/to/output.xml

# PDF com várias notas: converter só páginas específicas (alternadas ou intervalo)
python app.py --input path/to/file.pdf --output path/to/output.xml --pages "1,3,6"
python app.py --input path/to/file.pdf --output path/to/output.xml --pages "1-3,6"

# Conversão em lote (diretório)
python app.py --batch path/to/pdfs --output path/to/output_dir

# Conversão de contrato de locação (JSON)
python app.py --contrato path/to/contrato.json --output path/to/output_dir
```

> `--pages` vale só com `--input` (um único PDF). Aceita páginas soltas (`1,3,6`) e intervalos (`1-3,6`); a numeração é a **página real do PDF** (1-based) e só páginas com nota reconhecida são geradas. Na GUI, o mesmo é feito pela janela de seleção com caixas por página (ver [COMO_USAR_GUI.md](COMO_USAR_GUI.md)).

## Uso via GUI

```bash
python gui_app.py
```

## Estrutura de Diretórios

```
python-automation-pro/
├─ src/                     # Código-fonte principal
│  ├─ extractors/           # Extratores de PDF (pdf_extractor.py: SPPdfExtractor)
│  ├─ transformers/         # Transformadores para XML (abrasf_transformer.py)
│  ├─ models/               # Modelos de dados Pydantic (Nfse, Entidade, Endereco, Valores)
│  └─ utils/                # Utilidades auxiliares (ibge_resolver.py, etc.)
├─ tests/                   # Testes unitários (um test_<cidade>_layout.py por layout)
├─ templates/               # Templates de UI (se houver)
├─ gui_app.py               # Aplicação de desktop baseada em flet
├─ app.py                   # CLI de alto nível
├─ DOCUMENTACAO_CONVERSAO.md# Detalhamento de cada layout suportado
├─ COMO_USAR_GUI.md         # Guia da interface gráfica
└─ README.md                # Documentação principal (este arquivo)
```

## Adicionando um Novo Layout

O extrator é escalável por layout, sem reescrever o núcleo. O checklist típico (ver detalhes em [DOCUMENTACAO_CONVERSAO.md](DOCUMENTACAO_CONVERSAO.md)):

1. Definir a constante `LAYOUT_<NOME>` em `pdf_extractor.py`.
2. Adicionar a marca de detecção em `_detect_layout` **e** `_detect_layout_page`.
3. Plugar ramos específicos onde o layout divergir do genérico (`_extrair_numero`, `_extrair_valores`, `_extrair_entidade`, etc.).
4. Registrar a cidade no `IBGEResolver.KNOWN_CITIES` (`src/utils/ibge_resolver.py`) quando aplicável.
5. Criar `tests/test_<nome>_layout.py` validando contra o **texto real** do PDF (imagem→OCR e/ou digital), não só mocks limpos.

## Contribuição

1. Fork o repositório.
2. Crie uma branch para sua feature (`git checkout -b feature/minha-feature`).
3. Submeta seus testes e documentação.
4. Abra um Pull Request.

---

*Documentação atualizada em 2026‑08‑28 (52 layouts de NFS-e + 1 DANFE Estadual); **novo layout — São Paulo/SP, CAIXA CARTÕES (mesma plataforma Qive do SKYTEF, outro emitente, `sp_caixa_cartoes`): fatura de "Taxa de Serviço" (reembolso/adquirência de cartões pré-pagos) da CAIXA CARTÕES PRÉ-PAGOS S.A. (CNPJ 39.459.331/0006-34, nota real nº 27723844, R$6,30) caía no fallback `generico`** — detectado só pelo CNPJ do emitente (nunca pela marca "Qive", mesmo critério do SKYTEF, para não colidir com outros emitentes na mesma plataforma); prestador fixo (hardcoded do letterhead); tomador dinâmico com extrator dedicado e separado do SKYTEF (mesmo racional já usado entre `nfcom_salvador`/`nfcom_rlgr`), tolerante à razão social colada na mesma linha do rótulo "Nome / Nome Empresarial" (diferente do quirk em 2 linhas do SKYTEF); ValorIr extraído de um valor de IRRF real citado na discriminação (R$0,09, I.N. 153/87, Lei 7450/85 art. 53), não fabricado nem zerado como no SKYTEF. Suíte 347→**351 verdes**; teste novo `test_sao_paulo_caixa_cartoes_layout.py` (4 testes). Antes: **novo layout — São Paulo/SP, SKYTEF (plataforma Qive, `sp_skytef`): fatura de licenciamento de uso de software (SKYTEF SOLUÇÕES EM CAPTURA DE TRANSAÇÕES LTDA, CNPJ 04.988.631/0001-11, nota real nº 902735, R$86,80) caía inteira no fallback `generico`, com entidades trocadas/garbladas e valores zerados** — detectado só pelo CNPJ do emitente (nunca pela marca "Qive", plataforma SaaS compartilhada por outros emitentes, mesmo critério já usado em ISSNet Cuiabá×Goiânia e Mata de São João/SAATRI); prestador fixo (hardcoded do letterhead); tomador dinâmico tolerante à razão social deslocada ANTES do cabeçalho de seção "Dados do Tomador de Serviços" (o mesmo rótulo "Nome / Nome Empresarial" aparece antes, colado na mesma linha do valor do prestador) e à Competência impressa como valor órfão ("02/2026") antes do rótulo "Numero da NFS-e:", sem rótulo "Competência" adjacente ao próprio valor. Suíte 325→**329 verdes**; teste novo `test_sao_paulo_skytef_layout.py` (4 testes). Antes: **fix — NFCom Rlgr Telefonia (`nfcom_rlgr`): nota real nº 29377, 1ª nota ESCANEADA deste layout — o OCR de página inteira com PSM padrão funde os 2 blocos lado a lado do cabeçalho, vazando a coluna direita (NOTA FISCAL Nº/DATA DE EMISSÃO/CHAVE DE ACESSO) pra dentro da razão social do tomador e quebrando CNPJ (1 dígito trocado no meio), endereço e Código de Verificação** — corrigido com uma 2ª tentativa de OCR em `--psm 4` (separa as 2 colunas corretamente), trocada quando pontua pelo menos empatado com a leitura padrão; marca de detecção ampliada pra tolerar a palavra "FISCAL" do título partida ao meio (efeito colateral do PSM 4); validação de checksum adicionada ao CNPJ do tomador (cai no sentinela em vez de propagar dígito trocado). Grade de valores sai impressa em cinza muito claro (acima dos limiares usuais de binarização) — recuperada com recorte dedicado (autocontraste + limiar alto de 230), agora extraindo R$71,37 corretamente. Suíte 332→**339 verdes**; 7 testes novos `test_nfcom_rlgr_escaneado.py`. Antes: **novo layout — NFCom Rlgr Telefonia (`nfcom_rlgr`): nota real nº 7271 (SINDICATO DOS DELEGADOS DE POLICIA DO ESTADO DA BAHIA ADPE, R$71,37 de Serviço de Terminação de Tráfego de Voz/STTV), mesmo template nacional NFCom (portal SVRS) já usado por `nfcom_salvador`, mas de um emitente diferente (Rlgr Telefonia LTDA, CNPJ 57.675.896/0001-26, Barueri/SP)** — sem detecção própria caía no fallback genérico e saía com o CNPJ do tomador igual ao do prestador, razão social do tomador vazada de um rótulo vizinho ('REFERÊNCIA (ANO/MÊS)'), valores todos zerados e Código de Verificação em branco. Detecção gated pelo CNPJ deste emitente especificamente (mesma decisão já aplicada à EBJ do `nfcom_salvador`); prestador fixo (letterhead hardcoded); tomador extraído dinamicamente com rótulos 'NOME:'/'CPF/CNPJ:'/'ENDEREÇO:' em ordem DIRETA (ao contrário do `nfcom_salvador`, onde vêm parcialmente invertidos) — e, diferente daquele layout, o CEP do tomador é extraído de verdade, pois esta nota o imprime inline no endereço. Valor via 'TOTAL A PAGAR:'; Base de Cálculo/Alíquota/ISS mantidos em 0,00 (tributado por ICMS, não ISS), sinalizado em aviso. Suíte 325→**332 verdes**; 7 testes novos `test_nfcom_rlgr.py` (inclui extração dedicada da Data de Emissão, data+hora na mesma linha do rótulo). Antes: **fix — Salvador/BA (`salvador_ba`): PSM automático do OCR derruba o bloco INTEIRO do prestador e a grade de valores, SEM marca d'água nenhuma (nota real nº 00024910, BDP LOGÍSTICA INTEGRADA DE RESÍDUOS LTDA → BONI TRANSPORTES, PDF de 1 página só)** — pedido do usuário para verificar o layout Salvador e criar `salvador_bdp` se necessário; confirmado o MESMO template oficial já coberto, não é layout novo. A leitura de página inteira em zoom 3x/PSM automático pulava direto de "Código de verificação:" pra "Endereço:" — rótulo do prestador, CPF/CNPJ e Nome/Razão Social nem apareciam, e a grade de valores saía por completo perdida. `_ocr_page` agora tenta um 2º OCR de página inteira no mesmo zoom com `--psm 6` sempre que o layout Salvador é detectado, e troca `best_text` só quando essa leitura pontuar melhor em `_score_ocr_text` (zero risco pras notas já validadas onde o PSM padrão já basta). Isso expôs 3 bugs latentes no fallback genérico de entidade/valor: razão social do prestador pegando a própria linha do CNPJ sem o rótulo "Nome/Razão Social"; razão social do tomador engolindo o CNPJ formatado e o endereço inteiro sem o rótulo "CPF/CNPJ"; e "VALOR TOTAL DA NOTA" saindo sem separador decimal, resolvido com a linha "Valor Liquido R$ X" como último recurso em vez de zerar. Também generalizada a votação do Número da Nota (2 amostras novas, critério de maioria relaxado de estrita pra pelo menos metade). CNPJ de ambas as entidades, Código de Verificação e logradouro/CEP continuam ilegíveis mesmo após o PSM 6 — mantidos como sentinela/"Não informado", nunca fabricados (mesma família registrada numa issue GitHub aberta pro padrão recorrente Salvador/Luniteck-BONI). Suíte 321→**325 verdes**; 4 testes novos `test_salvador_bdp_psm_pagina_inteira.py`. **fix — Salvador/BA (`salvador_ba`): razão social fabricada com ruído em vez de sentinela honesto, e Código de Serviço perdido, na MESMA nota nº 2419 já catalogada como catastroficamente degradada na pág.1 (LUNITECK SOLUÇÕES E DESENVOLVIMENTO EM TECNOLOGIA LTDA ME → BONI TRANSPORTES — pág.2/NFTS já correta, sem alteração)** — auditoria pedida pelo usuário: a proteção `_NOISE_RAZAO` que deveria rejeitar o próprio rótulo garblado "CPF/CNPJ Inscrição Municipal" como razão social tinha um bug de `` que nunca casava contra a palavra completa "Inscrição"/"Endereço" — corrigido de forma genérica (beneficia ~30 layouts); razão social da BONI TRANSPORTES (tomadora) vazando para o bloco do PRESTADOR quando o cabeçalho TOMADOR não sobra reconhecível — guard gated novo; 3 guards genéricos contra ruído puro (colchete/pipe, dois-pontos, fragmento de 3 letras); comparação fuzzy (`difflib`) contra o rótulo canônico "Nome/Razão Social" no lugar de mais uma regex literal por variante de garble; e novo fallback de Código de Serviço via "Código de Tributação do Município" (recupera 14.02, antes caía no genérico "03115"). Tentativa revertida durante o desenvolvimento: um requisito extra de "parece nome de empresa" causou a busca atravessar a quebra de página e deduplicar as 2 páginas como se fossem a MESMA nota (perdendo a nota da pág.1 do resultado) — revertido antes de entrar na suíte. Razão social do prestador nesta nota segue sem recuperação garantida (mesma decisão de 2026-08-21 de não perseguir mais fixes nesta página catastroficamente degradada). Suíte 314→**319 verdes**; 5 testes novos `test_salvador_lauro_freitas_2419_razao_e_codigo_servico.py` **Ampliação na MESMA leva (nota real nº 2418, PDF irmão da 2419, mesmo prestador LUNITECK):** a heurística "confiar na grade de Base de Cálculo recuperada quando diverge da linha isolada VALOR TOTAL DA NOTA" trocou um valor CORRETO por um ERRADO nesta nota — "VALOR TOTAL DA NOTA = R$397,14" estava certo e legível, mas a mesma grade densamente corrompida fez o recut ler "8,00" — corrigido exigindo divergência ≤10% (plausível como 1 dígito trocado) antes de confiar na grade; divergência grande mantém o valor do cabeçalho. Decisão do usuário via `AskUserQuestion`: não implementar o padrão "prestador fixo" pra LUNITECK apesar do CNPJ raiz já confirmado em 2 notas reais. Suíte 319→**321 verdes**; 2 testes novos `test_salvador_2418_valor_grade_recut_divergencia.py`.. Antes: **fix — Salvador/BA (`salvador_ba`): Prestador/Tomador colidiam no mesmo CNPJ e Valor Total saía errado quando o PSM automático do Tesseract derruba a razão social do prestador e o cabeçalho "TOMADOR DE SERVIÇOS" (nota real nº 00000061, MCLA CONSTRUÇÕES LTDA → BONI TRANSPORTES, LOGISTICA E COMERCIO LTDA)** — a leitura de página inteira derrubava por completo a razão social do prestador e corrompia "TOMADOR DE SERVIÇOS" a ponto da palavra "TOMADOR" não sobreviver, então o bloco genérico do prestador vazava a razão/CNPJ do TOMADOR para as duas entidades — a guarda existente do CNPJ crônico da BONI então disparava para as duas, reforçando o erro; a linha "VALOR TOTAL DA NOTA" também saía com 1 dígito errado, defeito irrecuperável mesmo em releitura ultra-zoom dedicada da própria linha. Corrigido tolerando vírgula/espaço espúrio no separador do CNPJ, emendando (não prependando solto) a razão social recuperada do prestador na posição certa do texto, e cruzando o Valor Total com a Base de Cálculo da grade (2 leituras redundantes) quando divergem. Suíte 309→**313 verdes**; teste novo `test_salvador_prestador_tomador_colisao_psm_padrao.py`. Antes: **Ampliação na MESMA leva (nota real nº 00000006, RC INFORMÁTICA E ACESSÓRIOS LTDA → BONI TRANSPORTES):** o próprio rótulo "Nome/Razão Social" saía garblado a ponto de nenhum filtro reconhecer ("NomeiRazão Socia'" — a "/" vira "i" e o "l" final some), roubando a linha real (a empresa) que vinha logo depois; e 2 recortes dedicados do Salvador calculavam gatilho/índice sobre o texto JÁ ACUMULADO com prepends sintéticos de OUTROS recortes, em vez do texto real da página — o gatilho da marca d'água dava falso-positivo e o recorte de CNPJ com dígito trocado colava o CNPJ do PRESTADOR no bloco do TOMADOR. Suíte 313→**314 verdes**. Antes: **novo layout Barueri/SP (`barueri_sp`) e release v1.4.0 — leva de fixes em Lauro de Freitas/BA (3ª variante, `lauro_de_freitas_ba`) e Salvador/BA (`salvador_ba`), notas reais MAG COMERCIO VAREJISTA/LUNITECK → BONI TRANSPORTES**: (1) suporte à 3ª variante do template Lauro de Freitas, compatível com a Reforma Tributária; (2) zoom único não confiável nessa 3ª variante — corrigido com reamostragem+votação (6 zooms × 2 PSMs) pro Número NFS-e/Data de Emissão/CEP/grade de Valores em vez de um recorte fixo; (3) guard de Competência com ano trocado pelo OCR generalizado para TODOS os layouts (antes só disparava dentro do branch do Salvador, deixando os outros ~44 layouts sem essa proteção); (4) Salvador: Número da Nota e CPF/CNPJ do prestador saindo com 1 dígito trocado por um zoom "azarado" no recorte de cabeçalho; (5) grade de valores do Lauro de Freitas PARTIDA em 3 pedaços não-contíguos (nota 2026326/NFTS LUNITECK) — Deduções/Base/Alíquota, Valor do ISS e ISSQN Retido saem em linhas separadas por outro conteúdo no meio, recuperados com 3 capturas dedicadas em vez do fallback zerado; (6) Salvador/Lauro de Freitas: Código de Verificação com falso-positivo, CNPJ com vírgula no lugar do ponto, discriminação vazada e rótulo do tomador garblado ("TOMADOR **DE** SERVIÇOS"→"BE SERVIÇOS") corrigidos numa 2ª nota da mesma leva (2418). Suíte 298→**310 verdes**. Antes: **fix — Brasília/DF (`brasilia_df`): endereço/município/UF/e-mail/telefone do TOMADOR (e do prestador) saindo corrompidos numa nota real (nº 44, AFG DIGITAL COMUNICACAO E PRODUCAO LTDA → ELOS ESTUDIO E SERVICOS LTDA, R$ 4.950,00), reportado pelo usuário** - 3 bugs no extrator GENÉRICO de entidade (compartilhado por ~30 layouts, não específicos do Brasília): (1) a captura de Endereço não parava antes do rótulo "Cidade:" (só reconhecia "Município"/"Municipio"), engolindo a linha inteira seguinte dentro do campo Número; (2) o casamento de Município/UF também não reconhecia "Cidade:" isolado (plataforma "ISS.NET - Sistema Nota Control"), caindo no fallback de capital (Salvador/BA); (3) "Estado/Prov./Reg.:" imprime o nome COMPLETO da UF ("Distrito Federal"), não a sigla de 2 letras que a regex exigia - novo dicionário `_UF_POR_NOME_ESTADO` resolve o nome completo. Achados colaterais (bugs pré-existentes independentes, não gated a este layout): e-mail/telefone saíam sempre `None` porque a regex genérica não tolerava o ":" impresso entre rótulo e valor, e a regex de Telefone tinha um `{8,20}` com chave simples dentro de uma f-string - o Python interpreta isso como a tupla `(8, 20)` e insere o literal errado na regex, quebrando o casamento sem erro de sintaxe. Suíte 268→**269 verdes**; teste novo em `test_brasilia_layout.py`. Antes: **novo layout Goiânia/GO (`goiania_go`), plataforma ISSNet Online (issnetonline.com.br/goiania) - nota real nº 4, R$ 600,00 - e fix de colisão de detecção com o Cuiabá/MT (`cuiaba_issnet`)** - a nota inteira caia em `LAYOUT_CUIABA` porque aquele detector casava a palavra solta "ISSNet" (sem exigir "Cuiabá" por perto) em QUALQUER documento que a contivesse, e "issnetonline.com.br/goiania" contem "issnet" como substring - zerava `valor_servicos`, trocava `ValorIss`/`ValorIr` (saiam ambos 600,00, o real e ValorIss=12,06/ValorIr=0,00), razao social do prestador saindo "Serie do Documento" (rotulo solto do letterhead), razao social do tomador saindo igual ao proprio endereco dele, e um Intermediario fantasma inventado a partir do rotulo "Municipio Incidencia". Corrigido exigindo que "ISSNet" nao seja seguido de "online" na marca do Cuiaba, e detectando Goiania pelo nome do MUNICIPIO (nao pela marca da plataforma, compartilhada por varias cidades - mesma decisao ja tomada para Mata de Sao Joao/SAATRI e Rosario da Limeira/FUTURIZE). PDF DIGITAL cuja ordem de leitura do `extract_text()` sai embaralhada de forma NAO-monotonica (nem indice fixo resolve, diferente do Vinhedo) - usa `_reconstruir_texto_por_coordenadas` (mesma tecnica do `camacari_sisloc`) antes de extrair qualquer campo; apos a reconstrucao, o bloco do PRESTADOR fica intercalado linha a linha com os metadados do cabecalho (mesma faixa de Y) - cada regex pula 1 linha ate o valor real. Suite 265→**268 verdes**; teste novo `test_goiania_go_layout.py`. Antes: **novo layout Vinhedo/SP (`vinhedo_sp`), plataforma Balker (vinhedo.balker.com.br) - nota real nº 139, R$ 1.049,79** - caia no fallback `generico`, que fabricava `valor_iss=28.0` (nao bate com o valor real, 41,99), zerava `valor_servicos`, saia com UF "BA" em vez de "SP" para o prestador, cidade do prestador caindo no fallback Salvador/BA (Vinhedo nao cadastrada em `KNOWN_CITIES`), `servico_codigo` saindo "03115" (nao bate com o item real "7.19"), e a razao social do TOMADOR saindo "Pais: BRASIL". Estrutura propria: blocos PRESTADOR/TOMADOR com rotulo-valor adjacente na MESMA linha, mas o cabecalho "TOMADOR DE SERVICOS" desloca para o MEIO do proprio bloco do tomador (mesmo quirk do Santos/SP) - fatiado pela 2a ocorrencia de "Razao Social/Nome:"; grade de retencoes/base/aliquota/ISS com cada valor defasado em 1 coluna em relacao ao proprio rotulo, mapeado por indice fixo. Suite 261→**265 verdes**; teste novo `test_vinhedo_sp_layout.py`. Antes: **novo layout Santos/SP (`santos_sp`), plataforma Ginfes (santos.ginfes.com.br) - nota real nº 16, R$ 6.666,86** - caia no fallback `generico`, que fabricava `valor_iss=14.0` (numero aleatorio do documento), zerava `valor_servicos`, saia com UF "BA" em vez de "SP" para as duas entidades, cidade do prestador caindo no fallback Salvador/BA (Santos nao cadastrada em `KNOWN_CITIES`), e a razao social do TOMADOR saindo igual ao proprio endereco dele. Estrutura propria: campos em pares rotulo-valor adjacentes mas em ordem visual de 2 colunas (cabecalho "Tomador de Servico" desloca para o MEIO do proprio bloco do tomador - fatiado pela 2a ocorrencia de "CPF/CNPJ:"); grade de valores com 13 rotulos fixos mas so 10 valores impressos (ISSQN/IBS/CBS saem em branco para optante do Simples Nacional) - mapeado pelos 2 extremos fixos da lista (9 primeiros + Valor Liquido por ultimo), ISSQN mantido em 0,00 sempre (decisao confirmada do usuario, nunca derivar de Base x Aliquota). Suite 257→**261 verdes**; teste novo `test_santos_sp_layout.py`. Antes: **fix — DANFSe Nacional (`danfse_nacional`), plataforma WebISS (Prefeitura Municipal de Aracaju/SE, nota nº 2026000000014, LY5T-1DG5): "Valor dos Serviços"/"Valor Líquido"/"Base de Cálculo ISS"/"Alíquota ISS" saíam todos ZERADOS (nota real de R$ 4.000,00), reportado pelo usuário** — esta plataforma usa vocabulário próprio ("Valor dos Serviços", plural) e imprime o número da célula SEM o token "R$" (só o rótulo tem o sufixo "(R$)"), formato que os padrões existentes (que exigem "R$ n,nn" logo após o rótulo) nunca casavam. Estendido de forma ADITIVA (fallback só ativa quando o padrão original não casou, zero risco às ~8 outras cidades já cobertas por este layout compartilhado): reconhece o rótulo no plural, aceita número sem "R$", e passa a extrair também Alíquota ISS e as Retenções Federais individuais (PIS/COFINS/INSS/IR/CSLL) desta plataforma. A própria nota imprime "*****" (mascarado) em Base de Cálculo ISS/ISS/ISS Retido (regime ME/EPP do Simples Nacional) — mantidos em 0,00 (nunca fabricar um valor sem lastro no documento), sinalizado por um aviso dedicado. Suíte 256→**257 verdes**; teste novo `test_danfse_nacional_aracaju_webiss.py`. Antes: **fix — São Paulo/SP (`sao_paulo_sp`/`sao_paulo_sp_scan`, mesma nota Valestra nº 00028202): intermediário FANTASMA sobrevivia ao guard existente por causa de um travessão em vez de hífen** — o guard contra intermediário fantasma (nota UNIMED CNU, 2026-07-31) só reconhecia o placeholder `"----"` (2+ hífens ASCII); esta nota usa um único travessão "—" (em dash) em "CPF/CNPJ: —", que não casava, fabricando `<Intermediario>` com CNPJ sentinela `00000000000100` e `RazaoSocial=": —"` no XML. Corrigido tolerando também 1+ caractere da família en/em dash (nunca usada num CNPJ real), mantendo a exigência de 2+ hífens ASCII (para não colidir com o hífen único de um CNPJ real). Teste existente ampliado; suíte permanece **242 verdes**. Antes: **novo — atualização automática do app (GUI):** checagem automática ao abrir + botão manual "Verificar atualizações", com download e substituição automática do `.exe` em execução via GitHub Releases (decisão do usuário: não só notificar/linkar) — pede confirmação antes de aplicar. Exige publicar Releases de verdade no GitHub com o `.exe` anexado como asset (novo passo manual no processo de release, documentado em "Processo de Release" no DOCUMENTACAO_CONVERSAO.md). Suíte 242→**256 verdes**; teste novo `test_auto_updater.py`. Antes: **fix — São Paulo/SP escaneado (`sao_paulo_sp_scan`, mesma nota Valestra nº 00028202): "Código do Serviço" (item de tributação municipal) desaparecia por completo do OCR (nem rótulo nem valor), caindo no fallback genérico `03115` em vez do real `01899`** — achado ao reconferir o XML campo a campo contra a imagem depois do fix da dobra física acima (o usuário reportou "gerou um arquivo totalmente diferente da nota fiscal informada"); só esse campo estava mesmo errado, os demais já batiam. Recuperado com uma 3ª captura no mesmo recorte dedicado, na mesma região/zoom do IRRF mas com PSM 4 em vez de PSM 6 — a mesma imagem/zoom pode precisar de PSM diferente pra sub-regiões adjacentes da mesma célula. Teste existente ampliado; suíte permanece **242 verdes**. Antes: **fix — São Paulo/SP escaneado (`sao_paulo_sp_scan`): dobra física do papel cobrindo "PREFEITURA DO" no título fazia a nota inteira cair em `LAYOUT_GENERICO` (nota real nº 00028202, VALESTRA NEGOCIOS E INVESTIMENTOS LTDA → MASSA ALIMENTACAO E SERVICOS S/A)** — a mesma dobra derrubava mais 6 campos na mesma nota: Data/Hora de Emissão e Código de Verificação (rótulo some do OCR, recuperado por busca da FORMA do valor), endereço do prestador com 3 segmentos (bairro nem sempre é o último), "Município" sem acento quebrando o casamento de rótulo (ambas as entidades caíam no fallback de capital, Salvador/BA), e-mail do tomador com "@" lido como "Q", razão social do tomador com ";" vazando no início do valor (fix genérico), e as duas grades de valores (retenções federais + Deduções/Alíquota/ISS) ilegíveis em zoom padrão — Base de Cálculo e Valor Líquido passam a ser DERIVADOS matematicamente em vez de re-OCRizados. Suíte 236→**237 verdes**; teste novo `test_sao_paulo2_valestra_fold_defect.py`. Antes: **fix — Salvador/BA (`salvador_ba`): prestador saindo com o CNPJ do TOMADOR** (notas reais nº 2150/2169, INSTITUIÇÃO ASSISTENCIAL BENEFICENTE CONCEIÇÃO MACEDO → BONI TRANSPORTES, reportado pelo usuário) — o recut dedicado de CNPJ com dígito errado exigia ponto literal como separador e o zoom alto às vezes recupera espaço no lugar; regex sem match caía no fallback genérico ("1º CNPJ válido do documento"), que pegava o do tomador. Corrigido para tolerar espaço/tab OU ponto nesse separador. Achados colaterais na mesma nota (2169): Código de Verificação saindo como a palavra "PRESTADOR" (recorte de cabeçalho ganhou tentativas adicionais de zoom/PSM/altura, agora exigindo um candidato plausível antes de aceitar); e CNPJ/razão social/endereço do TOMADOR corrompidos (gatilho do recut passou a validar também o checksum, não só a formatação, e o recut tenta múltiplos zooms em sequência). Suíte 236→**241 verdes**; teste novo `test_salvador_codigo_verificacao_e_tomador_2169.py`. Antes: **add — BIO CONTROL DESINSETIZADORA (Lauro de Freitas/BA, `biocontrol_dedetizadora`): novo layout — 3º sistema diferente para o MESMO município (ao lado da Prefeitura oficial e da plataforma eNotas Gateway), template próprio "DEMONSTRATIVO DA NOTA FISCAL DE SERVIÇO" (nota real nº 202600000036345, BONI TRANSPORTES, LOGISTICA E COMERCIO LTDA, R$5.200,00, dedetização/controle de pragas), antes caía inteira em `LAYOUT_GENERICO`** — detecção pelo CNPJ/razão social do emissor específico; entidades prestador/tomador extraídas DINAMICAMENTE (ao contrário do padrão "prestador fixo" de outras faturas de locação), pois o bloco sai limpo o bastante em zoom 3x; recorte dedicado em zoom 8x (`_ocr_recut_biocontrol`) recupera 2 grades densas que a leitura de página inteira embaralha (Código LC 116 "7.13"→"743"; PIS/COFINS/IR trocados entre si; Alíquota/Valor ISS somem) — validado contra o render real da página, não só o texto OCR. Item LC116 "7.13" (dedetização/controle de pragas urbanas), confirmado pela discriminação real. Suíte 234→**236 verdes**; teste novo `test_biocontrol_layout.py`. Antes: **add — Retenções federais no Portal Nacional (`danfse_nacional`): IRRF, INSS ("Contribuição Previdenciária - Retida") e novo campo `valor_contribuicoes_sociais_retidas` (valor COMBINADO PIS+COFINS+CSLL, rótulo "Contribuições Sociais - Retidas", somado a `OutrasRetencoes` no XML por não haver tag ABRASF própria)** — nenhum dos 3 era extraído antes, mesmo com valor real na nota (ex.: nº 730080, R$ 17,34); achado a partir de um pedido do usuário pra analisar viabilidade de extrair PIS/COFINS/CSLL/Contribuições Sociais/INSS/ISS/IRRF Retidos em todo o conversor. Extração por adjacência ESTRITA rótulo→valor — fica em 0,00 (não adivinha) quando o pdfminer despeja os rótulos sem os valores aparecerem no texto. Não confunde com "PIS/COFINS - Débito Apuração Própria" (débito próprio do prestador, não retenção). Suíte 231→**234 verdes**; teste novo `test_danfse_nacional_retencoes_federais.py`. Antes: 45 documentos suportados (44 layouts de NFS-e + 1 DANFE Estadual); **add — DANFE Estadual / NF-e de Produto (Modelo 55) (`LAYOUT_DANFE_PRODUTO`, novo modelo `NfeProduto` + `NfeProdutoTransformer`): 1º documento de PRODUTO/mercadoria (ICMS/IPI) tratado pelo conversor, estruturalmente diferente de qualquer NFS-e** — nota real de compra de café (GRAN COFFEE COM. LOC. E SERVICOS S.A. → SINDICATO DOS DELEGADOS DE POLICIA DO ESTADO DA BAHIA, nº 52.136, R$ 595,00) caia inteira em `LAYOUT_LOCALIZA` (rótulo genérico "FATURA/DUPLICATA" colidindo com a marca da Localiza), saindo com tomador não identificado, valor zerado e prestador hardcoded errado; detecção ESTRUTURAL (não gated a emitente) no topo da cadeia; XML NF-e 4.00 gerado com chave de acesso e valores de ICMS REAIS do documento (não um workaround calculado). Suíte 229→**231 verdes**; teste novo `test_danfe_produto_layout.py`. Antes: 44 layouts suportados; **fix — São José/SC (`sao_jose_sc`): novo layout — nota real nº 348301 (INTELBRAS S/A, R$ 178,80 → SINDICATO DOS DELEGADOS DE POLICIA) sem nenhum layout dedicado, e um bug mais amplo no `parse_multiple`: um canhoto de recibo ANTES da nota, separado por 200+ hifens, virava uma "nota" fantasma (nº 00000000)** — blocos PRESTADOR/TOMADOR com reordenação própria (Município realocado pro início da sequência de valores) e CEP/UF do prestador deslocados após o cabeçalho do tomador (2 colunas do pdfminer); corrigido descartando blocos de preâmbulo sem nenhum sinal de nota quando nenhuma nota ainda foi iniciada (fix genérico, não gated a um layout); IBGE de São José/SC (4216602) registrado; corrigido também um bug de `city_hint` ausente (fallback silencioso pra capital do estado) em 3 funções de entidade novas. Suíte 227→**229 verdes**; teste novo `test_sao_jose_sc_layout.py`. Antes: **fix — NFCom Salvador (`nfcom_salvador`): novo layout — nota real nº 624 (SIND DELEGADOS DE POLICIA DO EST DA BAHIA, R$ 400,00) caía no fallback `danfse_nacional` e saía com o valor ZERADO e o tomador com a razão social vazada do rótulo "Nº TELEFONE"** — a NFCom (tributada por ICMS, não ISS) não usa os rótulos que o parser da NFS-e Nacional espera; corrigido com detecção específica do CNPJ do emissor (EBJ), extração dedicada do tomador (rótulos e valores em ordem parcialmente invertida) e leitura do "TOTAL A PAGAR (R$)" — BaseCalculo/Aliquota/ValorIss mantidos em 0,00 propositalmente (ICMS ≠ ISS), sinalizado em aviso. Suíte 225→**227 verdes**; teste novo `test_nfcom_salvador_layout.py`. Antes: **fix — São Paulo/SP escaneado (`sao_paulo_sp_scan`): `Numero` saindo com os dígitos da data/hora de emissão em vez do número real (nota real nº 08336055, PLUXEE BENEFÍCIOS BRASIL S.A., pág.23 do lote Guarajuba 07/2026)** — o valor saía com uma aspa espúria colada na frente (`"08336055`), e o gatilho do recorte dedicado de cabeçalho exigia o token inteiro ser dígito puro (`re.fullmatch`), descartando o candidato mesmo com 8 dígitos legíveis; caía no recorte fixo por percentual (calibrado noutra nota), que aqui acerta a caixa "Data e Hora de Emissão" e devolve `16072026203205` em vez de `08336055`. Corrigido trocando `fullmatch` por `search` no gatilho — aceita dígitos com ruído colado antes/depois, mantendo a exigência de 6+ dígitos CONSECUTIVOS. Suíte 224→**225 verdes**; teste novo `test_ocr_header_box_sao_paulo_valor_com_aspa_espuria_colada`. Antes: **fix — Salvador/BA (`salvador_ba`): Código de Verificação, CNPJ/razão social do PRESTADOR e grade de valores saindo todos ERRADOS/zerados numa nota real escaneada com marca d'água diagonal cobrindo a página inteira (nº 00039029, A LIMPCANO DESENTUPIMENTO E SUCÇÃO DE FOSSAS LTDA - EPP → SOHO RESTAURANTE LTDA)** — o padrão de pontos (halftone) do carimbo degrada o OCR onde cruza texto impresso, corrompendo o rótulo "PRESTADOR DE SERVIÇOS" (nenhuma etiqueta reconhece) — o bloco genérico da entidade virava o documento INTEIRO e o CNPJ/razão do TOMADOR vazava para as DUAS entidades — e os rótulos da grade de valores, zerando `valor_servicos` e `base_calculo` juntos. Corrigido com 4 recortes dedicados gateados por evidência do defeito: Código de Verificação e bloco do Prestador via despeculagem (filtro de mediana) em zoom alto; grade de valores por CÉLULA individual (ISS derivado matematicamente de Base × Alíquota; Crédito/Outras Retenções fixados em 0,00). Suíte 223→**224 verdes**; teste novo `test_salvador_marca_agua_pagina.py`. Antes: **fix — DANFSe Nacional: razão social do prestador saindo como o PRÓPRIO ENDEREÇO dele (nota real nº 4, Camaçari/BA, prestador MEI ANA PAULA RIBEIRO DA SILVA)** — a grade imprime "Nome / Nome Empresarial" e "E-mail" colados na mesma linha de valores sem separador, e o OCR corrompeu o "@" do e-mail em `" (O"` (espaço + parênteses + O), um padrão não coberto pela tolerância existente (`Q`/`O`/`.` colados sem espaço); a linha inteira era descartada como razão inválida, e o fallback linha-a-linha acabava aceitando a linha de Endereço/Município/CEP como razão social. Corrigido tolerando `"(O"`/`"QO"` (com espaço opcional antes) como forma corrompida do "@". Suíte 214→**215 verdes**; teste novo `test_danfse_nacional_razao_social_email_colado.py`. Antes: **fix — NF-e de Serviço de Comunicação (`telecom_comunicacao`): 6 bugs achados num review de uma nota real (nº 31696, F&F Comunicações/Grupo F&F → Boutique Guarajuba/PH Gestão, R$558,40), layout que não tinha teste nenhum até então** — (1) colisão de detecção com `ff_locacao` (mesmo CNPJ emissor, documento diferente) — o título da fatura de comunicação agora tem prioridade sobre o CNPJ do emissor na cadeia de detecção; (2) CNPJ do prestador saía IGUAL ao do tomador quando o OCR degradava o separador do CNPJ da F&F (vírgula em vez de ponto) — corrigido tolerando o ruído e excluindo candidatos já rotulados "CNPJ/CPF" (sempre do tomador); (3) a leitura padrão (zoom 3x) perdia a coluna direita inteira do cabeçalho (número, data de emissão, referência, vencimento, total) — número caía num fallback genérico perigoso que pescava o número da Resolução ANATEL citada no rodapé em vez do real; novo recorte dedicado (`_ocr_recut_telecom_comunicacao`, zoom 6x, prependado) resolve; (4) total a pagar com rótulo colado sem vírgula decimal causaria valor 100x maior — corrigido tratando os 2 últimos dígitos como centavos quando não há separador; (5) endereço do tomador vazava o do prestador (tomador sem "Rua/Av" no próprio endereço) e o município vazava o bloco anterior colado por um regex que casava quebra de linha — ambos corrigidos; (6) nomes do prestador/tomador saíam corrompidos pelo próprio recorte de zoom alto (ruído de colunas fundidas) quando prependado — corrigido preferindo a cópia mais limpa (leitura padrão) e removendo ruído de pontuação nas pontas. Decisão do usuário: manter a razão social do prestador como vem do OCR ("Grupo FeF", nome comercial — não hardcoded, ao contrário do `ff_locacao`). Zero regressão; suíte 211→**218 verdes**; teste novo `test_telecom_comunicacao_ff_layout.py` (7 testes). Antes: **fix — Camaçari/BA escaneado (`camacari_ba_scan_v3`): número da nota saía zerado (`00000000`) quando uma das 3 tentativas de recorte do cabeçalho degrada o rótulo "Número da Nota" para "nero da Nota" (nota real nº 285, pág.20 do lote PH Gestão 07/2026, AVANÇO GESTÃO E ADMINISTRAÇÃO LTDA → PH GESTÃO, R$572,42)** — a âncora original exigia literalmente "...mero da Nota" (tolerando só a troca do "ú"→"i"), mas essa leitura específica perde o "úm" inteiro; a ocorrência com o rótulo limpo, mais abaixo no corpo do documento, não tem número nenhum por perto (cai antes do nome da prefeitura). Diferente da nota nº 20335 (PADUA, já catalogada — ali o número colado ao rótulo degradado era simplesmente ERRADO, sem eco em nenhum outro lugar do texto, e por isso o layout vai direto pro fallback honesto do nome do arquivo), aqui o "285" é corroborado por aparecer também como 1ª linha solta de OUTRO bloco do cabeçalho (as 3 tentativas de recorte concordam). Corrigido tolerando a âncora degradada "nero da Nota", mas só aceitando o número colado quando ele repete como linha isolada em outro bloco do texto (evita aceitar um dígito de ruído — testado que exigir apenas "o número repete em qualquer lugar" é enganoso: nesta mesma nota, "09" repete por coincidência em timestamps HH:MM). Zero regressão (baseline-vs-fix no lote de 24 notas do mês muda só a página-alvo; suíte Padua/20335 intacta); suíte 211→**212 verdes**; teste novo `test_camacari3_numero_nota_degradado_nero.py`. Antes: **fix — Monte Santo/BA: `municipio_incidencia_override` (revisão)** — a nota traz "Local do Serviço: Fora do Município" e a cidade da obra em texto livre ("OBRA: ..., <CIDADE>/<UF>"); a decisão original (quando o layout Monte Santo foi criado, mesma data) tratou essa cidade como "só texto livre, frágil demais" e deixou o override de fora — revista após um review completo do layout com comparação ao XML gerado: a linha "OBRA:" sempre termina no formato ", <CIDADE>/<UF>" (âncora no ÚLTIMO "," da linha, tolerando vírgulas dentro da própria descrição), risco equivalente ao já aceito noutros layouts (ex.: Guarulhos/SP). `Nfse.municipio_incidencia_override` agora também cobre Monte Santo: quando presente, a incidência do ISSQN (`Servico.MunicipioIncidencia`/`Servico.CodigoMunicipio`) vai para o município da obra (ex.: Camaçari, IBGE `2905701`), mantendo o cadastro/endereço do prestador em Monte Santo (IBGE `2921550`) intocado; teste `test_monte_santo_layout.py` estendido com a asserção do override. Antes: **fix — Salvador/BA escaneado: tomador extraído com o CNPJ ERRADO por recut de OCR desnecessário (nota real nº 00011629, SAFE - SEGURANÇA ELETRÔNICA LTDA → MANUELLA CARVALHO MARTINS BAHIA, R$699,00)** — o OCR da página principal já lia o CNPJ do tomador corretamente (`13.583.542/0001 -86`, só com um espaço extra antes do hífen — ruído tolerado pela extração real), mas o gatilho do recut `_ocr_tomador_salvador` exigia o formato `\d{2}\.\d{3}\.\d{3}/\d{4}-\d{2}` sem tolerar esse espaço, disparando o recut mesmo sem necessidade; o recut lia o CNPJ ERRADO (reprovava checksum) e, por ser prependado ao texto, criava um 2º bloco "TOMADOR DE SERVIÇOS" que o fatiamento genérico encontrava primeiro — o CNPJ correto do bloco original nunca era alcançado, e o tomador caía no sentinela `00000000000100` com o aviso "Dados do tomador não identificados". Corrigido alinhando a tolerância do gatilho à da extração real (mesmo padrão de espaço em volta do hífen). **Ampliação na MESMA nota:** o Código de Verificação saía `ALVADORETNEWBUQ` em vez de `ETNEWBUQ` — o OCR funde o fim de "Salvador" (do título, impresso bem perto) ao valor real, com ou sem espaço, e o guard antigo exigia um dígito no candidato para aceitá-lo; mas o código real desta nota (e de um caso anterior já catalogado, "YRYSURMV") é só letras, então o guard rejeitava o valor certo e caía num fallback que reconcatenava o prefixo "ALVADOR"; corrigido pulando esse prefixo explicitamente na regex e removendo a exigência de dígito. Zero regressão; suíte 207→**211 verdes**; teste novo `test_salvador_tomador_cnpj_espaco_antes_hifen.py`. Antes: **fix — São Paulo/SP escaneado: número da nota saía errado (`392` em vez de `05121900`, nota real FLASH TECNOLOGIA E INSTITUICAO DE PAGAMENTO LTDA)** — `_ocr_header_box_sao_paulo` recortava a caixa "Número da Nota" por percentual FIXO da altura da página (calibrado contra a nota BOM NEGÓCIO), mas a altura do cabeçalho acima dessa caixa (logo + título + "RPS Nº..." + "NOTA FISCAL ELETRÔNICA...") varia de nota para nota; nesta, o recorte caiu 1 caixa abaixo do esperado e leu "Código de Verificação" (`MKT3-B9ZH`) em vez de "Número da Nota", e a whitelist de dígitos do OCR "inventou" um número (`392`) a partir das letras. Corrigido localizando o rótulo "Número da Nota" dinamicamente via `image_to_data` (zoom 3x, restrito à metade direita/terço superior da página, onde a caixa sempre fica) antes de recortar em zoom alto — imune à altura variável do cabeçalho; recorte fixo antigo mantido como fallback. Achado colateral na mesma investigação: a whitelist de dígitos também gera um dígito espúrio a partir da borda vertical da célula (`|`→`4`) colado ao número real — corrigido extraindo o MAIOR grupo contíguo de dígitos em vez de concatenar tudo. Validado ponta a ponta contra o PDF real (número, código de verificação e demais campos saindo corretos). **Ampliação na MESMA nota:** a hora de emissão saía sempre `00:00:00` — a extração genérica de Data de Emissão (`_extrair_data_emissao`, compartilhada por ~30 layouts, não específica do São Paulo) tenta uma lista fixa de rótulos em ordem de prioridade e retorna no primeiro que casar; o aviso de substituição do RPS desta nota ("...emitido em 06/07/2026") casava com o rótulo "Emitido em" (1º da lista, sem hora) antes do rótulo "Data e Hora de Emissão" (mais abaixo, mas com o timestamp completo "06/07/2026 16:41:44" do cabeçalho de verdade). Corrigido coletando TODOS os rótulos que casaram antes de decidir e preferindo o primeiro candidato COM hora, em vez do primeiro da lista — comportamento idêntico para todo layout com um único rótulo casando. Zero regressão; suíte 206→**207 verdes**; teste novo `test_data_emissao_prefere_horario_completo.py` (cobre os 2 bugs desta nota). Antes: **fix — Lauro de Freitas/BA: ISSQN devido em outro município não mudava a incidência (nota real nº 202645, ADRIAN NASCIMENTO DE JESUS → SÃO PEDRO CONSTRUTORA, obra em Salvador/BA)** — a nota indica explicitamente "LOCAL DA PRESTAÇÃO DO(S) SERVIÇO(S): SALVADOR - BA" e "Tributado fora do Município de Lauro de Freitas" (obra de construção civil, LC 116/2003 art. 3º III), mas `MunicipioIncidencia`/`Servico.CodigoMunicipio` saíam com o município do prestador (Lauro de Freitas, `2919207`) em vez de Salvador (`2927408`) — endereços de prestador/tomador continuam corretos (cada um na própria sede), só a incidência do ISSQN estava errada. O override de incidência (`_extrair_municipio_incidencia_override`) já existia mas só cobria o layout Guarulhos/SP, cuja frase-gatilho ("Tributação fora do município") e rótulo ("Local da Prestação:", sem "DO(S) SERVIÇO(S)" no meio) são levemente diferentes dos de Lauro de Freitas; estendida a mesma função para também cobrir `LAYOUT_LAURO_FREITAS` com sua própria âncora textual, sem criar layout novo. Zero regressão; suíte 205→**206 verdes**; teste novo `test_lauro_freitas_incidencia_fora_municipio.py`. Antes: **fix — 4 bugs no Salvador/BA achados num review de uma nota real (nº 00006508, INTERNET TECNOLOGIA → ITACIMIRIM PARTICIPAÇÕES)**: (1) CNPJ do prestador e do tomador reprovam o checksum (1 dígito corrompido no scan de cada um) e corretamente caem no sentinela — mas o filtro de Inscrição Municipal só excluía o CNPJ já resolvido, não o candidato bruto rejeitado, e os próprios dígitos do CNPJ vazavam pra IM; corrigido rastreando também os candidatos rejeitados. (2) O `Numero` do endereço do prestador nunca era separado do complemento/bairro/cidade pelo bloco Salvador-específico (só corrigia logradouro/bairro/município) — corrigido com a mesma técnica de separação já usada no `LAYOUT_SAO_PAULO_2`. (3) A grade Base de Cálculo/Alíquota/Valor do ISS saía errada quando o rótulo "Alíquota (%)" vinha com um dígito de ruído de OCR embutido ("Alíquota (9%)") — as 3 regex antigas liam esse ruído como se fosse um valor; corrigido casando a linha de 5 valores de uma vez, na posição, mesma técnica já usada na grade vizinha. (4) Código de serviço caía no fallback genérico "03115" quando o OCR lia "ltem" em vez de "Item" — corrigido tolerando as duas grafias. (5) O endereço do tomador tinha "SN" (sem número) colado ao logradouro sem vírgula ("RUA ALA DAS DUNAS SN") — achado ao verificar o fix #2 ponta a ponta contra o PDF real; a regex de número só casava dígitos, corrigido tolerando "SN"/"S/N" também. Nenhum exigiu layout novo (`salvador2` cogitado e descartado — mesmo template físico, só ruído de OCR não coberto); zero regressão nos 4 testes Salvador pré-existentes; teste novo `test_salvador_cnpj_invalido_endereco_e_grade.py` (7 testes); suíte 198→**205 verdes**. **Ampliação na MESMA nota, depois de confirmar na prática que o sentinela bloqueava a importação real do usuário (Domínio Sistemas rejeitava a nota inteira por "CNPJ do fornecedor inválido"):** lendo a imagem da própria página confirmei que os dois CNPJs são VÁLIDOS de verdade — o OCR trocou 1 dígito NO MEIO do número (não no verificador), ex. prestador `...699...`→real `...688...`. Testei "corrigir só o dígito verificador" contra uma API pública de CNPJ antes de olhar a imagem e bateu numa empresa REAL só que de outro estado — confirma que adivinhar dígito é perigoso. Novo recorte dedicado (`_ocr_recut_cnpj_invalido_salvador`, gated só quando o checksum já reprovou) localiza o rótulo "CPF/CNPJ" dinamicamente e reprocessa em zoom 8x só a linha de valores, validando o resultado antes de aceitar — nunca propaga um valor não validado no lugar do sentinela. Antes: **fix — PASSWORD/eNotas Gateway ganha um 2º emitente (INFOMIX Soluções em Tecnologia LTDA, Lauro de Freitas/BA)** — a nota caía em "layout não reconhecido" (0 XML gerado); detectada pelo próprio CNPJ do emitente (mesmo racional já usado para não colidir com futuros emitentes na mesma plataforma), reaproveitando o MESMO extrator dinâmico/genérico do PASSWORD (nenhuma constante `LAYOUT_*` nova). Dois bugs de robustez corrigidos no código compartilhado: (1) o código de serviço no formato "NN.NN / <código interno>" tinha regex rígida exigindo 4 dígitos no código interno — quebrava com "01.07 / 107" (3 dígitos, sem zero à esquerda), truncando o resultado; corrigido extraindo o item LC116 (sempre 4 dígitos) em vez do código interno do gateway; (2) razão social do tomador podia sair como o rótulo "E-MAIL" quando os rótulos "NOME/RAZÃO SOCIAL" e "E-MAIL" vêm despejados juntos, antes dos 2 valores (mesma classe de bug de "rótulos despejados" já vista em outros layouts) — corrigido com um pré-check específico desse padrão antes do fallback genérico. Zero regressão na nota PASSWORD original já validada; teste `test_password_enotas_layout.py` estendido com fixture e 2 novos testes dedicados a INFOMIX. Antes: **novo layout — Camaçari/BA via SISLOC/NFS-e Easy (Benefix)** — `camacari_sisloc`, nota real nº 24052 (FERIMPORTE SERVICE LTDA → DELTALINE SERVICOS LTDA., R$91,20): PDF totalmente digital cujo gerador desenha rótulo e valor como objetos de texto separados no fluxo do arquivo — o `extract_text()` padrão despejava todos os valores no fim do documento, fora de ordem (número saía "0"); corrigido com uma técnica nova neste projeto — reconstrução do texto a partir das coordenadas de caractere (`_reconstruir_texto_por_coordenadas`, agrupando por proximidade vertical e ordenando por posição horizontal), reproduzindo a ordem visual real da nota antes de qualquer extração de campo; detecção posicionada antes do check genérico de "PREFEITURA MUNICIPAL DE CAMAÇARI" para não colidir com o layout CPqD; valores de retenção extraídos por posição fixa (mesmo princípio do layout Cuiabá); zero regressão (suíte 189→**191 verdes**); teste `test_camacari_sisloc_layout.py`. **fix anterior — nota-fantasma por página em DANFSe Nacional escaneada com UMA única nota** — `danfse_nacional`, PDF "análise de notas SP-iss retido - inss retido.pdf" (062026), págs. 3 e 4 (nota real nº 175, ARMANDO CACAMBA LTDA → SÃO PEDRO CONSTRUTORA, Várzea Grande/MT, locação de caçambas, R$1.650,00): toda DANFSe Nacional imprime seu próprio título "DANFSe v1.0" nos primeiros ~70 caracteres da página (logo após o rótulo "Chave de Acesso da NFS-e") — mesmo com uma única nota ali; o split de múltiplas-notas-por-página de `parse_multiple` (`headers_regex`) cortava nesse 1º "DANFSe", fatiando esse preâmbulo de boilerplate (sem CNPJ/valor nenhum) como uma nota-fantasma própria (número "00000000", prestador/tomador = fragmento garbage "Serviço eletrônica") ANTES da nota real — dando a impressão de "número zero" pra quem olha a 1ª entrada da página; corrigido só tratando como início de nova nota as ocorrências de "DANFSe" a partir de ~200 caracteres do início do bloco (uma 2ª nota genuinamente colada na mesma página só teria seu título bem depois de todo o conteúdo da 1ª), preservando o caso de duas notas de fato coladas na mesma página. Zero regressão (código compartilhado por todos os layouts, sem gate específico — validado com baseline-vs-fix num recorte real de 6 páginas do mesmo lote, mudando só as 2 páginas-alvo). **2ª correção na MESMA nota:** o tomador saía com o CNPJ idêntico ao do prestador (`02.488.708/0001-69` em vez de `03.051.741/0001-90`) — a coluna "CNPJ/CPF/NIF" da grade é comum a todas as entidades e o OCR lê as linhas fora de ordem, colando o CNPJ do prestador dentro do bloco do tomador; corrigido em `_extrair_entidade` (função genérica, compartilhada por ~30 layouts): o CNPJ do prestador já extraído é guardado em `self._cnpj_prestador_extraido` e um candidato idêntico dentro do bloco do tomador/intermediário é descartado a favor de outro CNPJ válido do mesmo bloco, só voltando a aceitá-lo como último recurso se não houver nenhum outro — aditivo, não altera o comportamento quando o CNPJ correto já é encontrado de primeira (validado sem mudança nas outras 6 notas do mesmo lote de 6 páginas). Suíte **189 verdes**; teste `test_danfse_nacional_pagina_unica_sem_fantasma.py` (ambos os bugs). Antes: fix — DANFSe Nacional (Criciúma/SC) com valores estruturados em PONTO decimal saindo zerados — nota real nº 730080 (THOMSON REUTERS BRASIL → CAFES FINOS VITORIA DA CONQUISTA, plataforma Domínio Sistemas): todos os campos monetários da grade vinham com ponto decimal ("R$ 372.96") em vez da vírgula brasileira, e o regex de captura exigia vírgula — corrigido com regex tolerante a ambos os separadores + novo helper `_parse_valor_tolerante`; suíte de 187 verdes; teste `test_danfse_nacional_ponto_decimal.py`. Antes: novo layout Guarulhos/SP — plataforma Ginfes (guarulhos.ginfes.com.br), foto/CamScanner —, nota real nº 3 (KICHLER → SÃO PEDRO CONSTRUTORA, obra Sede de Tecnologia MTI/SECITECI em Cuiabá/MT, R$4.511,41): a leitura padrão de página inteira (zoom 3x) só recuperava ~850 caracteres, perdendo o Código de Verificação, o Local da Prestação e toda a grade "Cálculo do ISSQN devido no Município" (células cinza densas) — a nota inteira era descartada como "Layout não reconhecido"/0 XML; corrigido com zoom 4x/PSM 6 para o corpo da página + um recorte dedicado (`_ocr_recut_guarulhos`, 3 sub-recortes em zoom alto e binarização, mesmo racional do Camaçari) que resolve os campos e devolve um bloco de texto canônico e limpo. Decisão de negócio do usuário: o serviço é construção civil (item 7.02) prestado em OUTRO município — a própria nota traz "Local da Prestação: Cuiabá-MT" + "Natureza Operação: Tributação fora do município" + "ISS a reter: Não" —, então a incidência do ISSQN vai para o município da obra (Cuiabá, `5103403`), não para o do prestador (Guarulhos, `3518800`); implementado como um campo novo e aditivo `Nfse.municipio_incidencia_override` (`None` em todos os outros layouts, usado pelo transformer só quando setado — o endereço próprio do prestador continua apontando Guarulhos, só a tag `<Servico><MunicipioIncidencia>` muda). Município Guarulhos cadastrado em `KNOWN_CITIES` (`3518800`, confirmado via IBGE/RuaCEP, CEP 07043-120 dentro da faixa do município). Zero regressão nos layouts existentes; suíte de 185 verdes, incluindo o novo teste `test_guarulhos_layout.py`. Antes: novo layout Brotas de Macaúbas/BA — município nunca visto antes, PDF de origem em foto/JPG (tipicamente de cabeça para baixo), plataforma nfservico.com.br (mesma do Iaçu/BA); antes desta implementação a nota era 100% rejeitada (0 XML, "Layout não reconhecido"); reaproveita o parser de entidade do Iaçu por estrutura (mesmos rótulos "PRESTADOR/TOMADOR DE SERVIÇOS", "Nome/Razão Social:", "CPF/CNPJ:", endereço em linha única), mas com extrator próprio (endereço do prestador tem complemento — "CASA" — entre número e bairro, e um "|" de OCR colado no número "26-B"; razão do tomador vinha colada ao nome/CREA do engenheiro responsável impresso à direita, cortada pelo token "Eng."); caixa de cabeçalho reaproveita o MESMO recorte dedicado do Iaçu (`_ocr_header_box_iacu`), agora com suporte a ângulo de rotação (aditivo — Iaçu continua com angle=0 inalterado) porque as fotos de Brotas de Macaúbas chegam invertidas (180°) e o recorte sem rotação caía no canto errado (caixa vazia); grade de valores própria (Base de Cálculo/Alíquota/ISS/Crédito + IR/CSLL/Outras Retenções/Líquido + COFINS isolado, sem o campo "Valor total das deduções" que a grade do Iaçu tem na mesma linha — tolerante ao artefato de OCR "rentenções"); município resolvido via novo código IBGE cadastrado (Brotas de Macaúbas 2904506, confirmado via IBGE/RuaCEP — antes caía no fallback da capital Salvador); código de serviço fixo 0702 (a nota imprime "Item da lista de serviços: 0", que não é um código LC116 válido — mapeado do CNAE 4391600/obras de fundações, decisão do usuário) — nota real nº 70 (M P C ARAUJO→SÃO PEDRO CONSTRUTORA, revitalização de cobertura de estacionamento, R$10.091,13), zero regressão nos layouts existentes (Iaçu e todas as variantes Camaçari — suíte de 184 verdes, incluindo o novo teste `test_brotas_macaubas_layout.py`). Antes: fix no Camaçari/BA ESCANEADO — CPF/CNPJ do TOMADOR ausente do OCR por marca-texto + rabisco sobre o campo no scan original: o campo saía completamente vazio do OCR padrão (não garbled, ausente — o resto da página, inclusive o CNPJ do PRESTADOR, lia normalmente), caindo no sentinela 00000000000000; corrigido de forma aditiva/gated (só dispara quando o CNPJ do tomador já extraído é o sentinela E a nota é escaneada) com um recorte de recuperação que localiza dinamicamente o rótulo "CPF/CNPJ" do bloco TOMADOR por posição de palavra (image_to_data, ancorado após o cabeçalho "TOMADOR"), recorta a região à direita em zoom alto (10x) e converte pra escala de cinza + limiar de luminância antes do OCR final — a binarização remove a cor do destaque/rabisco preservando o traço preto — nota nº 962 (L DE J NASCIMENTO TRANSPORTE→PH GESTÃO, pág.20 do lote Guarajuba 06/2026), agora com tomador correto (25.311.856/0001-09), zero regressão (baseline-vs-fix no lote de 31 páginas muda só a nota-alvo — as outras 2 notas de Camaçari do mesmo lote, págs.16 e 19, já extraíam corretamente pelo pipeline normal e nunca acionam o recorte; suíte de 183 verdes). Antes: fix no Lauro de Freitas/BA ESCANEADO — variante MEI/Simples com rótulo e valor na MESMA linha causava valor zero E razão/município corrompidos: (1) Alíquota/Valor do ISS "inutilizados" (Resolução 94 do CGSN, MEI) imprimem "\*" em vez de "0,00", o OCR colapsa os 2 asteriscos em 1 só e a regex estrita da grade nunca casava, zerando até a Base de Cálculo (283,39, um número real) — com a âncora "VALOR TOTAL DA NOTA FISCAL" também ausente do OCR, o valor final saía 0; corrigido com um fallback tolerante que exige só Dedução/Base (sempre numéricos) e trata Alíquota/ISS ausentes como 0,00 (fiel à face); (2) as regexes de Nome/Razão, Endereço e Inscrição exigiam quebra de linha entre rótulo e valor, mas esta variante imprime os dois na mesma linha — caíam nos sentinelas "Prestador/Tomador Não Identificado"; e Bairro/Município compartilhavam linha com o rótulo seguinte, vazando texto e fazendo o município corrompido cair no fallback de capital (Salvador 2927408) em vez de Lauro de Freitas (2919207); corrigido tolerando rótulo+valor na mesma linha e capturando Bairro/Município de forma não-greedy até o próximo rótulo — nota nº 20264631 (ALFA MEDICAL→SÃO PEDRO, pág.6 do lote "NFS HJHJ"), zero regressão (baseline-vs-fix no lote de 8 notas muda só a nota-alvo; suíte de 182 verdes). Antes: fix de DETECÇÃO — DANFSe Nacional (NFS-e Nacional v1.0) emitida POR um município era roteada para o layout municipal homônimo: o cabeçalho da DANFSe traz "Prefeitura Municipal de <X>", que casava o check municipal (ex.: `PREFEITURA MUNICIPAL DE CAMAÇARI`) ANTES do check da DANFSe Nacional na cadeia de `_detect_layout`; como o parser DANFSe e a regra intermediário→tomador são gated em `LAYOUT_NACIONAL`, a nota caía em `camacari_ba_scan`/`rio_de_janeiro` e o tomador não era extraído; corrigido de forma aditiva com um check estreito e inequívoco ("DANFSe v1.0"/"Documento Auxiliar da NFS-e" — strings do documento nacional padrão, ausentes dos formatos municipais próprios) no TOPO de `_detect_layout` e `_detect_layout_page`, antes de todas as marcas municipais (o check largo por "Chave de Acesso"/"Competência da NFS-e" permanece adiante como fallback de OCR severo) — pág. 11 do lote Guarajuba 06/2026 (nota nº 3, ANA PAULA RIBEIRO DA SILVA MEI → tomador promovido do intermediário PH GESTÃO 25.311.856/0001-09, Camaçari-BA); baseline-vs-fix no lote de 31 páginas mudou EXATAMENTE 3 páginas, todas DANFSe antes mal-roteadas (págs. 5 e 11 vinham de Camaçari, pág. 13 de Rio), zero colateral; suíte de 181 verdes; teste `test_danfse_camacari_deteccao_vs_municipio.py`. Antes: fix no Salvador/BA ESCANEADO — nas notas em que o bloco do TOMADOR não tem cabeçalho "TOMADOR DE SERVIÇOS" (o tomador só se distingue pelo 2º "Nome/Razão Social") E a discriminação contém a palavra "TOMADOR" no CORPO (ex.: "-I8S RETIDO PELO TOMADOR 5% = R$450,00 (DEVIDO NA CIDADE DE CAMAÇARI- BA)"), a heurística que varria o texto INTEIRO achava esse "TOMADOR" do corpo, pulava o recorte dedicado do 2º "Nome/Razão Social" e a extração genérica ancorava ali — o tomador saía com razão "5% = R$450,00 (DEVIDO...)" e CNPJ sentinela 00000000000100; corrigido de forma aditiva/gated restringindo a busca do rótulo do tomador à região ANTES da DISCRIMINAÇÃO (cabeçalho da nota) — nota nº 624 GEOLINER→DELTALINE (lote 06/2026), agora com tomador correto (DELTALINE SERVICOS LTDA / 01.813.680/0001-25 / Salvador-BA), zero regressão (3 testes Salvador + suíte de 179 verdes; baseline-vs-fix na própria nota). Antes: regra de negócio no DANFSe Nacional — quando a nota traz "TOMADOR DO SERVIÇO NÃO IDENTIFICADO NA NFS-e" (campo em branco na origem) mas há um INTERMEDIÁRIO identificado, o intermediário é promovido a tomador e o bloco `<Intermediario>` é esvaziado (nota nº 44, pág.18 do lote Guarajuba Suítes: o MEI prestador lançou a PH Gestão como intermediário e deixou o tomador vazio; para a contabilidade, a PH Gestão é o tomador efetivo — decisão do usuário, regra geral gated por layout); junto, dois fixes no parser de entidade DANFSe: município lido pelo padrão "<Cidade> - <UF> <CEP>" (o rótulo "Município" é cabeçalho de coluna e o valor real fica na linha de valores — antes o resolver pescava a capital "Salvador" do topo do documento) e limpeza da razão social (prefixo do CNPJ com vírgula e inicial isolada da coluna "E-mail" vazada no fim); zero regressão (baseline-vs-fix no lote de 21 notas muda só a nota-alvo). Antes: fix no Osasco/NF-R de Repasse (iFood) ESCANEADO — a detecção do layout já funcionava no OCR, mas as âncoras de extração (feitas para o NF-R digital) quebravam no ruído do scan: número "Nota No,:" (vírgula no lugar do ponto) caía para "00000000", rótulo do CNPJ do tomador degradado ("CPF/CNPJ"→"CEF/CNPI") zerava o CNPJ, e "UF; BA" (ponto-e-vírgula) fazia o UF cair no default SP — com o resolver chamado sem city_hint, Camaçari/BA virava São Paulo/SP; corrigido de forma aditiva/gated (número tolera vírgula; CNPJ do tomador tem fallback ao 1º CNPJ bem-formado do bloco, imune ao rótulo garblado; UF tolera ";"; city_hint passado; e a hora de emissão agora é capturada do rodapé "emitida em … às HH:MM:SS") — nota nº 2279456 iFood, pág.8 do lote Guarajuba Suítes, agora com número/tomador/município corretos, zero regressão nas outras 19 notas do lote. Antes: fix no Camaçari escaneado com página TORTA — quando o scan está levemente inclinado (~1°), a linha "Número da Nota" (a mais alta da célula do cabeçalho) sai do enquadramento dos recortes fixos e a âncora do número não casa, fazendo o número desabar para o fallback "00000000" (nota nº 246, AVANÇO GESTÃO, pág.29 do lote PH Gestão); corrigido com um 3º recorte aditivo que estima a inclinação fina por página (±3°, por variância do perfil horizontal), desentorta e reprocessa a célula — recupera "Número da Nota → 246" sem tocar nos recortes já validados (zero regressão). Antes: fix no Camaçari escaneado — o recorte largo do cabeçalho lia a Inscrição Municipal do prestador no lugar da célula "Número/Data/Código", fazendo sair número e data de emissão errados; corrigido com um recorte estreito aditivo da célula + extração robusta (rejeita candidato de ≥8 dígitos como número, tolera rótulo com 1ª letra cortada, exige letra+dígito no código de autenticidade). Antes: novo layout PJB Construção — fatura de locação de máquinas/bens móveis escaneada, Simões Filho/BA, sem incidência de ISS; detecção no topo da cadeia por razão do emitente + marcador estrutural da fatura, para não colidir com os layouts municipais homônimos citados na nota nem com a planilha-resumo que lista o fornecedor; prestador fixo e tomador parseado do bloco DESTINATÁRIO. Anteriormente: novo layout F&F Comércio — fatura de locação de CFTV; fixes de robustez no Localiza — 2 estruturas de texto —, no São Paulo digital — extração de entidades/valores da nota AMIL —, no Camaçari escaneado — recorte do cabeçalho cortava o número da nota —, no Barreiras — grade de valores da locação de bens móveis caía no fallback zero — e no São Paulo (digital+escaneado) — endereço em linha única e intermediário fantasma); mais recentemente, no DANFE Modelo 55 (NF-e de PRODUTO, ICMS) escaneado (nota nº 764, PENELI METAIS LTDA) — cabeçalho de 3 colunas funde letterhead/caixa DANFE/código de barras e derruba a palavra "DANFE" por completo, quebrando a detecção; corrigido com marca alternativa tolerante a OCR (exclusiva do Modelo 55) e 3 recortes dedicados (emitente, grade de ICMS, linha do item), em branch própria para não misturar com layouts de NFS-e/SERVIÇO.*

