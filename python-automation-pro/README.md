# Conversor de Notas Abrasf

## Visão Geral

Este projeto fornece um conversor de PDFs de Notas Fiscais de Serviço (NFS‑e) e Contratos de Locação para o padrão **ABRASF 2.01 XML**. Ele inclui:

- **Extratores de PDF** com detecção automática de layout, cobrindo **42 layouts** (41 específicos + genérico de fallback) de múltiplos municípios e emissores:
  - **Prefeituras/NFS-e:** Cuiabá/MT, Barreiras/BA, Camaçari/BA (digital, escaneado/OCR, Nota Avulsa da Prefeitura e SISLOC/NFS-e Easy — quatro layouts dedicados), Salvador/BA, Feira de Santana/BA, Simões Filho/BA, Lauro de Freitas/BA, Iaçu/BA, Brotas de Macaúbas/BA (plataforma nfservico.com.br), Mata de São João/BA (plataforma SAATRI), **Monte Santo/BA**, Rosário da Limeira/MG (plataforma FUTURIZE), Rio de Janeiro/RJ (Nota Carioca), São Paulo/SP (digital **e escaneado/OCR** como layouts dedicados), Guarulhos/SP (plataforma Ginfes, escaneada), Ribeirão Pires/SP, Campinas/SP, Joinville/SC, Fortaleza/CE, Brasília/DF (GDF) e o Portal Nacional (DANFSe v1.0).
  - **Faturas de locação / serviços específicos:** Localiza Rent A Car, CPE Tecnologia, Guincho Cidade, B.F. Serviços Ambientais, LMR Engenharia, Geração & Energia, Locontainers, SUL&SEG (Nota de Cobrança), F&F Comércio (locação de CFTV, escaneada), **PJB Construção** (locação de máquinas, Simões Filho/BA, escaneada), **Fatura de Locação Genérica** (qualquer locadora não catalogada) e **ARMAC** (escaneada, tabela multi-item).
  - **Outros:** NF-e de Serviço de Comunicação (Telecom), Osasco/SP NF-R de Repasse (ex.: iFood Benefícios), PASSWORD/eNotas Gateway (NFS-e tributada), ISBET (Nota de Contribuição) e um layout **Genérico** de fallback.
- **OCR integrado** (Tesseract via `pytesseract` + PyMuPDF) para PDFs escaneados/imagem sem camada de texto — o extrator tolera os erros de reconhecimento típicos de cada layout.
- **Transformadores** que mapeiam os dados extraídos para o XML ABRASF 2.01.
- **Interface de linha de comando** (`app.py`) para conversões individuais ou em lote.
- **Interface gráfica** (`gui_app.py`) baseada em *flet* para usuários não‑técnicos.
- **Gerador de contratos de locação** a partir de JSON, produzindo XML pronto para importação.

## Principais Funcionalidades

- Suporte a múltiplos layouts de NFS‑e com detecção automática (`_detect_layout`).
- OCR automático para PDFs escaneados (sem texto extraível).
- Processamento de PDFs com múltiplas páginas e múltiplas notas por página.
- Geração de lote XML (`ListaNfse`).
- Conversão de contratos de locação (dados preenchidos via GUI ou JSON).
- **Avisos de baixa confiança** (`Nfse.avisos`): sinaliza quando um campo caiu em valor de fallback (número/CNPJ zerado, data atual, valor zero) em vez de falhar silenciosamente.
- Logs detalhados e barra de progresso.

## Como Instalar

```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt  # dependências principais
pip install "flet[desktop]"   # para a GUI
```

## Uso via CLI

```bash
# Conversão de um PDF único
python app.py --input path/to/file.pdf --output path/to/output.xml

# PDF com várias notas: converter só páginas específicas (alternadas ou intervalo)
python app.py --input path/to/file.pdf --output path/to/output.xml --pages "1,3,6"
python app.py --input path/to/file.pdf --output path/to/output.xml --pages "1-3,6"

# Conversão em lote (diretório)
python app.py --batch path/to/pdfs --output path/to/output_dir

# Conversão de contrato de locação (JSON)
python app.py --contrato path/to/contrato.json --output path/to/output_dir
```

> `--pages` vale só com `--input` (um único PDF). Aceita páginas soltas (`1,3,6`) e intervalos (`1-3,6`); a numeração é a **página real do PDF** (1-based) e só páginas com nota reconhecida são geradas. Na GUI, o mesmo é feito pela janela de seleção com caixas por página (ver [COMO_USAR_GUI.md](COMO_USAR_GUI.md)).

## Uso via GUI

```bash
python gui_app.py
```

## Estrutura de Diretórios

```
python-automation-pro/
├─ src/                     # Código-fonte principal
│  ├─ extractors/           # Extratores de PDF (pdf_extractor.py: SPPdfExtractor)
│  ├─ transformers/         # Transformadores para XML (abrasf_transformer.py)
│  ├─ models/               # Modelos de dados Pydantic (Nfse, Entidade, Endereco, Valores)
│  └─ utils/                # Utilidades auxiliares (ibge_resolver.py, etc.)
├─ tests/                   # Testes unitários (um test_<cidade>_layout.py por layout)
├─ templates/               # Templates de UI (se houver)
├─ gui_app.py               # Aplicação de desktop baseada em flet
├─ app.py                   # CLI de alto nível
├─ DOCUMENTACAO_CONVERSAO.md# Detalhamento de cada layout suportado
├─ COMO_USAR_GUI.md         # Guia da interface gráfica
└─ README.md                # Documentação principal (este arquivo)
```

## Adicionando um Novo Layout

O extrator é escalável por layout, sem reescrever o núcleo. O checklist típico (ver detalhes em [DOCUMENTACAO_CONVERSAO.md](DOCUMENTACAO_CONVERSAO.md)):

1. Definir a constante `LAYOUT_<NOME>` em `pdf_extractor.py`.
2. Adicionar a marca de detecção em `_detect_layout` **e** `_detect_layout_page`.
3. Plugar ramos específicos onde o layout divergir do genérico (`_extrair_numero`, `_extrair_valores`, `_extrair_entidade`, etc.).
4. Registrar a cidade no `IBGEResolver.KNOWN_CITIES` (`src/utils/ibge_resolver.py`) quando aplicável.
5. Criar `tests/test_<nome>_layout.py` validando contra o **texto real** do PDF (imagem→OCR e/ou digital), não só mocks limpos.

## Contribuição

1. Fork o repositório.
2. Crie uma branch para sua feature (`git checkout -b feature/minha-feature`).
3. Submeta seus testes e documentação.
4. Abra um Pull Request.

---

*Documentação atualizada em 2026‑08‑10 (42 layouts suportados; **fix — Monte Santo/BA: `municipio_incidencia_override` (revisão)** — a nota traz "Local do Serviço: Fora do Município" e a cidade da obra em texto livre ("OBRA: ..., <CIDADE>/<UF>"); a decisão original (quando o layout Monte Santo foi criado, mesma data) tratou essa cidade como "só texto livre, frágil demais" e deixou o override de fora — revista após um review completo do layout com comparação ao XML gerado: a linha "OBRA:" sempre termina no formato ", <CIDADE>/<UF>" (âncora no ÚLTIMO "," da linha, tolerando vírgulas dentro da própria descrição), risco equivalente ao já aceito noutros layouts (ex.: Guarulhos/SP). `Nfse.municipio_incidencia_override` agora também cobre Monte Santo: quando presente, a incidência do ISSQN (`Servico.MunicipioIncidencia`/`Servico.CodigoMunicipio`) vai para o município da obra (ex.: Camaçari, IBGE `2905701`), mantendo o cadastro/endereço do prestador em Monte Santo (IBGE `2921550`) intocado; teste `test_monte_santo_layout.py` estendido com a asserção do override. Antes: **novo layout — Camaçari/BA via SISLOC/NFS-e Easy (Benefix)** — `camacari_sisloc`, nota real nº 24052 (FERIMPORTE SERVICE LTDA → DELTALINE SERVICOS LTDA., R$91,20): PDF totalmente digital cujo gerador desenha rótulo e valor como objetos de texto separados no fluxo do arquivo — o `extract_text()` padrão despejava todos os valores no fim do documento, fora de ordem (número saía "0"); corrigido com uma técnica nova neste projeto — reconstrução do texto a partir das coordenadas de caractere (`_reconstruir_texto_por_coordenadas`, agrupando por proximidade vertical e ordenando por posição horizontal), reproduzindo a ordem visual real da nota antes de qualquer extração de campo; detecção posicionada antes do check genérico de "PREFEITURA MUNICIPAL DE CAMAÇARI" para não colidir com o layout CPqD; valores de retenção extraídos por posição fixa (mesmo princípio do layout Cuiabá); zero regressão (suíte 189→**191 verdes**); teste `test_camacari_sisloc_layout.py`. **fix anterior — nota-fantasma por página em DANFSe Nacional escaneada com UMA única nota** — `danfse_nacional`, PDF "análise de notas SP-iss retido - inss retido.pdf" (062026), págs. 3 e 4 (nota real nº 175, ARMANDO CACAMBA LTDA → SÃO PEDRO CONSTRUTORA, Várzea Grande/MT, locação de caçambas, R$1.650,00): toda DANFSe Nacional imprime seu próprio título "DANFSe v1.0" nos primeiros ~70 caracteres da página (logo após o rótulo "Chave de Acesso da NFS-e") — mesmo com uma única nota ali; o split de múltiplas-notas-por-página de `parse_multiple` (`headers_regex`) cortava nesse 1º "DANFSe", fatiando esse preâmbulo de boilerplate (sem CNPJ/valor nenhum) como uma nota-fantasma própria (número "00000000", prestador/tomador = fragmento garbage "Serviço eletrônica") ANTES da nota real — dando a impressão de "número zero" pra quem olha a 1ª entrada da página; corrigido só tratando como início de nova nota as ocorrências de "DANFSe" a partir de ~200 caracteres do início do bloco (uma 2ª nota genuinamente colada na mesma página só teria seu título bem depois de todo o conteúdo da 1ª), preservando o caso de duas notas de fato coladas na mesma página. Zero regressão (código compartilhado por todos os layouts, sem gate específico — validado com baseline-vs-fix num recorte real de 6 páginas do mesmo lote, mudando só as 2 páginas-alvo). **2ª correção na MESMA nota:** o tomador saía com o CNPJ idêntico ao do prestador (`02.488.708/0001-69` em vez de `03.051.741/0001-90`) — a coluna "CNPJ/CPF/NIF" da grade é comum a todas as entidades e o OCR lê as linhas fora de ordem, colando o CNPJ do prestador dentro do bloco do tomador; corrigido em `_extrair_entidade` (função genérica, compartilhada por ~30 layouts): o CNPJ do prestador já extraído é guardado em `self._cnpj_prestador_extraido` e um candidato idêntico dentro do bloco do tomador/intermediário é descartado a favor de outro CNPJ válido do mesmo bloco, só voltando a aceitá-lo como último recurso se não houver nenhum outro — aditivo, não altera o comportamento quando o CNPJ correto já é encontrado de primeira (validado sem mudança nas outras 6 notas do mesmo lote de 6 páginas). Suíte **189 verdes**; teste `test_danfse_nacional_pagina_unica_sem_fantasma.py` (ambos os bugs). Antes: fix — DANFSe Nacional (Criciúma/SC) com valores estruturados em PONTO decimal saindo zerados — nota real nº 730080 (THOMSON REUTERS BRASIL → CAFES FINOS VITORIA DA CONQUISTA, plataforma Domínio Sistemas): todos os campos monetários da grade vinham com ponto decimal ("R$ 372.96") em vez da vírgula brasileira, e o regex de captura exigia vírgula — corrigido com regex tolerante a ambos os separadores + novo helper `_parse_valor_tolerante`; suíte de 187 verdes; teste `test_danfse_nacional_ponto_decimal.py`. Antes: novo layout Guarulhos/SP — plataforma Ginfes (guarulhos.ginfes.com.br), foto/CamScanner —, nota real nº 3 (KICHLER → SÃO PEDRO CONSTRUTORA, obra Sede de Tecnologia MTI/SECITECI em Cuiabá/MT, R$4.511,41): a leitura padrão de página inteira (zoom 3x) só recuperava ~850 caracteres, perdendo o Código de Verificação, o Local da Prestação e toda a grade "Cálculo do ISSQN devido no Município" (células cinza densas) — a nota inteira era descartada como "Layout não reconhecido"/0 XML; corrigido com zoom 4x/PSM 6 para o corpo da página + um recorte dedicado (`_ocr_recut_guarulhos`, 3 sub-recortes em zoom alto e binarização, mesmo racional do Camaçari) que resolve os campos e devolve um bloco de texto canônico e limpo. Decisão de negócio do usuário: o serviço é construção civil (item 7.02) prestado em OUTRO município — a própria nota traz "Local da Prestação: Cuiabá-MT" + "Natureza Operação: Tributação fora do município" + "ISS a reter: Não" —, então a incidência do ISSQN vai para o município da obra (Cuiabá, `5103403`), não para o do prestador (Guarulhos, `3518800`); implementado como um campo novo e aditivo `Nfse.municipio_incidencia_override` (`None` em todos os outros layouts, usado pelo transformer só quando setado — o endereço próprio do prestador continua apontando Guarulhos, só a tag `<Servico><MunicipioIncidencia>` muda). Município Guarulhos cadastrado em `KNOWN_CITIES` (`3518800`, confirmado via IBGE/RuaCEP, CEP 07043-120 dentro da faixa do município). Zero regressão nos layouts existentes; suíte de 185 verdes, incluindo o novo teste `test_guarulhos_layout.py`. Antes: novo layout Brotas de Macaúbas/BA — município nunca visto antes, PDF de origem em foto/JPG (tipicamente de cabeça para baixo), plataforma nfservico.com.br (mesma do Iaçu/BA); antes desta implementação a nota era 100% rejeitada (0 XML, "Layout não reconhecido"); reaproveita o parser de entidade do Iaçu por estrutura (mesmos rótulos "PRESTADOR/TOMADOR DE SERVIÇOS", "Nome/Razão Social:", "CPF/CNPJ:", endereço em linha única), mas com extrator próprio (endereço do prestador tem complemento — "CASA" — entre número e bairro, e um "|" de OCR colado no número "26-B"; razão do tomador vinha colada ao nome/CREA do engenheiro responsável impresso à direita, cortada pelo token "Eng."); caixa de cabeçalho reaproveita o MESMO recorte dedicado do Iaçu (`_ocr_header_box_iacu`), agora com suporte a ângulo de rotação (aditivo — Iaçu continua com angle=0 inalterado) porque as fotos de Brotas de Macaúbas chegam invertidas (180°) e o recorte sem rotação caía no canto errado (caixa vazia); grade de valores própria (Base de Cálculo/Alíquota/ISS/Crédito + IR/CSLL/Outras Retenções/Líquido + COFINS isolado, sem o campo "Valor total das deduções" que a grade do Iaçu tem na mesma linha — tolerante ao artefato de OCR "rentenções"); município resolvido via novo código IBGE cadastrado (Brotas de Macaúbas 2904506, confirmado via IBGE/RuaCEP — antes caía no fallback da capital Salvador); código de serviço fixo 0702 (a nota imprime "Item da lista de serviços: 0", que não é um código LC116 válido — mapeado do CNAE 4391600/obras de fundações, decisão do usuário) — nota real nº 70 (M P C ARAUJO→SÃO PEDRO CONSTRUTORA, revitalização de cobertura de estacionamento, R$10.091,13), zero regressão nos layouts existentes (Iaçu e todas as variantes Camaçari — suíte de 184 verdes, incluindo o novo teste `test_brotas_macaubas_layout.py`). Antes: fix no Camaçari/BA ESCANEADO — CPF/CNPJ do TOMADOR ausente do OCR por marca-texto + rabisco sobre o campo no scan original: o campo saía completamente vazio do OCR padrão (não garbled, ausente — o resto da página, inclusive o CNPJ do PRESTADOR, lia normalmente), caindo no sentinela 00000000000000; corrigido de forma aditiva/gated (só dispara quando o CNPJ do tomador já extraído é o sentinela E a nota é escaneada) com um recorte de recuperação que localiza dinamicamente o rótulo "CPF/CNPJ" do bloco TOMADOR por posição de palavra (image_to_data, ancorado após o cabeçalho "TOMADOR"), recorta a região à direita em zoom alto (10x) e converte pra escala de cinza + limiar de luminância antes do OCR final — a binarização remove a cor do destaque/rabisco preservando o traço preto — nota nº 962 (L DE J NASCIMENTO TRANSPORTE→PH GESTÃO, pág.20 do lote Guarajuba 06/2026), agora com tomador correto (25.311.856/0001-09), zero regressão (baseline-vs-fix no lote de 31 páginas muda só a nota-alvo — as outras 2 notas de Camaçari do mesmo lote, págs.16 e 19, já extraíam corretamente pelo pipeline normal e nunca acionam o recorte; suíte de 183 verdes). Antes: fix no Lauro de Freitas/BA ESCANEADO — variante MEI/Simples com rótulo e valor na MESMA linha causava valor zero E razão/município corrompidos: (1) Alíquota/Valor do ISS "inutilizados" (Resolução 94 do CGSN, MEI) imprimem "\*" em vez de "0,00", o OCR colapsa os 2 asteriscos em 1 só e a regex estrita da grade nunca casava, zerando até a Base de Cálculo (283,39, um número real) — com a âncora "VALOR TOTAL DA NOTA FISCAL" também ausente do OCR, o valor final saía 0; corrigido com um fallback tolerante que exige só Dedução/Base (sempre numéricos) e trata Alíquota/ISS ausentes como 0,00 (fiel à face); (2) as regexes de Nome/Razão, Endereço e Inscrição exigiam quebra de linha entre rótulo e valor, mas esta variante imprime os dois na mesma linha — caíam nos sentinelas "Prestador/Tomador Não Identificado"; e Bairro/Município compartilhavam linha com o rótulo seguinte, vazando texto e fazendo o município corrompido cair no fallback de capital (Salvador 2927408) em vez de Lauro de Freitas (2919207); corrigido tolerando rótulo+valor na mesma linha e capturando Bairro/Município de forma não-greedy até o próximo rótulo — nota nº 20264631 (ALFA MEDICAL→SÃO PEDRO, pág.6 do lote "NFS HJHJ"), zero regressão (baseline-vs-fix no lote de 8 notas muda só a nota-alvo; suíte de 182 verdes). Antes: fix de DETECÇÃO — DANFSe Nacional (NFS-e Nacional v1.0) emitida POR um município era roteada para o layout municipal homônimo: o cabeçalho da DANFSe traz "Prefeitura Municipal de <X>", que casava o check municipal (ex.: `PREFEITURA MUNICIPAL DE CAMAÇARI`) ANTES do check da DANFSe Nacional na cadeia de `_detect_layout`; como o parser DANFSe e a regra intermediário→tomador são gated em `LAYOUT_NACIONAL`, a nota caía em `camacari_ba_scan`/`rio_de_janeiro` e o tomador não era extraído; corrigido de forma aditiva com um check estreito e inequívoco ("DANFSe v1.0"/"Documento Auxiliar da NFS-e" — strings do documento nacional padrão, ausentes dos formatos municipais próprios) no TOPO de `_detect_layout` e `_detect_layout_page`, antes de todas as marcas municipais (o check largo por "Chave de Acesso"/"Competência da NFS-e" permanece adiante como fallback de OCR severo) — pág. 11 do lote Guarajuba 06/2026 (nota nº 3, ANA PAULA RIBEIRO DA SILVA MEI → tomador promovido do intermediário PH GESTÃO 25.311.856/0001-09, Camaçari-BA); baseline-vs-fix no lote de 31 páginas mudou EXATAMENTE 3 páginas, todas DANFSe antes mal-roteadas (págs. 5 e 11 vinham de Camaçari, pág. 13 de Rio), zero colateral; suíte de 181 verdes; teste `test_danfse_camacari_deteccao_vs_municipio.py`. Antes: fix no Salvador/BA ESCANEADO — nas notas em que o bloco do TOMADOR não tem cabeçalho "TOMADOR DE SERVIÇOS" (o tomador só se distingue pelo 2º "Nome/Razão Social") E a discriminação contém a palavra "TOMADOR" no CORPO (ex.: "-I8S RETIDO PELO TOMADOR 5% = R$450,00 (DEVIDO NA CIDADE DE CAMAÇARI- BA)"), a heurística que varria o texto INTEIRO achava esse "TOMADOR" do corpo, pulava o recorte dedicado do 2º "Nome/Razão Social" e a extração genérica ancorava ali — o tomador saía com razão "5% = R$450,00 (DEVIDO...)" e CNPJ sentinela 00000000000100; corrigido de forma aditiva/gated restringindo a busca do rótulo do tomador à região ANTES da DISCRIMINAÇÃO (cabeçalho da nota) — nota nº 624 GEOLINER→DELTALINE (lote 06/2026), agora com tomador correto (DELTALINE SERVICOS LTDA / 01.813.680/0001-25 / Salvador-BA), zero regressão (3 testes Salvador + suíte de 179 verdes; baseline-vs-fix na própria nota). Antes: regra de negócio no DANFSe Nacional — quando a nota traz "TOMADOR DO SERVIÇO NÃO IDENTIFICADO NA NFS-e" (campo em branco na origem) mas há um INTERMEDIÁRIO identificado, o intermediário é promovido a tomador e o bloco `<Intermediario>` é esvaziado (nota nº 44, pág.18 do lote Guarajuba Suítes: o MEI prestador lançou a PH Gestão como intermediário e deixou o tomador vazio; para a contabilidade, a PH Gestão é o tomador efetivo — decisão do usuário, regra geral gated por layout); junto, dois fixes no parser de entidade DANFSe: município lido pelo padrão "<Cidade> - <UF> <CEP>" (o rótulo "Município" é cabeçalho de coluna e o valor real fica na linha de valores — antes o resolver pescava a capital "Salvador" do topo do documento) e limpeza da razão social (prefixo do CNPJ com vírgula e inicial isolada da coluna "E-mail" vazada no fim); zero regressão (baseline-vs-fix no lote de 21 notas muda só a nota-alvo). Antes: fix no Osasco/NF-R de Repasse (iFood) ESCANEADO — a detecção do layout já funcionava no OCR, mas as âncoras de extração (feitas para o NF-R digital) quebravam no ruído do scan: número "Nota No,:" (vírgula no lugar do ponto) caía para "00000000", rótulo do CNPJ do tomador degradado ("CPF/CNPJ"→"CEF/CNPI") zerava o CNPJ, e "UF; BA" (ponto-e-vírgula) fazia o UF cair no default SP — com o resolver chamado sem city_hint, Camaçari/BA virava São Paulo/SP; corrigido de forma aditiva/gated (número tolera vírgula; CNPJ do tomador tem fallback ao 1º CNPJ bem-formado do bloco, imune ao rótulo garblado; UF tolera ";"; city_hint passado; e a hora de emissão agora é capturada do rodapé "emitida em … às HH:MM:SS") — nota nº 2279456 iFood, pág.8 do lote Guarajuba Suítes, agora com número/tomador/município corretos, zero regressão nas outras 19 notas do lote. Antes: fix no Camaçari escaneado com página TORTA — quando o scan está levemente inclinado (~1°), a linha "Número da Nota" (a mais alta da célula do cabeçalho) sai do enquadramento dos recortes fixos e a âncora do número não casa, fazendo o número desabar para o fallback "00000000" (nota nº 246, AVANÇO GESTÃO, pág.29 do lote PH Gestão); corrigido com um 3º recorte aditivo que estima a inclinação fina por página (±3°, por variância do perfil horizontal), desentorta e reprocessa a célula — recupera "Número da Nota → 246" sem tocar nos recortes já validados (zero regressão). Antes: fix no Camaçari escaneado — o recorte largo do cabeçalho lia a Inscrição Municipal do prestador no lugar da célula "Número/Data/Código", fazendo sair número e data de emissão errados; corrigido com um recorte estreito aditivo da célula + extração robusta (rejeita candidato de ≥8 dígitos como número, tolera rótulo com 1ª letra cortada, exige letra+dígito no código de autenticidade). Antes: novo layout PJB Construção — fatura de locação de máquinas/bens móveis escaneada, Simões Filho/BA, sem incidência de ISS; detecção no topo da cadeia por razão do emitente + marcador estrutural da fatura, para não colidir com os layouts municipais homônimos citados na nota nem com a planilha-resumo que lista o fornecedor; prestador fixo e tomador parseado do bloco DESTINATÁRIO. Anteriormente: novo layout F&F Comércio — fatura de locação de CFTV; fixes de robustez no Localiza — 2 estruturas de texto —, no São Paulo digital — extração de entidades/valores da nota AMIL —, no Camaçari escaneado — recorte do cabeçalho cortava o número da nota —, no Barreiras — grade de valores da locação de bens móveis caía no fallback zero — e no São Paulo (digital+escaneado) — endereço em linha única e intermediário fantasma).*
